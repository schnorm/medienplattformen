# CLAUDE.md – Wissenschaftliches Schreiben an der IU

**Projekt:** [wird pro Arbeit ausgefüllt – Titel/Arbeitstitel] · **Papiertyp:** [Hausarbeit/Fallstudie/Seminararbeit/Projektbericht] · **Institution:** IU Internationale Hochschule

## Schnellreferenz – Session starten

| Du willst … | Schreibe in neuer Session |
|---|---|
| Umgebung prüfen (einmalig zu Beginn) | „Setup-Check" |
| Kapitelplan erstellen (Standard, 1 Session) | „Plan kompakt" |
| Kapitelplan ausführlich (empirische Seminararbeit) | „Plan ausführlich" |
| Nächstes Kapitel schreiben | „Schreib-Modus: nächstes Kapitel" |
| Ein Argument prüfen lassen | „Stresstest auf: <Argument>" |
| Arbeit mit fremden Augen gegen die Aufgabenstellung prüfen (alle Kapitel FERTIG) | „Gegenlesung" |
| Ganze Arbeit inhaltlich prüfen (sobald alle Kapitel FERTIG, **vor** dem Voll-Audit) | „Gesamt-Stresstest" |
| Erstes Kapitel gegenprüfen (früh, ohne Score) | „Audit Kapitel: <name>" |
| Kapitelplan auf Kohärenz prüfen (empfohlen vor dem Schreiben) | „Plan-Audit" |
| Mechanisch prüfen, ob das Schreiben starten kann | „Bereitschafts-Check" |
| Arbeit/Kapitelplan prüfen | „Audit" |
| Benotetes Tutor-Feedback in Lernpunkte übersetzen | „Feedback einarbeiten" |
| Status erfahren | „Wo stehen wir?" |

## Grundprinzipien

- **Kompilieren ist Sache des Nutzers, nicht meine.** Nach Edits an `.tex`-Dateien nicht ad-hoc selbst `lualatex`/`latexmk` laufen lassen, um die eigene Änderung zu verifizieren – der Nutzer kompiliert lokal. Ausnahme: Teil-Check D (Build) im `pruef-modus` bleibt bestehen, da er ein bewusster, score-relevanter Audit-Bestandteil ist und kein Ad-hoc-Selbstcheck.
- **Dateien sind Wahrheit, nicht der Chat-Verlauf.** `aufgabe.md`, `kapitelplan.md`, `kapitelplan.draft.md` und `chapters/**/*.tex` gelten immer vor Erinnerung/Memory – bei Zweifel lesen, nicht annehmen.
- **Erst prüfen, dann behaupten.** Bevor eine belastbare Aussage über die Außenwelt (Marktlage, Konkurrenzprodukte, Werkzeuge, Zahlen) **oder** eine formale/prozedurale Behauptung („das ist ein Blocker", „so darf man nicht zitieren", „die Regel verlangt X") getroffen wird: die maßgebliche Quelle konsultieren – Web für Außenwelt-Fakten, die **Projektdatei** für interne Regeln (`aufgabe.md`, das optionale `sources/Anmerkungen vom Prüfer.md`, `hard-rules-formal.md`, `typen/<typ>.md`). Kein Rating, kein Blocker, keine Formalia-Aussage aus dem Gedächtnis. Im Zweifel als Hypothese kennzeichnen und verifizieren, nicht als Fakt formulieren.
- **`main.tex`, `pages/cover.tex` und `references.bib` nicht umbauen** (Ausnahme: `references.bib` per Zotero/BBT-Auto-Export) – gilt unabhängig vom aktiven Skill. **Einzige erlaubte Änderung an `main.tex`: die dort markierten Aktivierungsblöcke ein- oder auskommentieren** (Gender-Disclaimer, Abbildungs-/Tabellenverzeichnis, Anhangsverzeichnis, Anhang). Ein Anhang, der nur deshalb im PDF fehlt, weil niemand den Block einkommentieren durfte, ist ein Abgabefehler; `check_formalia.py` meldet genau diesen Fall. Regeln dazu: `_shared/hard-rules-formal.md`. Alles andere in `main.tex` und `cover.tex` bleibt unangetastet. Projektspezifische Angaben (Titel, Papiertyp, Modul, Tutor:in, Abgabedatum, PDF-Schlagworte) stehen **ausschließlich in `pages/meta.tex`** – `cover.tex` und die PDF-Metadaten ziehen sich alles von dort. `meta.tex` ist im Gegensatz zu `main.tex`/`cover.tex` **editierbar**: Der `setup-check` erhebt die Angaben zu Projektbeginn im Dialog und trägt sie dort ein; unbekannte Felder bleiben als Platzhalter stehen und werden als OFFEN gemeldet.
- **Single Source of Truth**: `aufgabe.md` für die Aufgabenstellung (destilliert in Plan-Modus Schritt 0 – spätere Sessions lesen diese Datei, nicht das Aufgaben-PDF), `kapitelplan.md` für Struktur/Argumentation, `chapters/**/*.tex` für den finalen Text, `references.bib` für Zitationen (nie manuell editieren).
- **These und Kernargumente kommen vom Nutzer.** Ich hinterfrage und schärfe, liefere aber nicht die inhaltliche Position selbst. Das ist zugleich die Bedingung der IU-KI-Richtlinie: Die Verantwortung für alle Inhalte liegt beim Studierenden; jede von mir gelieferte Quelle, Zahl oder Behauptung muss der Nutzer am Original verifizieren, bevor sie in die Arbeit übernommen wird.
- **Freigabepflichtige Punkte aktiv per `AskUserQuestion` klären, nicht nur im Fließtext vermerken.** Trägt ein Arbeitsauftrag (z. B. ein Punkt in `AENDERUNGEN.md`, `pruefbericht.md`) einen Vorbehalt wie „nur mit expliziter Freigabe ändern", oder betrifft er These/Kernargumente/Leitfrage, dann reicht es nicht, den Punkt auszulassen und die Begründung nur in der Zusammenfassung zu erwähnen – das verlagert die Entscheidung stillschweigend auf den Nutzer, der sie leicht überliest. Stattdessen noch in derselben Session aktiv nachfragen, bevor der nächste Schritt (Audit, Commit, PR) beginnt.
- **Unsicherheit wird zur Prüfaufgabe für den Nutzer, nicht zur Fußnote.** Wo ich etwas nicht selbst verifizieren kann – Scan ohne Textebene, Quelle hinter Bezahlschranke, Heuristik mit bekannter Fehlerquote, widersprüchliche Angaben – wird das **nicht** stillschweigend als geprüft verbucht und auch nicht in einem Nebensatz erwähnt. Stattdessen steht am Ende der Antwort eine eigene Liste **„Bitte manuell prüfen"**: je Punkt (1) was genau nachzusehen ist, (2) wo – Datei, Zeile, Quelle, Seite, (3) woran zu erkennen ist, dass es stimmt, und (4) warum ich es nicht konnte. Ein „konnte ich nicht prüfen" ohne diese vier Angaben ist wertlos: Die Verantwortung für jede Aussage liegt laut KI-Richtlinie beim Studierenden, und dafür muss klar sein, wo er hinsehen soll.
- **Keine personenbezogenen Daten in die Session geben** (KI-Richtlinie der IU): Interview-Transkripte vor der Verarbeitung anonymisieren (Namen → B1, B2 …), keine vertraulichen Unternehmensdaten ohne Freigabe.
- **Nur „Pflicht"-Regeln aus `typen/<typ>.md` zählen als FAIL.** „Optional"/„empfohlen" sind Hinweise, „entfällt" wird gar nicht geprüft.
- **Checkpoints und gespeicherte Dateien werden im Chat sichtbar quittiert**, nie nur still geschrieben (siehe `plan-modus`/`schreib-modus` → Sichtbarkeitspflicht).
- **Explizite Korrekturen des Nutzers sofort als `[LERNEN:kategorie]` eintragen** (nicht am Sessionende sammeln – Sessions enden abrupt) und quittieren („📝 In PERSISTENT.md gelernt: …"). **Default-Ziel ist `PERSISTENT.md`**, die Datei, die von Projekt zu Projekt kopiert wird; nach `MEMORY.md` gehört ein Eintrag nur, wenn er sich konkret auf *diese* Arbeit bezieht (dieses Thema, dieser Papiertyp, diese Aufgabenstellung). Alles zu Stil, Ton, Zitier-Sonderfällen, Modellwahl und Session-Führung ist projektübergreifend – im falschen File ist es beim nächsten Projekt verloren. Widersprüchliche neue Einträge markieren den alten als `[ÜBERHOLT]`. Wo Skills `MEMORY.md` lesen, gilt: beide Dateien lesen; bei Widerspruch gewinnt `MEMORY.md`.

## Session-Regeln (Memory-Management)

Kern des Workflows: **viele kurze Sessions statt einer großen.** Lange Sessions verbrennen Tokens und werden gegen Ende unzuverlässig. Deshalb:

1. **Session-Start-Ritual (<1 Min.):** Statustabelle unten + „Aktuelle Richtung" lesen → `PERSISTENT.md`-/`MEMORY.md`-Einträge beachten → nur die Dateien laden, die das heutige Arbeitspaket braucht (der aktive Skill listet sie). Nicht pauschal Plan und alle Kapitel einlesen.
2. **Arbeitspaket-Größen:** Schreiben = 1 Hauptteil-Kapitel pro Session (**Ausnahme klein:** Einleitung + Schluss dürfen zusammen in eine Session – je ~10 %, inhaltlich zusammenhängend) · Audit = 1 Session, die **Überarbeitung danach ist eine eigene Schreib-Session** (`pruefbericht.md` trägt die Arbeitsliste hinüber – nicht im vollen Audit-Kontext weiterschreiben) · Plan kompakt = 1 Session, ausführlich 1–2. Wo ein Paket sinnvoll geschnitten wird (Plan-Modus nach Schritt 1), sagt der Skill an Ort und Stelle.
3. **Session-Ende-Ritual:** Checkpoint in der Draft-/Plan-Datei → Statustabelle aktualisieren → „Aktuelle Richtung" auf den nächsten Schritt setzen → im Chat quittieren.
4. **Kontext fast voll oder Session driftet?** Nicht „noch schnell fertig machen" – Checkpoint schreiben, Session beenden, frisch weitermachen. Die Dateien garantieren verlustfreie Fortsetzung.

## Ordnerstruktur

Vollständiger, kommentierter Baum: `.claude/skills/_shared/projektstruktur.md` – laden `setup-check` (prüft die Struktur) und `plan-modus` Schritt 2.5 (Dateinamen der Kapitel). Für jede andere Session genügen die Single-Source-Dateien:

| Datei / Ordner | Rolle |
|---|---|
| `aufgabe.md` | destillierte Aufgabenstellung – Single Source statt Aufgaben-PDF |
| `kapitelplan.md` | Struktur und Argumentation (Draft: `kapitelplan.draft.md`) |
| `chapters/**/*.tex` | finaler Text – ein Ordner je Hauptkapitel, Master bindet Subsections |
| `references.bib` | Zitationen (BBT-Export aus Zotero, nie manuell editieren) |
| `pages/meta.tex` | alle Projektangaben – einzige editierbare Datei in `pages/` |
| `quellencheck.md` · `quellencheck-state.json` | Volltextabgleich aller Zitationen: Bericht (pro Lauf neu) und Urteile (überdauern) – erzeugt `check_quellentreue.py`, nie von Hand ändern |
| `AENDERUNGEN.md` · `pruefbericht.md` | Arbeitsaufträge in Runden · letzter Audit-Bericht |
| `PERSISTENT.md` · `MEMORY.md` | projektübergreifende Präferenzen · Lernpunkte nur dieser Arbeit |
| `SKILL-ANPASSUNGEN.md` | offene Änderungsvorschläge an Skills, Skripten und dieser Datei – kein Abgabebestandteil |
| `sources/` · `sources/literature/` | IU-Leitfäden + optionale Prüfer-Anmerkungen · eigene Literatur |

## Workflow-Fahrplan

| Phase | Skill | Wann | Output |
|---|---|---|---|
| Setup | `setup-check` | Einmalig bei neuem Projektordner | Bestätigung: Umgebung bereit |
| Planen | `plan-modus` | Aufgabenstellung liegt vor, kein Kapitelplan | `aufgabe.md` + `kapitelplan.md` |
| Schreiben | `schreib-modus` | `kapitelplan.md` existiert | `chapters/**/*.tex` |
| Gegenlesen | `gegenlesung` | Sobald alle Kapitel FERTIG – **vor** Gesamt-Stresstest und Voll-Audit | neue Runde in `AENDERUNGEN.md` |
| Stresstest | `stresstest` | Jederzeit einzelnes Argument · Gesamt-Stresstest über die ganze Arbeit, **sobald alle Kapitel FERTIG sind – nach der Gegenlesung, vor dem Voll-Audit** (Stresstest prüft Inhalt, Prüf-Modus prüft Regeln; inhaltliche Befunde ändern den Text und würden ein vorher gefahrenes Audit entwerten) | Gegenargumente + Stärke · Gesamt: neue Runde in `AENDERUNGEN.md` |
| Prüfen | `pruef-modus` | Kapitel-Audit nach Kapitel 1 · Voll-Audit vor Abgabe, **erst nach abgearbeiteter Gegenlesung und abgearbeitetem Gesamt-Stresstest** (Umfänge: siehe Skill) | `pruefbericht.md` mit Score |

**Kompakt-Prüfkette (nur Hausarbeit & Projektbericht):** Bei den beiden kleinsten Formaten ist der Gesamt-Stresstest **optional** – Default-Kette dort: Gegenlesung → Abarbeiten → Voll-Audit. Grund: Bei 7–10 Seiten prüft die Gegenlesung dieselben wenigen Kernargumente ohnehin mit; ein voller Extra-Durchgang kostet mehr Session, als er findet. Auf Wunsch oder bei erkennbar wackliger Argumentation bleibt die volle Kette jederzeit möglich; ein bewusst übersprungener Gesamt-Stresstest wird im Prüfbericht vermerkt. **Fallstudie und Seminararbeit fahren immer die volle Kette** (eigene Analysen bzw. Empirie sind die typischen Overclaim-Quellen).

**Mehrsession-Fähigkeit Plan-Modus**: Zwischenstand liegt fortlaufend in `kapitelplan.draft.md` (Checkpoint nach jedem Schritt). Bei Sessionstart immer zuerst prüfen, ob `kapitelplan.draft.md` existiert, statt bei Schritt 0 neu zu beginnen. Schreib-Modus liest `kapitelplan.md` per Read-Tool, nie aus dem Gesprächsverlauf.

## Format `AENDERUNGEN.md`

Verbindliches Runden-Format in `.claude/skills/_shared/aenderungen-format.md` – laden nur die Skills, die die Datei schreiben oder abarbeiten (`gegenlesung`, `stresstest` → Gesamt, `schreib-modus` → Abarbeiten). Zwei Regeln gelten überall, auch ohne diese Datei: `[FREIGABE]`-Punkte (These, Kernargumente, Leitfrage) nur nach aktiver Rückfrage umsetzen, und „## Offen" ist leer, bevor ein Voll-Audit läuft.

## Papiertyp- und Formalia-Regeln

- **Papiertyp-spezifisch** (Pflichtregeln, Bewertungsgewichte, Kapitelgerüst, Kernfragen, Voice, Audit-Checks): eine Datei pro Typ in `.claude/skills/_shared/typen/<typ>.md` – geladen, sobald der Papiertyp feststeht, bewusst nicht hier. Quellen-Zahlen dort sind Richtwerte aus der Praxis, keine IU-Vorgabe.
- **Formale Hard Rules** (Zitationen, LaTeX, Pronomen, Struktur, Schreibstil, gendersensible Sprache): `.claude/skills/_shared/hard-rules-formal.md` – braucht nur `schreib-modus`/`pruef-modus`. Die sicherheitskritische Ausnahme (`main.tex`/`cover.tex`/`references.bib` nicht umbauen, nur Aktivierungsblöcke schalten) steht oben in den Grundprinzipien.
- **Mechanische Prüfungen gebündelt aufrufen**: `python .claude/skills/_shared/scripts/check_all.py` fährt Keys, Formalia, Bib-Hygiene und Status in einem Lauf (`--kapitel <ordner>` beim Schreiben, `--mit-quellen` im Audit) – statt vier Einzelaufrufe mit je eigener Ausgabe. Die Einzelskripte bleiben für die Detailarbeit an einem Befund aufrufbar.

## „Wo stehen wir?" (Status-Anfrage – definierter Ablauf)

Reine Lese-Operation, keine Arbeitssession:
1. Statustabelle unten + „Aktuelle Richtung" + „Fristen" lesen (sind ohnehin geladen).
2. `python .claude/skills/_shared/scripts/check_status.py` laufen lassen – meldet Widersprüche Tabelle ↔ Dateisystem deterministisch.
3. Antwort in höchstens 8 Zeilen: Stand heute (1 Satz) · zuletzt abgeschlossener Schritt · **nächster Schritt** als konkreter Session-Einstieg · offene Blocker/Fristen · check_status-Funde, falls vorhanden.
4. Keine Kapitel, keinen Plan, kein MEMORY pauschal laden. Bei Widerspruch Tabelle ↔ Dateisystem gilt das Dateisystem – Status korrigieren und quittieren ist die einzige zulässige Änderung in dieser Anfrage.

## Aktueller Projektstatus

**Zwei Arten von Information, zwei Regeln:**
- **Zeilenbestand** (welche Komponenten existieren) wird aus `kapitelplan.draft.md` (bzw. `kapitelplan.md`, sobald final) abgeleitet – nie geschätzt.
- **Statuswerte** (FERTIG / IN ARBEIT / ÜBERARBEITUNG NÖTIG / NICHT BEGONNEN) pflegt ausschließlich der jeweilige Skill nach Speichern/Audit – Datei-Existenz ≠ fertig. Fehlt die Datei einer FERTIG-Komponente, gilt das Dateisystem: Status korrigieren und Widerspruch melden.

| Komponente (Identifikator) | Pfad | Status | Notizen |
|---|---|---|---|
| Aufgabenstellung | `aufgabe.md` | FERTIG | Aufgabenstellung 2 (Hobbyköch:innen), Nische Zero-Waste |
| Kapitelplan | `kapitelplan.md` | FERTIG | Plattform „Resteria" (Name 2026-07-21 gesetzt, Kollision „Restlos Glücklich" behoben) — 3 Kapitel, 12 Subsections (2026-07-24: `sec:iteratives_design` ergänzt, Befund 5.2) |
| `sec:einleitung` | `chapters/01_einleitung/einleitung.tex` | FERTIG | Kürzungsrunde 2026-07-28: 579 → 521 Wörter (14,3 % Anteil, im Soll). Gekürzt ausschließlich in `01_ausgangslage_und_problem.tex` (Kontrastfiguren, doppelte Lückenbeschreibung) sowie im Master-Wegweiser; `02_zielsetzung` und `03_vorgehen` bewusst unangetastet, da nahe der Halbseiten-Schwelle. Verengungsbegründung und Zeitbezug (SDG 12.3) unverändert. `check_formalia.py` 0 FEHLER.
| `sec:durchfuehrung` | `chapters/02_durchfuehrung/…` | FERTIG | Kürzungsrunde 2026-07-28: 3.507 → 2.690 Wörter (74,0 % Anteil, im Soll), Hauptlast der Runde. `03_konzept.tex` von 1.041 auf ~560 Wörter (lag mit elf Absätzen weit über dem Richtwert 4–8), `01_wettbewerbsanalyse.tex` von 986 auf ~500. `fig:konzeptfluss` gestrichen (nach Freigabe; war ohne `\autoref`-Verweis und kein Lieferobjekt). Alle sechs Plattformen, alle Pflicht-Tabellen, alle Abhakliste-Schlüsselbegriffe und die Event-Kalender-Begründung erhalten. `check_formalia.py` 0 FEHLER, 6/6 BBT-Keys valide. **Nachtrag 2026-07-28:** `03_konzept.tex:3` um einen Begründungssatz zur Freemium-Trennlinie erweitert (+30 Wörter, jetzt ~2.720) — bedient das Bewertungskriterium Transfer („begründe jeweils Deine Wahl", Aufgabenstellung S. 6), da das Bezahlmodell bis dahin als gesetzte Entscheidung ohne Herleitung dastand. `check_formalia.py` 0 FEHLER. Anschließend im selben Absatz die undefinierte „erweiterte Vorratsverwaltung" (kam in Runde 6/6.6 als Ersatz für die gestrichenen exklusiven Challenges hinein, wurde aber nie in Fließtext oder `tab:funktionsuebersicht` durchgezogen) in die Menüplanung eingefaltet: „erweitert die Menüplanung um eine Vorratsübersicht mit Haltbarkeitsdaten und blendet Werbung aus" — definiert zugleich, was „erweitert" heißt (−2 Wörter). Werbung bleibt bewusst Teil des Modells (Gratis-Version werbefinanziert, Bezahlpaket blendet aus); ein Werbeverzicht war nie beschlossen. **Datenbeschaffung ergänzt (2026-07-28):** Nutzer-Einwand, dass der Beta-Test (Woche 15–17, prüft laut `07_iteratives_design.tex:5` das Reste-Matching) einen Rezeptbestand voraussetzt — zutreffend: Der Bestandsaufbau kam in keiner Phase der Inhaltsspalte vor. Jetzt in Phase 3 aufgenommen („Aufbau des Rezeptbestands, Umsetzung von …"), bewusst nicht in Phase 1–2, weil `05_phasenplanung.tex:3` diese als „mit diesem Bericht bereits erbracht" ausweist und der Bestand nicht existiert. Dazu ein Satz in `03_konzept.tex:7`: „Ein anfänglicher Rezeptbestand aus Rezeptkooperationen und lizenzierten Sammlungen ist damit Voraussetzung für den Beta-Test; Nutzerbeiträge erweitern ihn erst danach." — die Reihenfolge ist entscheidend, Nutzerbeiträge allein wären zirkulär. „Rezeptkooperationen" statt „Kooperationen", damit es nicht mit den Erlös-Kooperationen (Food-Sharing, Händler) in Z. 3 verschwimmt. Gegenfinanziert über Straffung des Menüplanungs-Satzes (Z. 13) und der Namensherleitung (Z. 3); in letzterer das Hedge „oft" bewusst wieder eingesetzt, da „-ia" ohne Einschränkung eine zu starke sprachliche Behauptung wäre. Roter Faden anschließend gegengelesen und zwei Anschlussstellen geschlossen: Risiko 2 in `05_phasenplanung.tex:23` um „oder beim Aufbau des Rezeptbestands" erweitert (statt eines dritten Risikos — „Zwei Risiken" bleibt korrekt, und der Zwei-Wochen-Puffer ist als Mitigation eines *Verzögerungs*risikos stimmig), und der Vorgriff auf den Beta-Test in `03_konzept.tex:7` mit `~\autoref{sec:phasenplanung}` verankert. Zuletzt in `06_evaluation_reflexion.tex:7` den Ankündigungssatz „Auch das Vorgehen selbst verdient einen Blick." ersatzlos gestrichen (−7 Wörter): einziger inhaltsleerer Absatzöffner des Abschnitts, während alle vier übrigen Absätze mit einer belastbaren Aussage beginnen; der Folgesatz trägt die Vorgehens-Bewertung samt Begründung bereits allein. Gemessen: **3.677 Wörter ≈ 9,8 S.** (`check_umfang.py`), Session netto +40 Wörter gegenüber 3.637. **Mockup-Lücke gefunden (2026-07-28):** Alle vier Bilder per Read-Tool gesichtet — Wochenplan und Reste-Journal sind in keinem abgebildet, und beide Startseiten-Screens zeigen keinen Einstiegspunkt (Tab-Leiste durchgehend nur Start/Rettungs-Feed/Profil). Damit widerlegt das Bild die Textaussage in `03_konzept.tex:13` („Erreichbar ist der Bereich über die Startseite"). Altlast von Befund 5.7, der 2026-07-24 per Textzusatz statt neuem Bild gelöst wurde. Als **Anpassung 4** in `sources/claude-design-nachbesserung.md` dokumentiert (Nutzer-Schritt: Karte „Wochenplan & Reste-Journal" auf beiden Startseiten-Screens ergänzen, null Wörter, keine fünfte Abbildung); Fallback ohne Design-Runde ebenfalls dort notiert. `\quelle{}`-Zeilen bewusst **nicht** vorbereitet, damit kein nie ausgeführter Prompt zitiert wird. Befund 6.3 vom Nutzer behoben und verifiziert (Plus-Button frei, Lesezeichen sichtbar, Feed ohne „Foto:"-Doppelpunkt); die Platzhalter-Korrektur aus Anpassung 3 steht noch in Startseite, Bookmarks und Profil aus. `05_phasenplanung.tex`, `tab:phasenplanung`: Ressourcenspalte von Phase 3 um „Rezeptdaten aus Kooperation oder Nutzerbeiträgen" ergänzt — die Aufgabenstellung (S. 6) nennt **Daten** als erste zu bedenkende Ressource, die Tabelle führte keine, obwohl das Reste-Matching einen strukturierten Rezeptkorpus voraussetzt. In Phase 1 „Desk-Research" durch „öffentlich zugängliche Plattform- und App-Angebote" ersetzt: unerklärter Anglizismus und zugleich Kategoriefehler (Methode in der Ressourcenspalte, neben „Fachliteratur" nahezu tautologisch); die neue Formulierung deckt sich mit dem Methodensatz in `01_wettbewerbsanalyse.tex:13`. Bewusst NICHT ergänzt: Hinweis in Z. 3, dass Phase 1/2 real schneller liefen als veranschlagt (Reflexion nennt „rund zwei Wochen", Plan veranschlagt vier) — die KI-Unterstützung ist dort bereits als Grund genannt, und der Seitenumfang steht auf der Obergrenze. Zusätzlich Terminologie vereinheitlicht: „Mockup-Skizze" in `03_konzept.tex:34` → „Mockup" (−1 Wort). Ankerbegriff für die vier Abbildungen ist durchgängig **Mockup**; „Skizze" bezeichnet ab jetzt ausschließlich die Persona-Skizze in Anhang A (vorher doppelt belegt). Netto der Session ca. +27 Wörter. Seitenumfang stand vorher exakt bei 10 (Obergrenze) — vor Abgabe neu bauen. **AENDERUNGEN.md Runde 7 abgearbeitet (2026-07-28):** 7.1–7.3 und 7.5 umgesetzt (SEQ- und Think-Aloud-Fundierung des Beta-Tests plus Gruppengrößen-Begründung in `07_iteratives_design.tex:5`, Dublette in `:3` gestrichen, `tab:phasenplanung`-Zeile Beta-Test auf die neuen Verfahren umgestellt), 7.4 auf ausdrückliche Nutzeranweisung übersprungen. `\acro{SEQ}` neu in `pages/acronyms.tex`. `check_formalia.py` 0 FEHLER. **Offener Punkt:** Die drei neu zitierten Quellen (Sauro & Lewis, Nielsen, Erlhofer) haben kein `file`/`url`-Feld — `check_quellentreue.py` markiert sie NICHT PRÜFBAR, siehe „Bitte manuell prüfen" in `AENDERUNGEN.md` Runde 7. Netto +60 Wörter, Gesamtumfang laut `check_umfang.py` jetzt 3.783 Wörter ≈ 10,1 Seiten (Kapitel 2 allein 2.829 W. ≈ 7,5 S.) — vor Abgabe neu bauen, könnte die 10-Seiten-Grenze knapp überschreiten.
| `sec:fazit` | `chapters/03_fazit/…` | FERTIG | Kürzungsrunde 2026-07-28: 597 → 426 Wörter (11,7 % Anteil, im Soll). Die vier Verhaltenshebel in `01_kernergebnisse_und_ausblick.tex` von vier Einzelsätzen auf eine belegte Aufzählung verdichtet (narrative `\textcite` dabei korrekt zu `\parencite` gewechselt, da die Namen nicht mehr im Satz stehen). Alle drei Limitationen bleiben dreischichtig (warum · Mitigation · Folgeschritt) — gegengeprüft. `check_formalia.py` 0 FEHLER. **Nachtrag 2026-07-28:** Kooperations-Satz in `01_kernergebnisse_und_ausblick.tex:7` auf einen Rückbezug umgestellt (`\autoref{sec:konzept}` statt erneuter Einführung), da er die Kooperationen aus `03_konzept.tex` wortnah wiederholte und sie zugleich als noch offene Option darstellte, obwohl sie dort fester Teil des Erlösmodells sind. −2 Wörter; „regionale Zero-Waste-Vereine" dabei entfallen (Variante ohne eigenen Begründungswert). Ebenfalls korrigiert in `02_limitationen.tex:5`: Limitation 2 sprach im **Singular** vom „UI/UX-Mockup" (es sind vier) und nannte als Mitigation „die Beschränkung auf die Kernfunktion Reste-Matching" — sachlich überholt, da die Mockups auch Profil, gespeicherte Rezepte und Rettungs-Feed zeigen. Jetzt „die vier UI/UX-Mockups" und „die Beschränkung auf die drei Hauptbereiche der Navigation" (deckungsgleich mit `03_konzept.tex:34`). Dreischichtung (warum · Mitigation · Folgeschritt) bleibt erhalten — die Schicht war beim Voll-Audit vom 24.07. schon einmal als −15-FAIL aufgefallen. **Zwei Formulierungen überarbeitet (2026-07-28):** In `01_kernergebnisse_und_ausblick.tex:5` verknüpfte „…als wissenschaftlich bewiesen darzustellen, **obwohl** er auf eigener Marktbeobachtung beruht" die beiden Hälften konzessiv, obwohl das Verhältnis kausal ist; zudem war „er" doppeldeutig (Marktvorteil oder Bericht) und der Bericht stand als handelnde Instanz da — derselbe Akteurs-Fehler wie in Voll-Audit-Befund 4. Jetzt „Diese Trennung macht kenntlich, dass die Differenzierung auf eigener Marktbeobachtung beruht und deshalb nicht als wissenschaftlich belegt gelten kann." („Differenzierung" statt „Marktvorteil", deckungsgleich mit Z. 3). In `02_limitationen.tex:5` benannte die Mitigation nur zwei Maßnahmen, ohne zu sagen, wogegen sie wirken; jetzt „Der Anspruch bleibt entsprechend begrenzt: … die Mockups zeigen nur die drei Hauptbereiche der Navigation, nicht die vollständige Bedienführung." — bricht zugleich den dreifach wiederholten Öffner „Abgemildert wird das…" (kosmetischer Hinweis aus dem Delta-Re-Audit vom 26.07.). Mitigationsschicht bleibt eindeutig erkennbar. `check_formalia.py` 0 FEHLER.
| Anhang C (Style Tile) | `pages/appendix.tex` · `images/durchfuehrung/resteria_styletile.png` | FERTIG | **Mockup-Iteration abgeschlossen (2026-07-28):** Wochenplan-Banner auf beiden Startseiten (Anpassung 4) und „Foto"-Platzhalter in allen vier Screens vom Nutzer umgesetzt. Alle vier `\quelle{}`-Zeilen tragen jetzt die Folge-Prompts; der Foto-Prompt wurde sinngemäß nach dem Wortlaut aus `claude-design-nachbesserung.md` (Anpassung 3) ergänzt — vor Abgabe gegen die tatsächlich eingegebene Formulierung abgleichen. Daraufhin `07_iteratives_design.tex:3` von „Eine erste Rückkopplungsschleife" auf „Mehrere Rückkopplungsschleifen" umgestellt und die zwei Auslösertypen unterschieden (Abgleich gegen die Konzeptbeschreibung vs. Sichtung der Screens selbst) — stärkt Teilaufgabe 3 und das Kriterium Prozess; `06_evaluation_reflexion.tex:9` zur Konsistenz auf „Mehrere Nachbesserungsrunden" angeglichen (±0 Wörter). Neu 2026-07-28 auf Nutzerwunsch. `\section*{Anhang~C: Style Tile Resteria}` mit `fig:styletile` (Farbpalette inkl. oklch-Werten, Typografie Lora/Work Sans, Layout-Maße, Icons, Komponenten). Im Fließtext einmal erwähnt in `03_konzept.tex:34`. **Zweite einmalige `main.tex`-Freigabe** des Nutzers (nach dem 23.07.): Zeile „Anhang C: & Style Tile Resteria" in die Anhangsverzeichnis-Tabelle (`main.tex:339`) ergänzt — ohne sie wäre der Anhang unvollständig ausgewiesen. `\quelle{}` zitiert den Nutzer-Prompt wortgetreu inkl. grammatischer Unebenheit („für das Projekt erstellen"), da Prompt-Angaben Zitate der Eingabe sind. Zusatz „Abbildung nachträglich manuell bearbeitet." bewusst **nicht** gesetzt — anders als bei den vier Mockups ist keine manuelle Nachbearbeitung bekannt; vor Abgabe zu prüfen. |
| Prüf-Audit | `pruefbericht.md` | ÜBERARBEITUNG NÖTIG | **Überholt durch die Kürzungsrunde vom 2026-07-28.** Der dort dokumentierte Score 100/100 (Delta-Re-Audit 26.07.) bezieht sich auf den ungekürzten Textstand und gilt nicht mehr; `pruefbericht.md` ist als Momentaufnahme zu lesen. Neu: Teil-Check D (Build) ist erstmals durchführbar (`lualatex`/`latexmk`/`biber` verfügbar), Textteil gemessen 10 Seiten. Score im Delta-Re-Audit neu zu erheben.

**Update-Regel:** Eine Komponente = eine Zeile, bestehende Zeile überschreiben, nie anhängen. Identifikator = `sec:<slug>` (in Plan-Modus Schritt 2.5 festgelegt) — hält Tabelle, Dateinamen und LaTeX-Labels synchron. Platzhalter-Zeilen spätestens bei Schritt 2.5 durch echte Einträge ersetzen; aus dem Plan entfernte Komponenten löschen.

**Aktuelle Richtung**: `AENDERUNGEN.md` Runde 2 (GS-1…GS-6) vollständig abgearbeitet (2026-07-21). GS-1 nach aktiver Rückfrage (`AskUserQuestion`) mit Nutzer-Entscheidung „volle Aufnahme": leftovercooking per Web-Recherche (Verbraucherzentrale-Test 2024 + leftovercooking.app) als 6. Plattform in Wettbewerbsanalyse aufgenommen, USP dadurch geschärft statt widerlegt. GS-2 bis GS-6 wie in `AENDERUNGEN.md` angewiesen umgesetzt. `check_formalia.py` (0 FEHLER) und `check_bib_keys.py` (alle Keys valide) auf allen betroffenen Kapiteln bestätigt. **Runde-1-Befund 1 ist jetzt erledigt (2026-07-23):** Nutzer hat vier echte Mockup-Screenshots (Startseite, Startseite mit ausgeklappten „Gespeicherte Rezepte", Profil, Rettungs-Feed) unter `images/durchfuehrung/` geliefert. Alle drei Tikzpicture-Platzhalter in `pages/appendix.tex` durch `\includegraphics` ersetzt, ein viertes Mockup (Bookmarks-Screen) neu ergänzt, da dessen Feature „Gespeicherte Rezepte" im Screenshot sichtbar war, aber noch nicht im Fließtext stand — nach Rückfrage (`AskUserQuestion`) in `chapters/02_durchfuehrung/03_konzept.tex` per Satz ergänzt und auf die neue Abbildung verwiesen. Alle vier Quellenzeilen folgen dem Pflichtmuster „Erstellt mit dem Prompt [...] durch Claude Design (Claude Sonnet 5)" mit vom Nutzer bestätigten, aus `sources/claude-design-brief.md` abgeleiteten Prompt-Texten; da der Nutzer die KI-Outputs zusätzlich manuell nachbearbeitet hat (im Zitierleitfaden nicht explizit geregelter Fall), wurde nach Prüfung des Original-PDFs (`sources/Zitierleitfaden.pdf`, Kap. 2.2.5) je Quellenzeile der Zusatz „, Abbildung nachträglich manuell bearbeitet." ergänzt. `check_formalia.py` läuft auf `03_konzept.tex` und `appendix.tex` mit 0 FEHLER (nach Umstellung der geraden Anführungszeichen auf `\enquote{}`).

**Prüfbericht abgearbeitet (2026-07-23).** Nutzer hat für diese Session einmalig explizite Freigabe erteilt, `main.tex` zu ändern (Abweichung vom Grundprinzip „main.tex nie ändern"). Umgesetzt: (1) **Handlungsempfehlung 1** — `\appendix`/`\include{pages/appendix}` in `main.tex` einkommentiert; die vier UI/UX-Mockups + Persona-Tabelle werden jetzt kompiliert, die fünf Anhang-`\autoref`s sind aufgelöst. (2) **Handlungsempfehlung 2** — `\listoffigures`/`\listoftables` in `main.tex` einkommentiert. (3) **Handlungsempfehlung 3** — „vier" → „drei Plattform-Kategorien" in `chapters/03_fazit/02_limitationen.tex`. (4) **Handlungsempfehlung 4** — Zeitpuffer-Formulierung in `chapters/02_durchfuehrung/05_phasenplanung.tex:23` an die Tabelle angeglichen („innerhalb der MVP-Phase" statt „zwischen MVP-Entwicklung und Beta-Test"). Handlungsempfehlung 5 (UI/UX `\ac{}`-Vereinheitlichung) bewusst ausgelassen — als rein optional markiert, keine Regelverletzung. `check_formalia.py` auf beiden geänderten Kapiteldateien: 0 FEHLER. Teil-Check D (Build) bleibt vom Nutzer lokal zu verifizieren — `lualatex`/`latexmk` in dieser Umgebung nicht verfügbar.

**Delta-Re-Audit (2026-07-23, Score 100/100) ist durch Runde-3-Gegenlesung überholt** — der damalige Score bezog sich auf den Textstand vor den unten beschriebenen Änderungen an `01_wettbewerbsanalyse.tex`, `02_limitationen.tex` und `03_konzept.tex`; `pruefbericht.md` selbst wurde nicht überschrieben und ist als Momentaufnahme des alten Stands zu lesen, nicht als aktuell gültiger Score.

**AENDERUNGEN.md Runde 3 (Gegenlesung 2026-07-23) vollständig abgearbeitet.** Acht Befunde (3.1–3.8): 3.1 (Zähl-/Aufzählungsdrift „drei"→„vier" Einwände, „ein bis zwei"→„ein bis drei" Vertreter) und 3.6 (unbelegte Marktbehauptung abgeschwächt) mechanisch ohne Freigabe umgesetzt. Die vier `[FREIGABE]`-Punkte per `AskUserQuestion` einzeln geklärt: 3.2 Chefkoch Community-Funktionen nach Web-Faktencheck auf „stark" gehoben; 3.3 leftovercooking-Belohnungspunkte als individuelles Spar-Feedback vom Gamification-Kriterium abgegrenzt, Zelle „mittel" bleibt; 3.4 unbelegter Verbraucherzentrale-Verweis gestrichen, leftovercooking-Bewertung läuft rein als eigene Sichtung; 3.5 neue Matrixspalte „Rezept-Entdeckung" in `tab:wettbewerbsvergleich` ergänzt (jetzt 5 Kriterien statt 4). 3.7 (optionale Einstiegsliteratur) übersprungen wie in Runde 1 — Quellen fehlen weiter in `references.bib`. 3.8 ist ein reiner Build-Grenzvermerk ohne Textauftrag. `check_formalia.py` (0 FEHLER) und `check_bib_keys.py` (6/6 Keys valide) auf allen betroffenen Dateien bestätigt.

**Voll-Audit abgeschlossen (2026-07-23, Score 100/100 — abgabereif).** Alle Teil-Checks A–C fehlerfrei, `sources/Anmerkungen vom Prüfer.md` als zusätzlicher Maßstab abgeglichen (alle Prüfer-Punkte erfüllt, inkl. SWOT-Charakter der Wettbewerbsanalyse und KI-Mockup-Kennzeichnung). Keine Score-Abzüge. Teil-Check D (Build) blieb ÜBERSPRUNGEN (`lualatex`/`biber` nicht verfügbar), statisch aber sauber. Details in `pruefbericht.md`.

**Pruefbericht-Analyse (2026-07-23, diese Session):** Die vier Handlungsempfehlungen aus `pruefbericht.md` durchgesehen. Umsetzbar und rein kosmetisch/strukturell, ohne Bezug zu These/Kernargumenten, daher ohne Rückfrage umgesetzt: **Handlungsempfehlung 3** — `chapters/01_einleitung/03_vorgehen_und_aufbau.tex` um je einen Satz pro Absatz ergänzt (Verzahnung Wettbewerbsanalyse/Verhaltensliteratur; Trichter-Logik der Kapitelabfolge), keine neuen Fakten-Claims, `check_formalia.py` 0 FEHLER. Bewusst NICHT umgesetzt: **Handlungsempfehlung 1** (lokaler Build) und **2** (Seitenumfang 7–10 S.) — beides erfordert `lualatex`/`latexmk`, die in dieser Umgebung nicht verfügbar sind; bleibt Nutzer-Aufgabe laut „Vor der Einreichung". **Handlungsempfehlung 4** (optionale Einstiegsquelle Kapoor et al. / Hansch & Rentschler) übersprungen — beide Quellen fehlen in `references.bib`, das nur per Zotero/BBT-Auto-Export befüllt werden darf, nicht manuell; zudem laut `aufgabe.md` nicht Pflicht. Kein neuer Voll-Audit gefahren, da die Änderung rein strukturell ist und den bestehenden Score 100/100 nicht berührt.

**Terminologie-Korrektur (2026-07-24, diese Session):** Der Prüfer adressiert in `sources/Anmerkungen vom Prüfer.md` explizit eine Einzelperson („Du bist also Projektverantwortliche"), es gibt kein Team. „Projektgruppe" (16 Fundstellen über `01_einleitung`, `02_durchfuehrung`, `03_fazit`) durch pronomenfreie Selbstbezüge („diese Arbeit"/„dieser Bericht", Nutzerentscheidung per `AskUserQuestion`) ersetzt, in der Phasenplanungs-Tabelle „Projektgruppe"/„Gesamtes Team" (Ressourcen-Spalte) zu „Eigenleistung". Mehrere redundante „die Aufgabenstellung fordert/verlangt das auch nicht"-Nebensätze gestrichen (reine Wiederholung ohne Begründungswert); Stellen, die aktiv eine Scope-Entscheidung begründen (z. B. Event-Kalender-Verzicht, Funktionsübersicht-Tabelle), bleiben stehen. „Projektbericht" als Selbstbezeichnung des Dokuments bewusst NICHT verändert — das ist der tatsächliche Papiertyp laut `aufgabe.md`, kein austauschbares Label wie „Konzept" (das bezeichnet die Plattform-Idee, nicht das Dokument). Zusätzlich in `02_zielsetzung_und_verengung.tex:5` ein unbelegter Fakten-Claim „eher ein Hindernis als ein Mehrwert" auf die durch die Prämisse tatsächlich gedeckte Aussage „ohne echten Mehrwert" abgeschwächt. `check_formalia.py` über alle Kapitel: 0 FEHLER (nur Alt-Hinweise zu Satzlänge/Trias, unverändert). Kein neuer Voll-Audit gefahren — rein stilistisch/formal, kein neuer Fakten- oder Argumentations-Claim, Score 100/100 bleibt als letzter inhaltlicher Stand gültig, sollte aber vor Abgabe nochmal kurz gegengelesen werden, da mehrere Sätze umformuliert wurden.

**Mockup-Audit gegen Textansprüche (2026-07-24, diese Session):** `02_zielgruppe.tex` und `03_konzept.tex` gegen die vier bestehenden Mockups abgeglichen. Zwei Lücken gefunden: (1) Rezept-Upload-Funktion (`03_konzept.tex` + Funktionsübersicht-Tabelle) hat in keinem Mockup einen sichtbaren „Beitrag erstellen"-Button. (2) `02_zielgruppe.tex` verlangt eine sichtbare Einsparangabe („wie viel eine Person tatsächlich einspart"), das Profil-Mockup zeigt bisher nur abstrakte Rettungspunkte/Level. Neue Datei `sources/claude-design-nachbesserung.md` mit zwei konkreten Folge-Prompts für Claude Design erstellt (Plus-Button im Rettungs-Feed, Kennzahl „X kg gerettet" im Profil). Die `\quelle{}`-Zeilen in `pages/appendix.tex` für `fig:mockup_profil` und `fig:mockup_feed` bereits auf die kombinierten Prompts (Runde 1 + Nachbesserung) aktualisiert, `check_formalia.py`: 0 FEHLER. Nachtrag: Auf Nachfrage auch `04_community_und_feedback.tex` gegen die Mockups geprüft (war zunächst nicht Teil des Auftrags) — dritte Lücke gefunden: die dort beschriebenen Challenges/Rangliste („Zero-Waste-Woche") fehlen im Rettungs-Feed-Mockup komplett, zudem weicht das Tag-Format „gerettet: [Zutat]" leicht vom gezeigten „[Zutat]" ohne Präfix ab. Als „Anpassung 1" in `sources/claude-design-nachbesserung.md` mit dem bereits bestehenden Plus-Button-Fix zusammengelegt (derselbe Screen), Folge-Prompt bewusst unter 40 Wörtern gehalten (sonst hätte `hard-rules-formal.md` die `blockzitat`-Umgebung statt `\enquote{}` verlangt — in einer Bildquellenzeile unpassend). `\quelle{}` für `fig:mockup_feed` in `pages/appendix.tex` entsprechend erweitert, `check_formalia.py`: 0 FEHLER.

**Tag-Präfix „gerettet:" entfernt (2026-07-24, diese Session):** Auf Nutzerwunsch das Präfix „gerettet:" vor der Zutat im Rettungs-Feed-Tag gestrichen — wirkt auf dem kleinen Mobile-Screen überladen, der Kontext macht es ohnehin klar. Da die bestehenden Mockups sowieso schon nur die Zutat ohne Präfix zeigen, wurde stattdessen der Text in `04_community_und_feedback.tex:3` an das bestehende Bild angeglichen (`\enquote{gerettet: Brokkoli-Strunk}` → `\enquote{Brokkoli-Strunk}`), nicht umgekehrt. `sources/claude-design-nachbesserung.md` und die `\quelle{}`-Zeile für `fig:mockup_feed` in `pages/appendix.tex` entsprechend bereinigt — Anpassung 1 verlangt jetzt nur noch Plus-Button + Challenge-Banner, keine Tag-Änderung mehr. `check_formalia.py`: 0 FEHLER.

**Mockup-Nachbesserung abgeschlossen (2026-07-24, diese Session):** Nutzer hat `resteria_profil.png` und `resteria_rettungsfeed.png` mit den Folge-Prompts aus `sources/claude-design-nachbesserung.md` aktualisiert und die Dateien in `images/durchfuehrung/` ersetzt. Per Bild-Kontrolle (Read-Tool) verifiziert: Rettungs-Feed zeigt jetzt das Challenge-Banner „Zero-Waste-Woche · Rangliste · Platz 4" sowie den Terracotta-Plus-Button unten rechts; Profil zeigt „12,4 kg Lebensmittel gerettet" unter dem Rettungspunkte-Balken — beide exakt wie in `pages/appendix.tex` → `\quelle{}` zitiert. Damit sind alle drei in dieser Session gefundenen Text-Mockup-Lücken (Upload-Button, Einsparangabe, Challenges/Rangliste) auch visuell geschlossen. Kein weiterer Nutzer-Schritt zu den Mockups offen.

**Umfangsanalyse (2026-07-24, diese Session):** Fließtext über alle 14 `chapters/**/*.tex` gezählt — **4.508 Wörter**, bei 11pt/2 cm Rändern/`\onehalfspacing` geschätzt **10–11 Seiten Textteil** gegen die Vorgabe 7–10 (`aufgabe.md:18`). Kapitelanteile (14,3 / 70,8 / 14,9 %) liegen im Soll, Einleitung und Fazit aber am oberen Rand. Acht Kürzungsbefunde als **`AENDERUNGEN.md` Runde 4** angelegt (erwarteter Ertrag ~715 Wörter ≈ 16 %, ausschließlich Redundanz, kein Informationsverlust); 4.1, 4.2, 4.5, 4.7 sind `[FREIGABE]`. Befund 4.0 ist ein Nutzer-Schritt: lokaler Build als Messwert **vor** der Umsetzung — bei ≤ 9,5 Seiten genügen 4.1–4.4. Parallel drei Workflow-Befunde notiert [**KORREKTUR 2026-07-28:** Die hier genannte Datei `SKILL-VERBESSERUNGEN.md` existiert weder im Arbeitsverzeichnis noch in der Git-Historie — die drei Befunde sind verloren. Nachfolger ist `SKILL-ANPASSUNGEN.md` im Projekt-Root.] (4: kein Schritt im Fahrplan subtrahiert · 5: Umfangsprüfung hängt allein an Teil-Check D, der hier nie lief, weshalb 100/100 bei ungeprüftem Pflichtkriterium vergeben wurde · 6: Redundanz zwischen Dateien ist by design blind) samt Einschätzung gegen einen eigenen Kürzungs-Modus.

**AENDERUNGEN.md Runde 4 (Nutzer-Feedback/Umfangsanalyse, 2026-07-24) vollständig abgearbeitet.** Alle neun Punkte (4.0–4.8) umgesetzt: 4.0 ohne Messwert (lualatex/latexmk fehlen weiterhin in dieser Umgebung), Nutzer-Entscheidung nach Rückfrage: alle Kürzungen trotzdem umsetzen. Die vier `[FREIGABE]`-Punkte (4.1, 4.2, 4.5, 4.7) per `AskUserQuestion` einzeln geklärt und wie vorgeschlagen umgesetzt — bei 4.5 bewusst konservativer (3 statt 4 Stellen gekürzt, um eine Matrixbias-Mitigation aus Runde 1 und den laut 4.3 geschützten Prozess-Reflexions-Absatz nicht zu beschädigen) und bei 4.4 ebenfalls konservativer (vier statt zwei Sätze in `03_vorgehen_und_aufbau.tex`, sonst wäre die Datei unter die HALBSEITE-Schwelle von 150 Wörtern gerutscht). Bei 4.6 zusätzlich eine seit Runde 2 (GS-2) bestehende Propagationslücke behoben: Der Fazit-Satz zu Barkers Reminders verwies noch auf „die steigenden Reste-Level" statt auf die in `03_konzept.tex` bereits umbenannte Erinnerungsbenachrichtigung. `check_formalia.py`: 0 FEHLER (10 HINWEIS, überwiegend bekannte Tabellen-Satzlängen-Artefakte). `check_bib_keys.py`: alle 6 Keys valide. Tatsächlicher Wortzahl-Ertrag ca. 394 Wörter (~8,7 %) statt der geschätzten ~715 (~16 %) — geringer, weil mehrere Punkte bewusst konservativer umgesetzt wurden als ursprünglich skizziert. Details je Befund in `AENDERUNGEN.md`. „Offen" ist damit wieder vollständig leer.

**Delta-Re-Audit durchgeführt (2026-07-24, Score 90/100 — abgabereif).** Geprüft wurde der Delta der Runde-4-Kürzung (10 geänderte Kapiteldateien) plus beide Skripte vollständig und die kapitelübergreifenden Kürzungsfolgen. Ein Struktur-FAIL: In `chapters/02_durchfuehrung/01_wettbewerbsanalyse.tex:34` kündigt der Text „zwei Einwände" an, es folgen aber vier (drei kurz abgehandelte plus ein „gewichtigerer") — die Kürzung baute vier ausformulierte Einwände auf eine gestaffelte Darstellung um, ohne den Zählsatz mitzuführen. Derselbe Fehlertyp war in Runde 3 (Befund 3.1) an derselben Stelle schon einmal korrigiert worden. Substanz ist unbeschädigt (alle vier Einwände stehen und werden entkräftet), nur die Ankündigungszahl stimmt nicht. Keine weiteren Regressionen: Abhakliste-Schlüsselbegriffe per Grep vollständig abgedeckt, „Drivers/Levers" bleibt bei Erstnennung definiert, alle übrigen Zählangaben nachgezählt und stimmig, keine Datei unter die HALBSEITE-Schwelle gerutscht (Minimum 155 Wörter). Drei kosmetische Hinweise ohne Score-Wirkung im Bericht (unangekündigte Funktionsübersicht-Tabelle, entfallene UI/UX-Kurzerklärung, verwaistes „dabei").

**Überarbeitung nach Prüfbericht abgeschlossen (2026-07-24, diese Session).** Handlungsempfehlung 1 aus `pruefbericht.md` umgesetzt: Zählwiderspruch in `chapters/02_durchfuehrung/01_wettbewerbsanalyse.tex:34` behoben („zwei Einwände" → „mehrere Einwände", ohne inhaltlichen Eingriff — alle vier Einwände und ihre Entkräftung bleiben unverändert). `check_formalia.py` auf `chapters/02_durchfuehrung/`: 0 FEHLER, dieselben 10 bekannten HINWEISE wie im Delta-Re-Audit, keine neuen. Die drei optionalen kosmetischen Hinweise aus Handlungsempfehlung 3 (Anmoderation `tab:funktionsuebersicht`, „dabei" streichen, UI/UX-Kurzerklärung) bewusst NICHT umgesetzt — als score-neutral markiert, kein Auftrag dieser Session. Kein neuer Voll-Audit gefahren, da der Fix rein mechanisch ist und `pruefbericht.md` selbst als ÜBERHOLT markiert bleibt, bis ein neuer Audit-Lauf den Stand bestätigt. **Nächster Schritt: reine Nutzer-Schritte vor der Einreichung** (siehe `pruefbericht.md` → „Vor der Einreichung"): lokalen Build fahren (`latexmk -lualatex main.tex`), Seitenumfang 7–10 S. bestätigen (Schätzung ≈ 9–10 S., damit im Soll), LanguageTool-Durchgang, Eidesstattliche Erklärung über myCampus. Optionaler Feinschliff (Einstiegsliteratur, die drei kosmetischen Hinweise) ist nicht abgabekritisch.

**Gegenlesung Runde 5 angelegt (2026-07-24, diese Session).** Kalte Prüfer-Gegenlesung gegen `aufgabe.md` durchgeführt, elf Befunde (5.1–5.11) als neue Runde in `AENDERUNGEN.md` — „Offen" ist damit nicht mehr leer. Kein schwerer Befund (kein fehlendes Artefakt, keine unbelegte Kernbehauptung), aber drei Gruppen: Sichtbarkeit (5.1 Instagram fehlt trotz wörtlicher Nennung in `aufgabe.md:10`, 5.2 Teilaufgabe 3 ohne eigene Überschrift, 5.4 Achse „Kommunikation" umbenannt), Nachweisbarkeit (5.3 Wettbewerbsanalyse ohne durchgängigen Erhebungsnachweis, 5.6 Prozess-Reflexion ohne Schwierigkeit, 5.10 Ressourcenträger der MVP-Phase offen), Konsistenz (5.5 Doppelmaßstab Gruppen/Following, 5.7 Menüplanung ohne UI-Zugang, 5.8 Prompt-Zeile ≠ Bild, 5.9 Mockup-Zahlen ≠ Punkte-Regel, 5.11 Satzschlussmuster). `[FREIGABE]`: 5.1 und 5.5. **Nächster Schritt: „Schreib-Modus: AENDERUNGEN.md abarbeiten" in frischer Session** — vorher Nutzer-Schritt: lokaler Build zur Seitenmessung, da die Runde teils additiv ist (Arbeit liegt bei ~4.250 Wörtern ≈ 9–10 S. am oberen Rand). Ein Voll-Audit ist erst nach Abarbeitung sinnvoll.

**AENDERUNGEN.md Runde 5 (Gegenlesung, 2026-07-24) vollständig abgearbeitet (2026-07-24, diese Session).** Alle elf Befunde (5.1–5.11) umgesetzt. Vorab per `AskUserQuestion` geklärt: Seitenbudget (Nutzer-Entscheidung: konservativ planen, kein lokaler Build verfügbar) sowie die beiden `[FREIGABE]`-Punkte 5.1 (Option B: TikTok stellvertretend für Instagram) und 5.5 (a）Gruppen aus der Kriteriendefinition gestrichen, (b) Following für Resteria ergänzt — auf Nutzer-Nachfrage bewusst an die bereits sichtbare Nutzername-Struktur im Rettungs-Feed-Mockup angebunden statt einen dort nicht gezeigten Folgen-Button zu behaupten, daher kein neues Mockup nötig. Strukturänderung: Befund 5.2 (Teilaufgabe 3 ohne eigene Überschrift) wurde durch eine neue Subsection gelöst — `chapters/02_durchfuehrung/07_iteratives_design.tex` (`\subsection{Iteratives Design und Feedback}\label{sec:iteratives_design}`), inhaltlich zwischen `04_community_und_feedback` und `05_phasenplanung` eingebunden, `kapitelplan.md` entsprechend nachgezogen. Übrige Befunde rein mechanisch bzw. textlich gelöst: 5.3 (einheitlicher Methodensatz statt zwei isolierter Sichtungsvermerke), 5.4 (Spalte „Community-Funktionen" → „Kommunikation \& Community"), 5.6 (Prozess-Reflexion um zwei konkrete Ereignisse ersetzt statt Leerformel), 5.7 (Menüplanung als Startseiten-Zugang erklärt, Option A ohne neues Bild), 5.8 (Prompt-Zeile an Bild angeglichen), 5.9 (Rettungspunkte-Regel von starrem 1:1 auf „nach Menge/Art" gelockert, macht Mockup-Zahlen widerspruchsfrei), 5.10 (Ressourcenträger für Design- und MVP-Phase ergänzt), 5.11 (drei Pointen-Schlusssätze gestrichen). `check_formalia.py`: 0 FEHLER, 11 HINWEIS (ausschließlich bekannte Tabellen-/Satzlängen-Artefakte, keine neuen Verstöße). `check_bib_keys.py`: alle 5 in Kapitel 2 genutzten Keys valide. „Offen" in `AENDERUNGEN.md` ist damit wieder vollständig leer.

**Gegenlesung Runde 6 angelegt (2026-07-24, diese Session).** Kalte Prüfer-Gegenlesung gegen `aufgabe.md`, ausgeführt von einem Subagenten unter striktem Leseverbot (kein `kapitelplan.md`, kein `pruefbericht.md`, kein `AENDERUNGEN.md`-Inhalt, kein Projektstatus), weil die Hauptsession bereits Projektkontext trug. Elf Befunde (6.1–6.11) als neue Runde in `AENDERUNGEN.md` — „Offen" ist damit nicht mehr leer. Kein fehlendes Artefakt, keine nicht ausgeführte Teilaufgabe. Schwerster Punkt: 6.1 `[FREIGABE]` — die Wettbewerbsanalyse trägt außer dem Methodensatz (`01_wettbewerbsanalyse.tex:15`) keine einzige Quellenangabe, obwohl sie nachprüfbare Plattform-Fakten behauptet und der Prüfer genau deren Nachvollziehbarkeit verlangt. Weiter: 6.2 (Startseiten-Mockup deckt den in `03_konzept.tex:53` behaupteten Menüplanungs-/Reste-Journal-Zugang nicht), 6.3 (Plus-Button überlappt im Feed-Mockup die Aktionsleiste; „Foto:" ohne Folgetext — Prüfer erlaubt KI-Mockups nur ohne gestalterische Fehler), 6.4 („innovativ/Innovation" kommt in `chapters/` kein einziges Mal vor, obwohl Teilaufgabe 2 und das 15-%-Kriterium Kreativität es verlangen), 6.5 (Vokabel „Kochtipp-Austausch" nirgends wiederauffindbar), 6.6 `[FREIGABE]` (Freemium stellt exklusive Challenges hinter die Bezahlschranke, während Challenges das einzige verbliebene Interaktionsformat sind), 6.7 (Entstehungsgeschichte nur punktuell statt „lückenlos"), 6.8 (Einstiegsliteratur Kapoor/Hansch fehlt in `references.bib`, laut `aufgabe.md` nicht verpflichtend), 6.9 (Kontrastfigur „nicht X, sondern Y" ~1,6×/Seite gegen die Regel max. 1/Seite), 6.10 (Anhangsverzeichnis fehlt im Inhaltsverzeichnis, Fix vollständig in `pages/appendix.tex`), 6.11 `[FREIGABE]`, optional (Resteria fehlt als Zeile in `tab:wettbewerbsvergleich`). Umfang ~4.680 Wörter ≈ 9–10 S.; die Runde ist selbstfinanzierend gebaut (6.9 deckt die Zugaben aus 6.4/6.7). **Nächster Schritt: „Schreib-Modus: AENDERUNGEN.md abarbeiten" in frischer Session**, vorher Nutzer-Schritt lokaler Build zur Seitenmessung. Hinweis: `AENDERUNGEN.md` folgt historisch nicht dem Offen/Erledigt-Format aus `aenderungen-format.md`; die neue Sektion „## Offen" steht deshalb am Dateiende unterhalb der Alt-Runden.

**AENDERUNGEN.md Runde 6 (Gegenlesung, 2026-07-24) abgearbeitet (2026-07-24, diese Session).** Zehn der elf Befunde vollständig umgesetzt, einer (6.8, optionale Einstiegsliteratur) wie in Runde 1/3 bewusst übersprungen, einer (6.3) nur vorbereitet. Die drei `[FREIGABE]`-Punkte per `AskUserQuestion` geklärt: 6.1 Option A kombiniert mit Option C (Quelle-Zeile um sechs offizielle Plattform-Domains + Sichtungsdatum erweitert, „über zwanzig Food-Creator:innen"/„bis zu drei übrige Zutaten" abgeschwächt — Nutzer-Vorbehalt „keine Blogs zitieren" per Web-Check und ZITIERREGEL-Abgleich bestätigt: Wettbewerbsanalyse ist eigene Sichtung, kein `\parencite`-Fall, ergänzte URLs sind ausschließlich die offiziellen Plattform-Domains), 6.6 Option B (exklusive Challenges aus dem Freemium-Paket gestrichen, durch erweiterte Vorratsverwaltung ersetzt), 6.11 Option B (unverändert gelassen). 6.3 bleibt teilweise offen: Folge-Prompt und `\quelle{}`-Zeile sind vorbereitet, der tatsächliche Bildaustausch in Claude Design ist wie bei den vorherigen Nachbesserungsrunden ein externer Nutzer-Schritt. `check_formalia.py`: 0 FEHLER, 12 HINWEIS (ausschließlich bekannte Artefakte plus ein neuer Satzschnitt-Hinweis in einer nicht bearbeiteten Datei). `check_bib_keys.py`: alle 6 Keys valide. Netto-Umfangsänderung ca. +100 bis +140 Wörter, Ausgangswert ~4.680 Wörter — bleibt im geschätzten 9–10-Seiten-Korridor, lokaler Build zur Bestätigung weiterhin Nutzer-Aufgabe. „Offen" in `AENDERUNGEN.md` ist bis auf den ausstehenden Mockup-Austausch (6.3) wieder leer.

**Voll-Audit durchgeführt (2026-07-24, Score 49/100 — nicht in diesem Stand einreichen).** Vorbedingungen erfüllt: Gegenlesung (Runden 5, 6) und Gesamt-Stresstest (Runde 2) abgearbeitet, `AENDERUNGEN.md` → „Offen" bis auf 6.3 leer. Befund 6.3 (Plus-Button-Überlapp im Rettungs-Feed-Mockup) wurde auf ausdrückliche Nutzer-Anweisung aus dem Audit ausgenommen und nicht gewertet. Fünf Befunde, Details je Befund in `pruefbericht.md`: (1) Formalia, −8 — elf narrative Zitate („Vittuari et al. unterscheiden … \parencite{}") doppeln bei `style=apa` den Autornamen; `\textcite` kommt in der Arbeit kein einziges Mal vor. Kein Regressions-, sondern ein Altbefund seit dem Erstschreiben, den die Audits vom 23.07. übersehen haben, weil `check_formalia.py` narrativ/parenthetisch nicht prüft. (2) Formalia, −8 — „BMEL", „SDG" und „USP" ohne Auflösung bei Erstnennung und ohne Eintrag in `pages/acronyms.tex`, während die Arbeit MVP, Nudge, Drivers/Levers, UI/UX und Fairteiler vorbildlich erklärt. (3) Struktur, −10 — `01_wettbewerbsanalyse.tex:36` folgert „Die USP … liegt damit nicht im bloßen Verbinden beider Elemente", der Folgesatz und Z. 34 sagen das Gegenteil; gemeint ist erkennbar eine Präzisierung gegen leftovercooking, formuliert ist eine Verneinung der eigenen Kernthese. (4) Struktur, −10 — `07_iteratives_design.tex:3` macht „diesen Bericht" zum Akteur der künftigen Design-Phase und prüft „am Klick-Prototyp", den es nicht gibt; der Folgeabsatz grenzt mit „im weiteren Entwicklungsprozess" genau diese eine Schleife von der Planungs-Kennzeichnung aus. Die Subsection stammt aus Runde 5 und war nie in einem Voll-Audit. (5) Pflichtregel, −15 — Limitation 2 fehlt die Mitigations-Schicht; sie stand bis Commit `3249b0f` (Kürzungsrunde 4) im Text und fiel als Kollateralschaden der Umfangskürzung weg. Teil-Check D blieb ÜBERSPRUNGEN (`lualatex`/`latexmk`/`biber` in dieser Umgebung nicht verfügbar); Umfang geschätzt 9–10 Seiten bei 4.870 Wörtern, Kapitelanteile 11,9 / 75,5 / 12,6 % im Soll. `check_formalia.py` 0 FEHLER, `check_bib_keys.py` 6/6 Keys valide, `check_status.py` 0 FEHLER.

**Nächster Schritt**: neue Session, „Schreib-Modus: Überarbeitung nach Prüfbericht" — die fünf Befunde in der Reihenfolge der Handlungsempfehlungen abarbeiten (größter Hebel zuerst: Mitigationssatz im Fazit). Danach genügt ein Delta-Re-Audit, kein neuer Voll-Audit. Erwarteter Score nach vollständiger Umsetzung: 100/100. Nicht in der Audit-Session weiterschreiben — `pruefbericht.md` trägt alles Nötige hinüber.

**Überarbeitung nach Prüfbericht abgeschlossen (2026-07-26, diese Session).** Alle fünf Befunde aus `pruefbericht.md` in der Reihenfolge der Handlungsempfehlungen umgesetzt, keiner betraf These oder Kernargumente, daher ohne `AskUserQuestion`-Rückfrage: **Befund 5** — Mitigationssatz in `02_limitationen.tex:5` ergänzt, gegenfinanziert durch Streichung des Pointen-Schlusssatzes am Kapitelende. **Befund 3** — Widerspruchssatz `01_wettbewerbsanalyse.tex:36` umformuliert, verneint die Kernthese nicht mehr, löst zugleich „USP" auf. **Befund 4** — `07_iteratives_design.tex:3` in den Planungsmodus gesetzt (dieser Bericht ist nicht mehr Akteur der künftigen Design-Phase), „im weiteren" gestrichen. **Befund 1** — alle elf narrativen Zitate (`03_konzept.tex`, `04_community_und_feedback.tex`, `01_kernergebnisse_und_ausblick.tex`, `02_limitationen.tex`) von `\parencite` auf `\textcite` umgestellt, doppelte Namensnennung jeweils gestrichen. **Befund 2** — `\acro{BMEL}` und `\acro{SDG}` in `pages/acronyms.tex` ergänzt (Klammer-Argument auf `[BMEL]` als längste Abkürzung aktualisiert), alle neun BMEL- und beide SDG-Fundstellen über `chapters/` auf `\ac{}` umgestellt. `check_formalia.py`: 0 FEHLER, 11 HINWEIS (bekannte Tabellen-/Satzlängen-Artefakte, ein Hinweis weniger als vor der Überarbeitung). `check_bib_keys.py --dir chapters/`: alle 6 Keys valide. `check_status.py`: 0 FEHLER, 9 HINWEIS (bekannte `DATEI-OHNE-ZEILE`-Hinweise, projekttypische Konvention). Kein neuer Voll-Audit gefahren — laut Handlungsempfehlung genügt ein Delta-Re-Audit.

**Delta-Re-Audit durchgeführt (2026-07-26, Score 100/100 — abgabereif).** Geprüft wurde der Delta der Überarbeitung (Commit `1843fa8`, 9 Dateien), beide Skripte vollständig, `check_status.py`, Teil-Check D und die Pflichtregeln der berührten Kapitel. Alle fünf Befunde verifiziert behoben: elf `\textcite`-Stellen vollständig und mit erhaltenen Seitenangaben umgestellt (die fünf legitimen `\parencite` unverändert), BMEL/SDG durchgängig über `\ac{}` mit korrekter Erstnennungs-Reihenfolge und „USP" restlos aufgelöst, der Widerspruchssatz zeigt jetzt in dieselbe Richtung wie Z. 34 und der Folgesatz, `07_iteratives_design.tex:3` steht im Planungsmodus ohne Akteurs-Fehler, Limitation 2 ist dreischichtig. Keine Regression: 0 FEHLER und 11 HINWEIS im Formalia-Skript (einer weniger als vorher, kein neuer), 6/6 BBT-Keys valide, alle 14 `\autoref`-Ziele existieren, keine Datei unter der HALBSEITE-Schwelle (Minimum 159 Wörter), alle Zählangaben der geänderten Stellen nachgezählt. Erstmals angewandt: die Vorbericht-Lehre, dass Streichungen Pflicht-Schichten mitreißen können — die einzige Streichung (Pointen-Schlusssatz) ist gegen alle sieben Pflichtregeln gegengeprüft, kein Verlust. Umfang netto **+20 Wörter** (4.890), Schätzung unverändert 9–10 Seiten. Teil-Check D bleibt ÜBERSPRUNGEN (`lualatex`/`latexmk`/`biber`/`pdflatex` geprüft, alle nicht installiert), daher ist der Score als „alle prüfbaren Kriterien erfüllt" zu lesen, nicht als „alle Kriterien geprüft" — der Seitenumfang ist ein Pflichtkriterium, das allein im Build gemessen wird. Vier kosmetische Hinweise ohne Score-Wirkung im Bericht (`\ac{}`-Erstnennung im Bindestrich-Kompositum bei BMEL und MVP, wiederholter Mitigations-Öffner in `02_limitationen.tex`, doppeltes „damit", gemischte Modalität in `07_iteratives_design.tex:3`).

**Skill-Update abgeglichen, drei Funde behoben (2026-07-28, diese Session).** Nach Anpassung von Skills und CLAUDE.md die neuen Skripte gegen den Textstand laufen lassen. (1) **Regression durch die Template-Updates** (`e06cd8e`/`f9c06c7`): `\listoffigures` und `\listoftables` in `main.tex` waren wieder auskommentiert, obwohl 5 Abbildungen und 4 Tabellen im Text stehen (ab 3 Pflicht) — derselbe Fix wie am 23.07., erneut nötig. Zulässige Änderung, da reines Schalten der markierten Aktivierungsblöcke. (2) Geviertstrich in `pages/appendix.tex:41` (innerhalb der zitierten Prompt-Zeile für `fig:mockup_profil`) durch Halbgeviertstrich ersetzt. (3) Feld **„Autorenschaft: Einzelarbeit"** in `aufgabe.md` ergänzt — das neue `typen/projektbericht.md` bindet den Selbstbezug daran, und `check_formalia.py` prüft „Projektgruppe" erst, wenn das Feld gesetzt ist. `check_formalia.py`: **0 FEHLER**, 36 HINWEIS.

**Abbildungs-Annotation gegen den aktuellen Zitierleitfaden geprüft und korrigiert (2026-07-28, diese Session).** Nutzer-Hinweis, dass die Leitfaden-PDFs ausgetauscht wurden — `git status` bestätigt: `Zitierleitfaden.pdf` (jetzt **Stand 01.10.2025**) und `Richtlinien für die Gestaltung wissenschaftlicher Arbeiten.pdf` modifiziert, neu hinzugekommen `sources/Richtlinie für die Nutzung KI-gestützter Systeme im Studium.pdf`. Alle drei neu extrahiert. Maßgeblich ist Zitierleitfaden Kap. 2.2.5 (S. 19–24): Beschriftung über der Abb./Tab. mit „Abb. N"/„Tab. N", Quellenzeile darunter nach dem Muster `Quelle: [Art der Erstellung] [Kurzbeleg] [ggf. Lizenz]`, KI-Bilder als `Erstellt mit dem Prompt „…" durch <Unternehmen>, <Jahr>` (Beispiel S. 24: „durch Microsoft, 2024. Microsoft 365-Lizenz."). Die neue KI-Richtlinie verweist für Bildquellen ausdrücklich auf den Zitierleitfaden zurück und legt für Texte „keine explizite Kennzeichnungspflicht" fest. **Korrigiert:** In allen vier Mockup-Quellenzeilen (`pages/appendix.tex`) stand „durch Claude Design (Claude Sonnet 5)" — also das Werkzeug statt des anbietenden Unternehmens und ohne Jahr; jetzt „durch Anthropic, 2026 (Claude Design, Modell Claude Sonnet 5)." Der projekteigene Zusatz „Abbildung nachträglich manuell bearbeitet." bleibt. Layout, `\quelle{}`-Makro, „Eigene Darstellung." bei den drei Eigenwerken und die Caption-Reihenfolge waren bereits regelkonform. `check_formalia.py`: 0 FEHLER. **Offen (Nutzer-Entscheidung):** Lizenzangabe (im Muster „ggf.", im KI-Beispiel gesetzt) bewusst weggelassen, da das Abo-Modell nicht bekannt ist; sowie die Frage, ob KI-Mockups zusätzlich ins Literaturverzeichnis gehören — der Leitfaden verweist dafür auf „Kapitel 2.3.7", dort steht aber Multimedia (Software-Beispiel: 2.3.9), der Querverweis ist im Original fehlerhaft. Lesart hier: kein Bib-Eintrag, weil KI als Hilfsmittel und nicht als Untersuchungsgegenstand genutzt wurde (Zitierleitfaden S. 4).

**Umfang erstmals gemessen statt geschätzt (2026-07-28):** `check_umfang.py` (neu) meldet **4.683 Wörter ≈ 12,5 Seiten** gegen die Vorgabe 7–10 (`aufgabe.md`) — die bisherige Projektschätzung „9–10 Seiten" war zu optimistisch, Abbildungen und Tabellen sind darin noch nicht enthalten. Kapitelanteile: Einleitung 579 / Durchführung 3.507 / Fazit 597 Wörter. Damit ist der Delta-Re-Audit vom 26.07. (100/100) überholt: Der neue `pruef-modus` deckelt den Score bei 89, solange der Umfang ungeprüft bzw. außerhalb der Vorgabe liegt.

**Kürzungsrunde durchgeführt (2026-07-28, diese Session) — Textteil jetzt gemessen 10 Seiten, Vorgabe 7–10 erfüllt.** Erstmals stand `lualatex`/`latexmk`/`biber` in der Umgebung zur Verfügung, der Umfang wurde deshalb **am gebauten PDF gemessen statt geschätzt**. Ausgangsmessung: **14 Seiten** Textteil (arabisch S. 1–14) bei 4.683 Wörtern — deutlich schlechter als die Heuristik von `check_umfang.py` (12,5 S.). Endstand: **10 Seiten, 3.637 Wörter** (Einleitung 521 / Durchführung 2.690 / Fazit 426 = 14,3 / 74,0 / 11,7 % — alle drei Anteile im Soll).

Drei Hebel, in dieser Reihenfolge wirksam:
1. **Template-Regression behoben** (größter Einzelposten, ohne jeden Textverlust): `pages/chapters.tex` trug wieder `\pagebreak` zwischen den Hauptkapiteln — genau der Fall, den `PERSISTENT.md` → `[LERNEN:sonstiges]` 2026-07-22 ausdrücklich verbietet und zur Entfernung anweist. Die Seitenmessung belegte den Schaden: eine Seite mit 169, eine weitere mit **13 Wörtern**. Entfernt; `\blankpage` und `\pagenumbering{arabic}` vor Kapitel 1 bleiben. Gleiche Regressionsklasse wie das ausgeknipste Abbildungsverzeichnis am selben Tag.
2. **Redundanzkürzung über alle 12 Subsections** (~1.000 Wörter): mehrfach ausformulierte Kernbefunde (die leftovercooking-Einordnung stand dreimal), Kontrastfiguren als Absatzschluss, Wegweiser-Absätze in den drei Kapitel-Mastern (dupliziert `03_vorgehen_und_aufbau.tex`), die als Aufzählung wiederholten drei Gegenargumente in `01_wettbewerbsanalyse.tex` (alle drei Fakten stehen unverändert in den Plattform-Absätzen; der gewichtigste Einwand samt Entkräftung bleibt ausformuliert), die dritte „Lehre" in `06_evaluation_reflexion.tex` (die Namenskollision selbst bleibt im Reflexionsabsatz erzählt, „drei Lehren" → „zwei Lehren" mitgeführt).
3. **`fig:konzeptfluss` gestrichen** (nach Rückfrage per `AskUserQuestion`): Das Tikz-Flussdiagramm gab den umgebenden Fließtext wieder, war **an keiner Stelle per `\autoref` referenziert** (also auch formal ein Float ohne Textverweis) und ist kein Lieferobjekt laut `aufgabe.md`. Abbildungs- und Tabellenverzeichnis bleiben mit je 4 Einträgen über der Pflichtschwelle von 3.

Ausdrücklich **nicht** gekürzt, nach Nutzer-Rückfrage: Wettbewerbsvergleich bleibt bei **sechs** Plattformen. Begründung: nur ~90 Wörter Ertrag, aber `02_limitationen.tex` führt „sechs Vertreter aus drei Plattform-Kategorien" als Stärke der Auswahl, und das gesamte Alleinstellungsmerkmal ist gegen leftovercooking formuliert. Ebenfalls unangetastet: die drei Subsections nahe der Halbseiten-Schwelle (`02_zielsetzung` 152, `03_vorgehen` 152, `07_iteratives_design` 148 Wörter). Alle Pflichtschichten geprüft erhalten: Limitationen dreischichtig, Phasenplanung als Tabelle, Syntheseleistung je Kapitel, Abhakliste-Schlüsselbegriffe (Event-Kalender-Begründung, Journaling/Menüplanung, Gruppen/Foren, Rezept-Upload, Bewertungs-/Kommentarsystem, personalisierte Empfehlungen), alle Lieferobjekte. `check_formalia.py` über `chapters/` + `pages/`: **0 FEHLER**, 52 HINWEIS. `check_bib_keys.py`: 6/6 valide. `check_status.py`: 0 FEHLER.

**Nächster Schritt: Delta-Re-Audit in frischer Session** — der Score ist neu zu erheben (der letzte, 100/100 vom 26.07., bezog sich auf den ungekürzten Textstand und ist damit überholt). Teil-Check D (Build) ist erstmals durchführbar und nicht mehr zu überspringen.

**Danach reine Nutzer-Schritte vor der Einreichung** (siehe `pruefbericht.md` → „Vor der Einreichung"): Eidesstattliche Erklärung über myCampus, LanguageTool-Durchgang, Mockup-Nachbesserung zu Befund 6.3. **Erledigt am 2026-07-28:** lokaler Build und Seitenumfang-Bestätigung — `latexmk -lualatex main.tex` läuft fehlerfrei durch, Textteil 10 Seiten, damit innerhalb 7–10. Zusätzlich offen: `\ModuleCode` und `\MatrikelNr` in `pages/meta.tex` sind noch Platzhalter und erscheinen so auf dem Titelblatt.

**AENDERUNGEN.md Runde 7 angelegt (2026-07-28, Methodenfundierung Beta-Test) — „Offen" ist damit nicht mehr leer.** Anlass: Abgleich der eigenen Vorprojektarbeit (`sources/vorherige-projektarbeit/`, Volltext `/home/normi/Documents/projekt-ui-design/latex/`) gegen den Resteria-Stand. Dort sind Usability-Tests real durchgeführt und methodisch belegt (ISO 9241-210, Think-Aloud nach Nielsen, SEQ nach Sauro & Lewis, Stichprobengröße nach Erlhofer); hier ist der Beta-Test in `07_iteratives_design.tex:5` und `05_phasenplanung.tex:14` rein organisatorisch beschrieben — ohne Verfahren, Erfolgskriterium oder Begründung der Gruppengröße. Fünf Befunde: 7.1 SEQ als messbares Kriterium, 7.2 Think-Aloud als benanntes Verfahren, 7.3 Gruppengröße begründen, 7.4 `[FREIGABE]` ISO-9241-210-Prozessrahmen (drei Optionen, inkl. Verwerfen), 7.5 Gegenfinanzierung über eine Dublette zwischen Iterations- und Phasenkapitel. Netto rund +60 Wörter bei 10 Seiten Textstand — **vor dem Commit neu bauen**, bei 11 Seiten zuerst 7.4 verwerfen. Norman und Tidwell bewusst ausgeschlossen (deuten in der Vorarbeit beobachtetes Nutzerverhalten, das hier fehlt). Die Runde enthält einen verbindlichen Abschnitt „Eigenplagiat-Grenze": keine Satzübernahme aus der Vorarbeit (die drei kritischen Stellen sind benannt), keine Ergebnisübernahme (SEQ-Werte, Zeitmessungen, Testpersonen betreffen DocSecBox), keine Zitationspflicht der Vorarbeit, da ausschließlich fremde Primärquellen übernommen werden. **Einmalige Nutzer-Freigabe für `references.bib`** genutzt: vier Einträge (`sauroLewisQuantifying2016`, `nielsenUsabilityEngineering1993`, `erlhoferWebsiteKonzeptionUndRelaunch2017`, `iso_9241_210`) aus der Vorarbeits-Bib von Hand übernommen — **keine BBT-Exporte**, ein künftiger Zotero-Export löscht sie wieder. `check_bib_hygiene.py` meldet für sie elf Hinweise (abgekürzter Institutionenautor „DIN", `location` bei drei Büchern, fehlendes Sprachfeld bei allen vier), die vor Abgabe über Zotero zu bereinigen sind. `check_bib_keys.py`: 6/6 genutzte Keys valide, die vier neuen erwartungsgemäß als ungenutzt gemeldet. `check_status.py`: 0 FEHLER. **Nächster Schritt: „Schreib-Modus: AENDERUNGEN.md abarbeiten" in frischer Session** — 7.4 dort per `AskUserQuestion` klären. Danach Delta-Re-Audit.

**AENDERUNGEN.md Runde 7 (Methodenfundierung Beta-Test) abgearbeitet (2026-07-28, diese Session).** Vier der fünf Befunde umgesetzt, einer auf direkte Nutzeranweisung übersprungen: 7.1 (SEQ als messbares Erfolgskriterium, Schwelle Mittelwert ≥ 5), 7.2 (Think-Aloud-Verfahren für das moderierte Gespräch) und 7.3 (Gruppengröße über Erlhofer begründet, Richtungsfehler „erforderlich" statt „ausreichend" vermieden) als drei neue Sätze in `chapters/02_durchfuehrung/07_iteratives_design.tex:5` ergänzt, jeweils mit den drei von Hand eingetragenen Bib-Quellen belegt. `\acro{SEQ}{Single Ease Question}` in `pages/acronyms.tex` ergänzt, da der Begriff zusätzlich in der Phasenplanungs-Tabelle vorkommt. 7.5 (Gegenfinanzierung): Dublette in `07_iteratives_design.tex:3` gestrichen („genau das begründet die Abhängigkeit…"), `tab:phasenplanung`-Zelle „Beta-Test & Feedback" von „Test mit kleiner Nutzergruppe, Fragebögen, Feedback-Gespräche" auf „Think-Aloud-Sitzungen, \ac{SEQ}-Fragebogen" umgestellt. **7.4 auf ausdrückliche Nutzeranweisung beim Sessionstart übersprungen** (Option C, kein neuer Prozessrahmen-Absatz) — keine `AskUserQuestion`-Rückfrage nötig, da die Entscheidung bereits direkt vorlag, nicht aus einer Auslassung stillschweigend abgeleitet wurde. `iso_9241_210` bleibt entsprechend ungenutzt in `references.bib`. `check_formalia.py` auf beiden Dateien: 0 FEHLER (nur bekannte HINWEIS-Artefakte plus ein neu identifizierter Skript-Blindfleck: `check_formalia.py`s `LOCATOR_RE` erkennt `[Kap.~8]`-Stellenangaben mit dem IU-korrekten geschützten Leerzeichen `~` vor der Ziffer nicht, meldet fälschlich SEITENANGABE-Hinweise — die Stellenangabe ist inhaltlich vorhanden, kein echter Regelverstoß). `check_bib_keys.py`: alle drei neu zitierten Keys valide. **Nicht maschinell geprüft:** Die drei neuen Zitationen haben keinen Volltext im Bib-Eintrag (`check_quellentreue.py` → NICHT PRÜFBAR); Details und Prüfauftrag in `AENDERUNGEN.md` Runde 7 → „Bitte manuell prüfen". Umfang: `check_umfang.py` 3.783 Wörter ≈ 10,1 Seiten gesamt — vor dem nächsten Build-Check die Seitenzahl neu messen, die Runde könnte die zuvor exakt bei 10 stehende Grenze in Richtung 11 kippen.

**`check_quellentreue.py` um E-Book-Unterstützung erweitert (2026-07-28, diese Session, auf Nutzerwunsch).** Bib-Einträge dürfen im `file`-Feld jetzt auch ein `.epub` tragen; statt eines PDF-Seitenversatzes wird das eigene NCX-Inhaltsverzeichnis des E-Books ausgewertet und `[Kap. X]`-Locators (z. B. „17.2.2") direkt gegen die dort hinterlegte Kapitelnummer aufgelöst – reine Stdlib (`zipfile`/`re`), kein neues Pip-Paket. Löst das Inhaltsverzeichnis die zitierte Unterebene nicht auf, greift dieselbe Ganzbuch-Fallback-Logik wie bei PDFs ohne bestimmbaren Seitenversatz (Prüfung bleibt möglich, nur nicht kapitelgenau). `references.bib` → `erlhoferWebsiteKonzeptionUndRelaunch2017` um `file = sources/literatur/Website-Konzeption…epub` ergänzt (fällt unter die bereits erteilte einmalige Freigabe für die vier Runde-7-Einträge). Getestet: `epub_kapitel()`/`resolve_kapitel()` direkt gegen die reale Datei (259 erkannte Kapitel, „17.2.2" exakt aufgelöst, „17.2.2.9" korrekt auf „17.2.2" zurückgefallen, unbekannte Nummer „99.9" korrekt in den Ganzbuch-Fallback gegangen, defektes Test-EPUB sauber als Exception abgefangen). Voller Lauf über `chapters/` bestätigt: Die Runde-7-Zitation `\textcite[Kap.~17.2.2]{erlhoferWebsiteKonzeptionUndRelaunch2017}` ist jetzt **NICHT PRÜFBAR → PRÜFEN** (`quellencheck.md`, Hash `c27b5ba809`) – kein FEHLER in `check_formalia.py`/`check_bib_hygiene.py`. **Nebenbefund beim Testen:** Die zitierte Aussage ist am Original verifiziert – Erlhofer, Kap. 17.2.2, Praxistipp „Fünf Test-User reichen meist": „Schon bei fünf Personen werden Sie feststellen, dass sich die meisten Usability-Probleme wiederholen und Sie damit die gravierenden Stolperstellen aufgedeckt haben." Deckt die Textaussage in `07_iteratives_design.tex:5` inhaltlich; damit ist Punkt 1 aus der „Bitte manuell prüfen"-Liste der Runde 7 für Erlhofer erledigt, Sauro & Lewis sowie Nielsen bleiben offen (kein epub/PDF für diese beiden im `file`-Feld hinterlegt).

**Nachtrag selbe Session (2026-07-28): Nutzer hat auch die Volltexte für Sauro & Lewis und Nielsen nachgelegt** (beides PDF, nicht epub, unter `sources/literatur/`) — `references.bib` um die passenden `file`-Felder ergänzt. Dabei zwei echte Bugs gefunden und behoben, keine kosmetischen: (1) `[Kap. X]`-Locators funktionierten bisher nur für epub-Quellen; eine PDF-Quelle mit `[Kap. X]` blieb fälschlich NICHT PRÜFBAR, obwohl ein PDF vorlag. Fix: PDF+Kapitel-Locator fällt jetzt sauber auf eine Ganzdokument-Suche zurück (neuer `elif z.kapitel:`-Zweig in `pruefe()`, eigene Catch-all-Meldung ohne das sonst PDF-Seiten-spezifische `--offset`). (2) `pdf_pfad`/`epub_pfad` zerschnitten das `file`-Feld an jedem `;` (Zotero-Mehrdatei-Konvention) — die Anna's-Archive-Dateinamen enthalten aber selbst Semikolons („Jeff Sauro; Professor of Religious Studies…", „Morgan Kaufmann; Morgan Kaufmann"), wodurch der Pfad in unbrauchbare Fragmente zerfiel und beide Dateien trotz vorhandenem `file`-Feld als „weder PDF noch URL" galten. Fix: `_datei_kandidaten()` versucht das ganze Feld zuerst unverändert, bevor es nach Zotero-Konvention aufteilt. Nach dem Fix lösen beide Pfade korrekt auf. **Volltextprüfung:** Nielsen Kap. 6.8 („Thinking Aloud", S. 195) deckt die Textaussage vollständig ab. Sauro & Lewis Kap. 8: die SEQ selbst (siebenstufige Skala) ist exakt belegt, die konkrete Schwelle „Mittelwert ≥ 5" fand sich in der Stichwortsuche **nicht** als explizit formulierte Regel — bleibt als offener Punkt in `AENDERUNGEN.md` Runde 7 → „Bitte manuell prüfen" stehen. Voller Lauf über `chapters/`: 0 NICHT PRÜFBAR mehr (vorher 2), `check_formalia.py`/`check_bib_hygiene.py`/`check_bib_keys.py` weiterhin 0 FEHLER.

**Nachtrag auf Nutzerwunsch „nochmal nachlesen" (2026-07-28, diese Session):** Alle 41 SEQ-Fundstellen im Sauro-&-Lewis-Volltext einzeln durchgesehen (nicht nur der Definitionsabschnitt) — die Schwelle „Mittelwert von mindestens fünf" steht an keiner Stelle als Regel; die einzige „5" im SEQ-Kontext betrifft die 5- vs. 7-Punkte-Skalenvariante, eine spätere Übungsaufgabe („average of 5.6 … greater than 5?") ist ein reines Statistikbeispiel ohne Bezug zu einem SEQ-Erfolgskriterium. **Entschärft:** `chapters/02_durchfuehrung/07_iteratives_design.tex:5` — „Als Schwelle für eine gelungene Aufgabe gilt dabei ein Mittelwert von mindestens fünf." → „Als Schwelle für eine gelungene Aufgabe legt diese Arbeit einen Mittelwert von mindestens fünf fest." Der `\parencite[Kap.~8]{sauroLewisQuantifying2016}` bleibt am SEQ-Definitionssatz stehen (dort korrekt belegt); die Schwelle selbst ist jetzt explizit als eigene methodische Festlegung ausgewiesen, nicht mehr implizit Sauro & Lewis zugeschrieben. `check_formalia.py`: weiterhin 0 FEHLER, `check_umfang.py`: +1 Wort (vernachlässigbar). Damit ist der letzte offene Punkt aus der „Bitte manuell prüfen"-Liste der Runde 7 geschlossen.

**Kleine Kürzungsrunde (2026-07-28, diese Session):** Nutzer meldete 10,3 Seiten aus einem lokalen Build (Runde 7 stand bei geschätzten 10,1 S. laut `check_umfang.py`, also war real schon mehr Overflow da als die Heuristik zeigte). Acht rein redundanzbasierte Kürzungen ohne jeden Bezug zu These, Kernargumenten oder bereits per Freigabe/Audit fixierten Formulierungen (Widerspruchssatz `01_wettbewerbsanalyse.tex:34`, SWOT-Satz, Freemium-Begründung, Instagram/TikTok-Stellvertreter-Regelung — alles unangetastet): (1) `06_evaluation_reflexion.tex` — Dublette „Challenge-Banner und Einsparangabe" gestrichen, die wortgleich schon in `07_iteratives_design.tex:3` (unmittelbar davor gelesen) steht; (2) ebd. — generischer Reflexionssatz „mehr Zeit für Vergleichsplattformen" gestrichen (Absatz behält zwei konkrete, belegte Kursänderungen); (3) ebd. — Teilaufgaben-/Berichtsinhalte-Checkliste sprachlich verdichtet (alle `\autoref`s erhalten); (4) ebd. — „Zwei Lehren"-Absatz gestrafft; (5) `05_phasenplanung.tex` — Tabellenankündigungssatz verdichtet; (6) ebd. — selbsterklärende Abhängigkeits-Begründung „da sich Nutzer:innen sonst nicht sinnvoll äußern können" gestrichen (Kernaussage „Beta-Test setzt MVP voraus" bleibt); (7) `01_wettbewerbsanalyse.tex` — abstrakte Lücken-Vorwegnahme gestrichen, die der unmittelbar folgende Satz ohnehin konkret ausführt; (8) `03_konzept.tex` — vorwegnehmender Navigations-Satz gestrichen, der in Z. 34 mit den drei Bereichen ausführlich wiederholt wird. Bei zwei Edits (Rückblickend-Satz, Navigation-Satz) riss die Ersetzung versehentlich die Absatzleerzeile mit heraus — beide direkt nachkorrigiert und per `git diff` verifiziert. Ergebnis: **3.784 → 3.703 Wörter** (`check_umfang.py`: 9,9 statt 10,1 S., Kapitel 2 allein 2.749 W.). `check_formalia.py`: 0 FEHLER (43 HINWEIS, ausschließlich bekannte Artefakte). `check_bib_keys.py`/`check_status.py`: unverändert ok. Kompiliert wurde bewusst **nicht** selbst (Grundprinzip „Kompilieren ist Sache des Nutzers") — die 81 Wörter Ersparnis liegen nach dem alten Ankerwert 3.637 W. ≈ 10,0 S. (echter Build, große Kürzungsrunde) in der Größenordnung, die für „unter 10 Seiten" nötig war, aber ob es angesichts der Lumpigkeit von Seitenumbrüchen reicht, zeigt erst der nächste lokale Build.

**Kürzungsrunde 2, selbe Session (2026-07-28):** Nutzer-Rückmeldung nach Runde 1: lokaler Build bricht weiterhin auf 11 Seiten um. Vor weiteren Kürzungen zuerst auf eine Strukturregression geprüft (Grund für den großen Sprung 14→10 S. in der ursprünglichen Kürzungsrunde war ein wiedereingeschlichener `\pagebreak`) — `pages/chapters.tex` und `main.tex` diesmal sauber, kein `\pagebreak`, keine Regression; der Rückstand ist real inhaltlich (Runde 7 vom selben Tag hatte ~150 Wörter Methodenfundierung ergänzt, die offenbar genau die Seite kippen ließ). Sieben weitere Kürzungen, wieder ausschließlich Prosa-Verdichtung ohne Faktenverlust, keine Zitation/kein `\autoref` entfernt: `07_iteratives_design.tex` Übergangssatz zur Design-Phase verschmolzen, Beta-Test-Methodik-Absatz gestrafft (SEQ/Think-Aloud/Erlhofer-Belege, Stichprobenbegründung, Disclaimer-Satz alle erhalten) · `03_konzept.tex` Cafeteria-Beispiel bei der Namensherleitung gestrichen · `05_phasenplanung.tex` „Das MVP ist dabei..." verdichtet · `01_wettbewerbsanalyse.tex` Einwand-Einleitung verdichtet (der audit-fixierte Schlusssatz zum Alleinstellungsmerkmal blieb unangetastet) sowie die leftovercooking-Beschreibung verdichtet (jede Einzelaussage — KI-Vorschläge, Food-Creator:innen, Punktesystem-Einordnung — erhalten) · `01_kernergebnisse_und_ausblick.tex` dritte Wiederholung von „Verfolgen von Creator-Inhalten" gestrichen (stand bereits zweimal in der Wettbewerbsanalyse). **Ergebnis: 3.784 → 3.654 Wörter** (`check_umfang.py`: 9,7 S.), praktisch auf dem alten Ankerwert 3.637 W., der beim letzten echten Build exakt 10 Seiten ergab. `check_all.py --kapitel chapters/02_durchfuehrung`: 0 FEHLER (die einzelne `check_formalia.py`-FEHLER-Meldung bei isoliertem Lauf auf nur `chapters/` war ein Scope-Artefakt — Abbildungen liegen in `pages/appendix.tex`, das der Einzelaufruf nicht sah; mit `check_all.py` sauber). Kompiliert wurde weiterhin bewusst nicht selbst.

**Offene Fragen**: Keine blockierenden Punkte. Nutzer-Restpflichten (nicht-blockierend): Namensverfügbarkeit „Resteria" (DPMA / .de-Domain) · Nivedhitha/Shen nur per Abstract → beim Schreiben vorsichtig formulieren, bis Volltext vorliegt · Rettungs-Feed-Mockup (`resteria_rettungsfeed.png`) zeigt noch den in Befund 6.3 beschriebenen Plus-Button-Überlapp, bis der Nutzer das Bild in Claude Design nachbessert · **lokaler Build nach Kürzungsrunde 2 vom 2026-07-28 steht noch aus** — bei weiterhin >10 Seiten ist die freie Redundanz-Reserve jetzt spürbar erschöpft; nächster Schritt wäre eine Rückfrage, ob die Sechs-Plattform-Wettbewerbsanalyse (bisher zweimal auf Nutzerwunsch bei sechs belassen, ~90 Wörter Ersparnis bei Reduktion) diesmal doch gekürzt werden soll · die drei Runde-7-Zitationen sind ungeprüft (siehe oben).

**Fristen**: Abgabe: kein festes Datum (`\today` auf Nutzerwunsch, siehe `PERSISTENT.md`) · Eidesstattliche Erklärung (myCampus): [offen]

> Aktualisiert nach jedem Checkpoint bzw. jeder gespeicherten Datei, sichtbar quittiert. Für Planungsinhalte gilt bei Widerspruch der Plan/Draft, für Statuswerte diese Tabelle.
