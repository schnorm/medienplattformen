# CLAUDE.md – Wissenschaftliches Schreiben an der IU

**Projekt:** [wird pro Arbeit ausgefüllt – Titel/Arbeitstitel] · **Papiertyp:** [Hausarbeit/Fallstudie/Seminararbeit/Projektbericht] · **Institution:** IU Internationale Hochschule

## Schnellreferenz – Session starten

| Du willst … | Schreibe in neuer Session |
|---|---|
| Umgebung prüfen (einmalig zu Beginn) | „Setup-Check" |
| Kapitelplan erstellen (Standard, 1 Session) | „Plan kompakt" |
| Kapitelplan ausführlich (empirische Seminararbeit) | „Plan ausführlich" |
| Nächstes Kapitel schreiben | „Schreib-Modus: nächstes Kapitel" |
| Ein Argument prüfen lassen | „Stresstest auf: <Argument>" |
| Arbeit mit fremden Augen gegen die Aufgabenstellung prüfen (alle Kapitel FERTIG) | „Gegenlesung" |
| Ganze Arbeit inhaltlich prüfen (sobald alle Kapitel FERTIG, **vor** dem Voll-Audit) | „Gesamt-Stresstest" |
| Erstes Kapitel gegenprüfen (früh, ohne Score) | „Audit Kapitel: <name>" |
| Kapitelplan auf Kohärenz prüfen (empfohlen vor dem Schreiben) | „Plan-Audit" |
| Mechanisch prüfen, ob das Schreiben starten kann | „Bereitschafts-Check" |
| Arbeit/Kapitelplan prüfen | „Audit" |
| Benotetes Tutor-Feedback in Lernpunkte übersetzen | „Feedback einarbeiten" |
| Status erfahren | „Wo stehen wir?" |

## Grundprinzipien

- **Kompilieren ist Sache des Nutzers, nicht meine.** Nach Edits an `.tex`-Dateien nicht ad-hoc selbst `lualatex`/`latexmk` laufen lassen, um die eigene Änderung zu verifizieren – der Nutzer kompiliert lokal. Ausnahme: Teil-Check D (Build) im `pruef-modus` bleibt bestehen, da er ein bewusster, score-relevanter Audit-Bestandteil ist und kein Ad-hoc-Selbstcheck.
- **Dateien sind Wahrheit, nicht der Chat-Verlauf.** `aufgabe.md`, `kapitelplan.md`, `kapitelplan.draft.md` und `chapters/**/*.tex` gelten immer vor Erinnerung/Memory – bei Zweifel lesen, nicht annehmen.
- **Erst prüfen, dann behaupten.** Bevor eine belastbare Aussage über die Außenwelt (Marktlage, Konkurrenzprodukte, Werkzeuge, Zahlen) **oder** eine formale/prozedurale Behauptung („das ist ein Blocker", „so darf man nicht zitieren", „die Regel verlangt X") getroffen wird: die maßgebliche Quelle konsultieren – Web für Außenwelt-Fakten, die **Projektdatei** für interne Regeln (`aufgabe.md`, das optionale `sources/Anmerkungen vom Prüfer.md`, `hard-rules-formal.md`, `typen/<typ>.md`). Kein Rating, kein Blocker, keine Formalia-Aussage aus dem Gedächtnis. Im Zweifel als Hypothese kennzeichnen und verifizieren, nicht als Fakt formulieren.
- **`main.tex`, `pages/cover.tex` und `references.bib` nicht umbauen** (Ausnahme: `references.bib` per Zotero/BBT-Auto-Export) – gilt unabhängig vom aktiven Skill. **Einzige erlaubte Änderung an `main.tex`: die dort markierten Aktivierungsblöcke ein- oder auskommentieren** (Gender-Disclaimer, Abbildungs-/Tabellenverzeichnis, Anhangsverzeichnis, Anhang). Ein Anhang, der nur deshalb im PDF fehlt, weil niemand den Block einkommentieren durfte, ist ein Abgabefehler; `check_formalia.py` meldet genau diesen Fall. Regeln dazu: `_shared/hard-rules-formal.md`. Alles andere in `main.tex` und `cover.tex` bleibt unangetastet. Projektspezifische Angaben (Titel, Papiertyp, Modul, Tutor:in, Abgabedatum, PDF-Schlagworte) stehen **ausschließlich in `pages/meta.tex`** – `cover.tex` und die PDF-Metadaten ziehen sich alles von dort. `meta.tex` ist im Gegensatz zu `main.tex`/`cover.tex` **editierbar**: Der `setup-check` erhebt die Angaben zu Projektbeginn im Dialog und trägt sie dort ein; unbekannte Felder bleiben als Platzhalter stehen und werden als OFFEN gemeldet.
- **Single Source of Truth**: `aufgabe.md` für die Aufgabenstellung (destilliert in Plan-Modus Schritt 0 – spätere Sessions lesen diese Datei, nicht das Aufgaben-PDF), `kapitelplan.md` für Struktur/Argumentation, `chapters/**/*.tex` für den finalen Text, `references.bib` für Zitationen (nie manuell editieren).
- **These und Kernargumente kommen vom Nutzer.** Ich hinterfrage und schärfe, liefere aber nicht die inhaltliche Position selbst. Das ist zugleich die Bedingung der IU-KI-Richtlinie: Die Verantwortung für alle Inhalte liegt beim Studierenden; jede von mir gelieferte Quelle, Zahl oder Behauptung muss der Nutzer am Original verifizieren, bevor sie in die Arbeit übernommen wird.
- **Freigabepflichtige Punkte aktiv per `AskUserQuestion` klären, nicht nur im Fließtext vermerken.** Trägt ein Arbeitsauftrag (z. B. ein Punkt in `AENDERUNGEN.md`, `pruefbericht.md`) einen Vorbehalt wie „nur mit expliziter Freigabe ändern", oder betrifft er These/Kernargumente/Leitfrage, dann reicht es nicht, den Punkt auszulassen und die Begründung nur in der Zusammenfassung zu erwähnen – das verlagert die Entscheidung stillschweigend auf den Nutzer, der sie leicht überliest. Stattdessen noch in derselben Session aktiv nachfragen, bevor der nächste Schritt (Audit, Commit, PR) beginnt.
- **Unsicherheit wird zur Prüfaufgabe für den Nutzer, nicht zur Fußnote.** Wo ich etwas nicht selbst verifizieren kann – Scan ohne Textebene, Quelle hinter Bezahlschranke, Heuristik mit bekannter Fehlerquote, widersprüchliche Angaben – wird das **nicht** stillschweigend als geprüft verbucht und auch nicht in einem Nebensatz erwähnt. Stattdessen steht am Ende der Antwort eine eigene Liste **„Bitte manuell prüfen"**: je Punkt (1) was genau nachzusehen ist, (2) wo – Datei, Zeile, Quelle, Seite, (3) woran zu erkennen ist, dass es stimmt, und (4) warum ich es nicht konnte. Ein „konnte ich nicht prüfen" ohne diese vier Angaben ist wertlos: Die Verantwortung für jede Aussage liegt laut KI-Richtlinie beim Studierenden, und dafür muss klar sein, wo er hinsehen soll.
- **Keine personenbezogenen Daten in die Session geben** (KI-Richtlinie der IU): Interview-Transkripte vor der Verarbeitung anonymisieren (Namen → B1, B2 …), keine vertraulichen Unternehmensdaten ohne Freigabe.
- **Nur „Pflicht"-Regeln aus `typen/<typ>.md` zählen als FAIL.** „Optional"/„empfohlen" sind Hinweise, „entfällt" wird gar nicht geprüft.
- **Checkpoints und gespeicherte Dateien werden im Chat sichtbar quittiert**, nie nur still geschrieben (siehe `plan-modus`/`schreib-modus` → Sichtbarkeitspflicht).
- **Explizite Korrekturen des Nutzers sofort als `[LERNEN:kategorie]` eintragen** (nicht am Sessionende sammeln – Sessions enden abrupt) und quittieren („📝 In PERSISTENT.md gelernt: …"). **Default-Ziel ist `PERSISTENT.md`**, die Datei, die von Projekt zu Projekt kopiert wird; nach `MEMORY.md` gehört ein Eintrag nur, wenn er sich konkret auf *diese* Arbeit bezieht (dieses Thema, dieser Papiertyp, diese Aufgabenstellung). Alles zu Stil, Ton, Zitier-Sonderfällen, Modellwahl und Session-Führung ist projektübergreifend – im falschen File ist es beim nächsten Projekt verloren. Widersprüchliche neue Einträge markieren den alten als `[ÜBERHOLT]`. Wo Skills `MEMORY.md` lesen, gilt: beide Dateien lesen; bei Widerspruch gewinnt `MEMORY.md`.

## Session-Regeln (Memory-Management)

Kern des Workflows: **viele kurze Sessions statt einer großen.** Lange Sessions verbrennen Tokens und werden gegen Ende unzuverlässig. Deshalb:

1. **Session-Start-Ritual (<1 Min.):** Statustabelle unten + „Aktuelle Richtung" lesen → `PERSISTENT.md`-/`MEMORY.md`-Einträge beachten → nur die Dateien laden, die das heutige Arbeitspaket braucht (der aktive Skill listet sie). Nicht pauschal Plan und alle Kapitel einlesen.
2. **Arbeitspaket-Größen:** Schreiben = 1 Hauptteil-Kapitel pro Session (**Ausnahme klein:** Einleitung + Schluss dürfen zusammen in eine Session – je ~10 %, inhaltlich zusammenhängend) · Audit = 1 Session, die **Überarbeitung danach ist eine eigene Schreib-Session** (`pruefbericht.md` trägt die Arbeitsliste hinüber – nicht im vollen Audit-Kontext weiterschreiben) · Plan kompakt = 1 Session, ausführlich 1–2. Wo ein Paket sinnvoll geschnitten wird (Plan-Modus nach Schritt 1), sagt der Skill an Ort und Stelle.
3. **Session-Ende-Ritual:** Checkpoint in der Draft-/Plan-Datei → Statustabelle aktualisieren → „Aktuelle Richtung" auf den nächsten Schritt setzen → im Chat quittieren.
4. **Kontext fast voll oder Session driftet?** Nicht „noch schnell fertig machen" – Checkpoint schreiben, Session beenden, frisch weitermachen. Die Dateien garantieren verlustfreie Fortsetzung.

## Ordnerstruktur

Vollständiger, kommentierter Baum: `.claude/skills/_shared/projektstruktur.md` – laden `setup-check` (prüft die Struktur) und `plan-modus` Schritt 2.5 (Dateinamen der Kapitel). Für jede andere Session genügen die Single-Source-Dateien:

| Datei / Ordner | Rolle |
|---|---|
| `aufgabe.md` | destillierte Aufgabenstellung – Single Source statt Aufgaben-PDF |
| `kapitelplan.md` | Struktur und Argumentation (Draft: `kapitelplan.draft.md`) |
| `chapters/**/*.tex` | finaler Text – ein Ordner je Hauptkapitel, Master bindet Subsections |
| `references.bib` | Zitationen (BBT-Export aus Zotero, nie manuell editieren) |
| `pages/meta.tex` | alle Projektangaben – einzige editierbare Datei in `pages/` |
| `quellencheck.md` · `quellencheck-state.json` | Volltextabgleich aller Zitationen: Bericht (pro Lauf neu) und Urteile (überdauern) – erzeugt `check_quellentreue.py`, nie von Hand ändern |
| `AENDERUNGEN.md` · `pruefbericht.md` | Arbeitsaufträge in Runden · letzter Audit-Bericht |
| `PERSISTENT.md` · `MEMORY.md` | projektübergreifende Präferenzen · Lernpunkte nur dieser Arbeit |
| `SKILL-ANPASSUNGEN.md` | offene Änderungsvorschläge an Skills, Skripten und dieser Datei – kein Abgabebestandteil |
| `sources/` · `sources/literatur/` | IU-Leitfäden + optionale Prüfer-Anmerkungen · eigene Literatur |

## Workflow-Fahrplan

| Phase | Skill | Wann | Output |
|---|---|---|---|
| Setup | `setup-check` | Einmalig bei neuem Projektordner | Bestätigung: Umgebung bereit |
| Planen | `plan-modus` | Aufgabenstellung liegt vor, kein Kapitelplan | `aufgabe.md` + `kapitelplan.md` |
| Schreiben | `schreib-modus` | `kapitelplan.md` existiert | `chapters/**/*.tex` |
| Gegenlesen | `gegenlesung` | Sobald alle Kapitel FERTIG – **vor** Gesamt-Stresstest und Voll-Audit | neue Runde in `AENDERUNGEN.md` |
| Stresstest | `stresstest` | Jederzeit einzelnes Argument · Gesamt-Stresstest über die ganze Arbeit, **sobald alle Kapitel FERTIG sind – nach der Gegenlesung, vor dem Voll-Audit** (Stresstest prüft Inhalt, Prüf-Modus prüft Regeln; inhaltliche Befunde ändern den Text und würden ein vorher gefahrenes Audit entwerten) | Gegenargumente + Stärke · Gesamt: neue Runde in `AENDERUNGEN.md` |
| Prüfen | `pruef-modus` | Kapitel-Audit nach Kapitel 1 · Voll-Audit vor Abgabe, **erst nach abgearbeiteter Gegenlesung und abgearbeitetem Gesamt-Stresstest** (Umfänge: siehe Skill) | `pruefbericht.md` mit Score |

**Kompakt-Prüfkette (nur Hausarbeit & Projektbericht):** Bei den beiden kleinsten Formaten ist der Gesamt-Stresstest **optional** – Default-Kette dort: Gegenlesung → Abarbeiten → Voll-Audit. Grund: Bei 7–10 Seiten prüft die Gegenlesung dieselben wenigen Kernargumente ohnehin mit; ein voller Extra-Durchgang kostet mehr Session, als er findet. Auf Wunsch oder bei erkennbar wackliger Argumentation bleibt die volle Kette jederzeit möglich; ein bewusst übersprungener Gesamt-Stresstest wird im Prüfbericht vermerkt. **Fallstudie und Seminararbeit fahren immer die volle Kette** (eigene Analysen bzw. Empirie sind die typischen Overclaim-Quellen).

**Mehrsession-Fähigkeit Plan-Modus**: Zwischenstand liegt fortlaufend in `kapitelplan.draft.md` (Checkpoint nach jedem Schritt). Bei Sessionstart immer zuerst prüfen, ob `kapitelplan.draft.md` existiert, statt bei Schritt 0 neu zu beginnen. Schreib-Modus liest `kapitelplan.md` per Read-Tool, nie aus dem Gesprächsverlauf.

## Format `AENDERUNGEN.md`

Verbindliches Runden-Format in `.claude/skills/_shared/aenderungen-format.md` – laden nur die Skills, die die Datei schreiben oder abarbeiten (`gegenlesung`, `stresstest` → Gesamt, `schreib-modus` → Abarbeiten). Zwei Regeln gelten überall, auch ohne diese Datei: `[FREIGABE]`-Punkte (These, Kernargumente, Leitfrage) nur nach aktiver Rückfrage umsetzen, und „## Offen" ist leer, bevor ein Voll-Audit läuft.

## Papiertyp- und Formalia-Regeln

- **Papiertyp-spezifisch** (Pflichtregeln, Bewertungsgewichte, Kapitelgerüst, Kernfragen, Voice, Audit-Checks): eine Datei pro Typ in `.claude/skills/_shared/typen/<typ>.md` – geladen, sobald der Papiertyp feststeht, bewusst nicht hier. Quellen-Zahlen dort sind Richtwerte aus der Praxis, keine IU-Vorgabe.
- **Formale Hard Rules** (Zitationen, LaTeX, Pronomen, Struktur, Schreibstil, gendersensible Sprache): `.claude/skills/_shared/hard-rules-formal.md` – braucht nur `schreib-modus`/`pruef-modus`. Die sicherheitskritische Ausnahme (`main.tex`/`cover.tex`/`references.bib` nicht umbauen, nur Aktivierungsblöcke schalten) steht oben in den Grundprinzipien.
- **Mechanische Prüfungen gebündelt aufrufen**: `python .claude/skills/_shared/scripts/check_all.py` fährt Keys, Formalia, Bib-Hygiene und Status in einem Lauf (`--kapitel <ordner>` beim Schreiben, `--mit-quellen` im Audit) – statt vier Einzelaufrufe mit je eigener Ausgabe. Die Einzelskripte bleiben für die Detailarbeit an einem Befund aufrufbar.

## „Wo stehen wir?" (Status-Anfrage – definierter Ablauf)

Reine Lese-Operation, keine Arbeitssession:
1. Statustabelle unten + „Aktuelle Richtung" + „Fristen" lesen (sind ohnehin geladen).
2. `python .claude/skills/_shared/scripts/check_status.py` laufen lassen – meldet Widersprüche Tabelle ↔ Dateisystem deterministisch.
3. Antwort in höchstens 8 Zeilen: Stand heute (1 Satz) · zuletzt abgeschlossener Schritt · **nächster Schritt** als konkreter Session-Einstieg · offene Blocker/Fristen · check_status-Funde, falls vorhanden.
4. Keine Kapitel, keinen Plan, kein MEMORY pauschal laden. Bei Widerspruch Tabelle ↔ Dateisystem gilt das Dateisystem – Status korrigieren und quittieren ist die einzige zulässige Änderung in dieser Anfrage.

## Aktueller Projektstatus

**Zwei Arten von Information, zwei Regeln:**
- **Zeilenbestand** (welche Komponenten existieren) wird aus `kapitelplan.draft.md` (bzw. `kapitelplan.md`, sobald final) abgeleitet – nie geschätzt.
- **Statuswerte** (FERTIG / IN ARBEIT / ÜBERARBEITUNG NÖTIG / NICHT BEGONNEN) pflegt ausschließlich der jeweilige Skill nach Speichern/Audit – Datei-Existenz ≠ fertig. Fehlt die Datei einer FERTIG-Komponente, gilt das Dateisystem: Status korrigieren und Widerspruch melden.

| Komponente (Identifikator) | Pfad | Status | Notizen |
|---|---|---|---|
| Aufgabenstellung | `aufgabe.md` | FERTIG | Aufgabenstellung 2 (Hobbyköch:innen), Nische Zero-Waste |
| Kapitelplan | `kapitelplan.md` | FERTIG | Plattform „Resteria" (Name 2026-07-21 gesetzt, Kollision „Restlos Glücklich" behoben) — 3 Kapitel, 12 Subsections (2026-07-24: `sec:iteratives_design` ergänzt, Befund 5.2) |
| `sec:einleitung` | `chapters/01_einleitung/einleitung.tex` | FERTIG | **Nachtrag 2026-07-29 (nach Delta-Re-Audit, auf Nutzerwunsch in derselben Session):** Haushalts-Claim in `01_ausgangslage_und_problem.tex:5` nach `AskUserQuestion` umformuliert — statt der Zuschreibung allein an Haushalte jetzt „nicht im Handel oder in der Industrie, sondern auf der Verbraucherebene und dort vor allem in privaten Haushalten". Grund: Vittuari S. 105 nennt 65 % für Haushalte UND Food Service zusammen und spricht von Verantwortung „to consumers, particularly at the household level" — die neue Fassung bildet Ebene und Schwerpunkt exakt ab. +8 Wörter, Seitenzahl unverändert 10. Neu quittiert (Hash `3ead511986`). **Stresstest-Nachtrag 2026-07-30 (diese Session):** In `02_zielsetzung_und_verengung.tex:5` die Absolutformulierung „Gamification-Elemente … ergeben **nur** für Menschen einen Anreiz, denen Nachhaltigkeit … bereits wichtig ist" auf „**dürften vor allem** für Menschen einen Anreiz darstellen … oder die sich dafür zumindest interessieren" abgeschwächt. Anlass: Einzel-Stresstest fand einen Selbstwiderspruch mit der eigenen, bereits zitierten Quelle Soma et al. 2020 — deren Gamification-Gruppe wurde ohne Vorab-Screening auf Nachhaltigkeitsinteresse rekrutiert (zufällige Haushalts-Cluster, Door-to-Door) und zeigte trotzdem einen marginal signifikanten Effekt (p = 0,07), was der „nur"-Behauptung widerspricht. Entscheidung nach `AskUserQuestion`-Abgleich mit `sources/Anmerkungen vom Prüfer.md` (Punkt 1, d, Antwort auf Frage 1): abgeschwächte Eigeneinschätzung statt Streichung oder Zusatzzitat, da der Prüfer explizit eigenständige Argumentation ohne Empirie-Pflicht verlangt und den Bericht ausdrücklich als „keine wissenschaftliche Arbeit" einordnet. `check_all.py` auf `chapters/01_einleitung`: 0 FEHLER. Rest der Datei unverändert. Kürzungsrunde 2026-07-28: 579 → 521 Wörter (14,3 % Anteil, im Soll). Gekürzt ausschließlich in `01_ausgangslage_und_problem.tex` (Kontrastfiguren, doppelte Lückenbeschreibung) sowie im Master-Wegweiser; `02_zielsetzung` und `03_vorgehen` bewusst unangetastet, da nahe der Halbseiten-Schwelle. Verengungsbegründung und Zeitbezug (SDG 12.3) unverändert. `check_formalia.py` 0 FEHLER.
| `sec:durchfuehrung` | `chapters/02_durchfuehrung/…` | FERTIG | **Befund 2 des Abgabe-Audits behoben (2026-07-30, Überarbeitungs-Session) — über die Empfehlung des Prüfberichts hinaus.** Zuerst nach Variante (a) gefixt („zwei der drei … Zwecke"); auf Nutzer-Rückfrage neu bewertet und verworfen, weil (a) zwar den Widerspruch zum Folgesatz beseitigt, aber die Zähl-Differenz nur umdreht (nach dem Doppelpunkt stehen weiterhin **drei** Glieder) und ausgerechnet an der einzigen Innovationsaussage — sie trägt Kreativität (15 %) allein — eine Defizitansage setzt. Nach `AskUserQuestion` steht in `03_konzept.tex:15` jetzt eine Fassung **ohne Zahl**: „…, wie Resteria den Zero-Waste-Fokus in die von der Aufgabenstellung genannten Zwecke Rezept-Teilen und gegenseitige Inspiration hineinträgt und ihn zugleich in der täglichen Nutzung verankert: …". Die zwei eingelösten Zwecke sind benannt statt gezählt; das dritte Glied (Menüplanung/Reste-Journal) steht unter einer eigenen Ankündigung, statt fälschlich als Zweck zu gelten. Das Schlussglied endete in der beschlossenen Fassung wortgleich wie die neue Rahmung und wurde umformuliert (Dopplung im selben Satz). **Anschließende Faktenprüfung des Absatzes auf Nutzerwunsch: ein Overclaim behoben.** Drei der vier Aussagen sind am eigenen Text belegt (Pflichtangabe der Reste-Zutat `:13`; monatliche Zero-Waste-Woche als Ersatz für Forum/Gruppen `04_community_und_feedback.tex:7` wörtlich; Kopplung an die Rettungspunkte `:13`). **Nicht belegt war der Frequenzanspruch „täglich":** Der Mechanismus ist ein **Wochen**plan (`:13`, `tab:funktionsuebersicht`), die Challenges sind monatlich, und die Erinnerung feuert erst, „wenn längere Zeit keine Zutat eingetragen wurde"; die Arbeit formuliert ihr Ziel in `:9` selbst als „aus einmaliger Nutzung **wiederkehrendes** Verhalten zu machen", und „täglich" kam im ganzen `chapters/` nur an dieser einen Stelle vor. Der Anspruch stand schon vor der Neufassung im Text — sie hatte ihn verdoppelt, nicht eingeführt. Jetzt „in der **wiederkehrenden** Nutzung verankert" und „**bindet die Reste-Verwertung in die Wochenplanung ein**". Nebenbefund: Der dritte Zweck (Kochtipp-Austausch) **ist** eingelöst — `:13` nennt beim Bewertungs- und Kommentarsystem das Austauschen von „Zubereitungstipps"; er steht nur bewusst als übernommenes Format statt als Innovation, womit Variante (c) des Prüfberichts gegenstandslos ist. `check_all.py --kapitel chapters/02_durchfuehrung`: 0 FEHLER, `check_quellentreue.py --datei chapters/`: 20/20 OK, keine Zitation invalidiert. Umfang **3.712 Wörter**, Build danach: Textteil weiter **10 Seiten**, 0 Overfull, 0 Missing character. — *Vorheriger Audit-Stand:* **Abgabe-Audit 2026-07-30: ÜBERARBEITUNG NÖTIG — ein Befund, eine Ein-Wort-Korrektur.** `03_konzept.tex:16` kündigt „die drei von der Aufgabenstellung genannten Zwecke" an, löst aber nur zwei ein (Rezept-Teilen, gegenseitige Inspiration); das dritte Glied betrifft Menüplanung/Reste-Journal, und den Kochtipp-Austausch schlägt der **unmittelbar folgende Satz** ausdrücklich der Gegenkategorie „bewusst übernommen" zu — die Ankündigung widerspricht dem eigenen Folgesatz. Betrifft den Absatz, der das Kriterium Kreativität (15 %) allein trägt. Empfohlener Fix: „zwei der drei von der Aufgabenstellung genannten Zwecke". Siehe `pruefbericht.md` → Befund 2. Kein weiterer Befund in diesem Kapitel: alle Zählangaben nachgezählt, Tabellenwertungen spaltenweise gegen die eigene Kriteriendefinition geprüft, alle Mockup-Aussagen am Bild belegt. — *Vorheriger Stand:* Kürzungsrunde 2026-07-28: 3.507 → 2.690 Wörter (74,0 % Anteil, im Soll), Hauptlast der Runde. `03_konzept.tex` von 1.041 auf ~560 Wörter (lag mit elf Absätzen weit über dem Richtwert 4–8), `01_wettbewerbsanalyse.tex` von 986 auf ~500. `fig:konzeptfluss` gestrichen (nach Freigabe; war ohne `\autoref`-Verweis und kein Lieferobjekt). Alle sechs Plattformen, alle Pflicht-Tabellen, alle Abhakliste-Schlüsselbegriffe und die Event-Kalender-Begründung erhalten. `check_formalia.py` 0 FEHLER, 6/6 BBT-Keys valide. **Nachtrag 2026-07-28:** `03_konzept.tex:3` um einen Begründungssatz zur Freemium-Trennlinie erweitert (+30 Wörter, jetzt ~2.720) — bedient das Bewertungskriterium Transfer („begründe jeweils Deine Wahl", Aufgabenstellung S. 6), da das Bezahlmodell bis dahin als gesetzte Entscheidung ohne Herleitung dastand. `check_formalia.py` 0 FEHLER. Anschließend im selben Absatz die undefinierte „erweiterte Vorratsverwaltung" (kam in Runde 6/6.6 als Ersatz für die gestrichenen exklusiven Challenges hinein, wurde aber nie in Fließtext oder `tab:funktionsuebersicht` durchgezogen) in die Menüplanung eingefaltet: „erweitert die Menüplanung um eine Vorratsübersicht mit Haltbarkeitsdaten und blendet Werbung aus" — definiert zugleich, was „erweitert" heißt (−2 Wörter). Werbung bleibt bewusst Teil des Modells (Gratis-Version werbefinanziert, Bezahlpaket blendet aus); ein Werbeverzicht war nie beschlossen. **Datenbeschaffung ergänzt (2026-07-28):** Nutzer-Einwand, dass der Beta-Test (Woche 15–17, prüft laut `07_iteratives_design.tex:5` das Reste-Matching) einen Rezeptbestand voraussetzt — zutreffend: Der Bestandsaufbau kam in keiner Phase der Inhaltsspalte vor. Jetzt in Phase 3 aufgenommen („Aufbau des Rezeptbestands, Umsetzung von …"), bewusst nicht in Phase 1–2, weil `05_phasenplanung.tex:3` diese als „mit diesem Bericht bereits erbracht" ausweist und der Bestand nicht existiert. Dazu ein Satz in `03_konzept.tex:7`: „Ein anfänglicher Rezeptbestand aus Rezeptkooperationen und lizenzierten Sammlungen ist damit Voraussetzung für den Beta-Test; Nutzerbeiträge erweitern ihn erst danach." — die Reihenfolge ist entscheidend, Nutzerbeiträge allein wären zirkulär. „Rezeptkooperationen" statt „Kooperationen", damit es nicht mit den Erlös-Kooperationen (Food-Sharing, Händler) in Z. 3 verschwimmt. Gegenfinanziert über Straffung des Menüplanungs-Satzes (Z. 13) und der Namensherleitung (Z. 3); in letzterer das Hedge „oft" bewusst wieder eingesetzt, da „-ia" ohne Einschränkung eine zu starke sprachliche Behauptung wäre. Roter Faden anschließend gegengelesen und zwei Anschlussstellen geschlossen: Risiko 2 in `05_phasenplanung.tex:23` um „oder beim Aufbau des Rezeptbestands" erweitert (statt eines dritten Risikos — „Zwei Risiken" bleibt korrekt, und der Zwei-Wochen-Puffer ist als Mitigation eines *Verzögerungs*risikos stimmig), und der Vorgriff auf den Beta-Test in `03_konzept.tex:7` mit `~\autoref{sec:phasenplanung}` verankert. Zuletzt in `06_evaluation_reflexion.tex:7` den Ankündigungssatz „Auch das Vorgehen selbst verdient einen Blick." ersatzlos gestrichen (−7 Wörter): einziger inhaltsleerer Absatzöffner des Abschnitts, während alle vier übrigen Absätze mit einer belastbaren Aussage beginnen; der Folgesatz trägt die Vorgehens-Bewertung samt Begründung bereits allein. Gemessen: **3.677 Wörter ≈ 9,8 S.** (`check_umfang.py`), Session netto +40 Wörter gegenüber 3.637. **Mockup-Lücke gefunden (2026-07-28):** Alle vier Bilder per Read-Tool gesichtet — Wochenplan und Reste-Journal sind in keinem abgebildet, und beide Startseiten-Screens zeigen keinen Einstiegspunkt (Tab-Leiste durchgehend nur Start/Rettungs-Feed/Profil). Damit widerlegt das Bild die Textaussage in `03_konzept.tex:13` („Erreichbar ist der Bereich über die Startseite"). Altlast von Befund 5.7, der 2026-07-24 per Textzusatz statt neuem Bild gelöst wurde. Als **Anpassung 4** in `sources/claude-design-nachbesserung.md` dokumentiert (Nutzer-Schritt: Karte „Wochenplan & Reste-Journal" auf beiden Startseiten-Screens ergänzen, null Wörter, keine fünfte Abbildung); Fallback ohne Design-Runde ebenfalls dort notiert. `\quelle{}`-Zeilen bewusst **nicht** vorbereitet, damit kein nie ausgeführter Prompt zitiert wird. Befund 6.3 vom Nutzer behoben und verifiziert (Plus-Button frei, Lesezeichen sichtbar, Feed ohne „Foto:"-Doppelpunkt); die Platzhalter-Korrektur aus Anpassung 3 steht noch in Startseite, Bookmarks und Profil aus. `05_phasenplanung.tex`, `tab:phasenplanung`: Ressourcenspalte von Phase 3 um „Rezeptdaten aus Kooperation oder Nutzerbeiträgen" ergänzt — die Aufgabenstellung (S. 6) nennt **Daten** als erste zu bedenkende Ressource, die Tabelle führte keine, obwohl das Reste-Matching einen strukturierten Rezeptkorpus voraussetzt. In Phase 1 „Desk-Research" durch „öffentlich zugängliche Plattform- und App-Angebote" ersetzt: unerklärter Anglizismus und zugleich Kategoriefehler (Methode in der Ressourcenspalte, neben „Fachliteratur" nahezu tautologisch); die neue Formulierung deckt sich mit dem Methodensatz in `01_wettbewerbsanalyse.tex:13`. Bewusst NICHT ergänzt: Hinweis in Z. 3, dass Phase 1/2 real schneller liefen als veranschlagt (Reflexion nennt „rund zwei Wochen", Plan veranschlagt vier) — die KI-Unterstützung ist dort bereits als Grund genannt, und der Seitenumfang steht auf der Obergrenze. Zusätzlich Terminologie vereinheitlicht: „Mockup-Skizze" in `03_konzept.tex:34` → „Mockup" (−1 Wort). Ankerbegriff für die vier Abbildungen ist durchgängig **Mockup**; „Skizze" bezeichnet ab jetzt ausschließlich die Persona-Skizze in Anhang A (vorher doppelt belegt). Netto der Session ca. +27 Wörter. Seitenumfang stand vorher exakt bei 10 (Obergrenze) — vor Abgabe neu bauen. **AENDERUNGEN.md Runde 7 abgearbeitet (2026-07-28):** 7.1–7.3 und 7.5 umgesetzt (SEQ- und Think-Aloud-Fundierung des Beta-Tests plus Gruppengrößen-Begründung in `07_iteratives_design.tex:5`, Dublette in `:3` gestrichen, `tab:phasenplanung`-Zeile Beta-Test auf die neuen Verfahren umgestellt), 7.4 auf ausdrückliche Nutzeranweisung übersprungen. `\acro{SEQ}` neu in `pages/acronyms.tex`. `check_formalia.py` 0 FEHLER. **Offener Punkt:** Die drei neu zitierten Quellen (Sauro & Lewis, Nielsen, Erlhofer) haben kein `file`/`url`-Feld — `check_quellentreue.py` markiert sie NICHT PRÜFBAR, siehe „Bitte manuell prüfen" in `AENDERUNGEN.md` Runde 7. Netto +60 Wörter, Gesamtumfang laut `check_umfang.py` jetzt 3.783 Wörter ≈ 10,1 Seiten (Kapitel 2 allein 2.829 W. ≈ 7,5 S.) — vor Abgabe neu bauen, könnte die 10-Seiten-Grenze knapp überschreiten. **Delta-Re-Audit 2026-07-28:** ÜBERARBEITUNG NÖTIG — Befund 3 (unbelegte Autorennennung „Soma et al." in `06_evaluation_reflexion.tex:7`) und Hauptlast der Kürzung um ~100 Wörter, siehe `pruefbericht.md`. **Delta-Re-Audit 2026-07-29:** ÜBERARBEITUNG NÖTIG — Hauptlast der verbleibenden Kürzung (~90 W. aus `06_evaluation_reflexion.tex`) sowie Befund 2 (Randüberstand `01_wettbewerbsanalyse.tex:28`), siehe `pruefbericht.md`. **Überarbeitung nach Prüfbericht abgeschlossen (2026-07-29, diese Session):** Befund 2 behoben — `\quelle{}`-Zeile unter `tab:wettbewerbsvergleich` (`01_wettbewerbsanalyse.tex:29`) von den sechs Domainnamen auf die sechs Plattformnamen ohne TLD umgestellt (Text aus dem Prüfbericht übernommen), behebt den Overfull-\hbox-Randüberstand und spart ~6 Wörter. Befund 1, vier der fünf priorisierten Kürzungsstellen umgesetzt (alle in `06_evaluation_reflexion.tex`, keine berührt These/Kernargumente): zweite „Lehre" in Z. 11 gestrichen („zwei Lehren" → „eine Lehre", erste Lehre bleibt unverändert), Berichtsinhalte-Aufzählung in Z. 3 auf einen Satz ohne Einzel-`\autoref`s verdichtet, Reichweitenrisiko-Satz in Z. 5 gestrichen (Dublette zu Limitation 1, nicht in die Limitation eingefaltet — reine Streichung spart mehr und die Limitationen-Dreischichtung bleibt unberührt), Anschlusssatz in Z. 9 in den Vorsatz gezogen. `check_all.py --kapitel chapters/02_durchfuehrung`: 0 FEHLER. `check_quellentreue.py --datei chapters/`: weiterhin 19/19 OK, keine Regression (keine Zitation inhaltlich verschoben). Gemessen: **3.533 Wörter** (`check_umfang.py`, ≈ 9,4 S. geschätzt) — Kapitel 2 allein 2.597 W. Netto dieser Session: −77 Wörter gegenüber dem Audit-Stand 3.610. Noch **kein lokaler Build** in dieser Session-Umgebung gefahren (Nutzer-Schritt steht aus) — ob die 77 Wörter die elfte Seite tatsächlich kippen, zeigt erst der nächste `latexmk`-Lauf. **AENDERUNGEN.md Runde 8+9 abgearbeitet (2026-07-29, diese Session):** Umfangreichste Überarbeitung seit der Gegenlesung — betrifft `01_wettbewerbsanalyse.tex` (8.1 Auswahlbegründung der sechs Plattformen mit drei Kategorien nach Freigabe, 8.9 Kriteriendefinition um abgeschwächtes Verfolgen von Creator-Inhalten erweitert nach Freigabe, Rezept-Entdeckung-Zelle für leftovercooking begründet), `02_zielgruppe.tex` (Verengungsbegründung auf Rückverweis gekürzt), `03_konzept.tex` (Autoref auf `tab:funktionsuebersicht` ergänzt, „zwei Befunde"-Überdehnung auf einen belegten Nudge-Typ zurückgenommen, Soma-Signifikanzaussage präzisiert, Nivedhitha-Mediator korrigiert, Innovationsabsatz den drei Aufgaben-Zwecken zugeordnet, UI und UX sprachlich getrennt, Event-Kalender-Begründung von „Moderation" auf „Terminpflege" umgestellt), `04_community_und_feedback.tex` (Somas Vergleichsgruppe korrekt als Präsenzveranstaltungen beschrieben plus Übertragungsvorbehalt, zwei fehlende Locatoren ergänzt), `07_iteratives_design.tex` (Selbstwiderspruch „bereits durchlaufen"/„nicht real durchgeführt" aufgelöst, Design-Phase korrekt als laufend statt bevorstehend beschrieben, Erlhofer-Gruppengröße korrekt als eigene Setzung ausgewiesen, SEQ-Schwelle als eigene Festlegung markiert), `05_phasenplanung.tex` (Phase-1-Inhalt um Community/Umsetzungsplanung ergänzt, drittes Risiko zum Kaltstart der Community-Beiträge). Die drei `[FREIGABE]`-Punkte (8.1, 8.9, 8.17) per `AskUserQuestion` geklärt — Details und Wortzahl-Bilanz in `AENDERUNGEN.md` → Runde 9 → „Nächster Schritt". `check_all.py --mit-quellen`: 0 FEHLER in allen sechs Skripten. `check_quellentreue.py`: alle 19 Zitationen erneut gegen die geänderten Trägersätze geprüft, 19/19 OK. Gemessen **3.646 Wörter** (`check_umfang.py`, ≈ 9,7 S. geschätzt), netto +79 Wörter gegenüber dem Stand vor der Runde (3.567 W. ≈ zuletzt real gebaute 10 Seiten) — **vor Abgabe unbedingt neu bauen**, ein Kippen auf 11 Seiten ist nicht ausgeschlossen. **Faktenfehler behoben (2026-07-30, diese Session, nach Web-Faktencheck auf Nutzerwunsch):** „Bundesministerium für Ernährung und Landwirtschaft (BMEL)" war seit dem Regierungswechsel am 6. Mai 2025 nicht mehr aktuell — das Ministerium heißt seither „Bundesministerium für Landwirtschaft, Ernährung und Heimat" (BMLEH), Zuständigkeit „Heimat" kam vom Innenministerium dazu (Web-Quellen: Wikipedia, agrarzeitung.de, die offizielle App-Seite läuft inzwischen unter `bmleh.de`). `pages/acronyms.tex` (`\acro{BMLEH}`, Spaltenbreite `[BMLEH]`) sowie alle vier `\ac{BMEL}`-Stellen in `01_wettbewerbsanalyse.tex` (Z. 7 ×2, Z. 9, Z. 13, Tabellenzelle Z. 26) und je eine in `01_ausgangslage_und_problem.tex:5` und `03_konzept.tex:7` auf `\ac{BMLEH}` umgestellt. Funktionsbehauptung selbst (Reste-Matching ja, Community/Austausch nein) am Volltext der BMLEH-Themenseite bestätigt, keine Änderung nötig. `check_all.py` auf beiden Kapitelordnern: 0 FEHLER. Bewusst nicht rückwirkend korrigiert: die „BMEL"-Erwähnungen in `kapitelplan.md`/`kapitelplan.draft.md`/Sitzungsverlauf — kein Abgabebestandteil. **Faktenfehler in der Wettbewerbsanalyse behoben (2026-07-30, diese Session, Web-Faktencheck aller sechs Plattform-Claims auf Nutzerwunsch):** Fünf der sechs Plattform-Absätze (Chefkoch, TikTok, BMLEH-App, leftovercooking, Foodsharing.de) an unabhängigen Quellen vollständig bestätigt — leftovercooking sogar bis ins Detail (Farbcodierung verderblicher Zutaten, Belohnungspunkte zeigen gesparten Betrag in Euro, Community bleibt auf Creator-Following beschränkt). Ein Fund bei **Restegourmet**: `01_wettbewerbsanalyse.tex:7` behauptete bisher „auch Restegourmets Teilen und Bewerten einzelner Rezepte schafft keinen Ort für Austausch" — in drei unabhängigen Quellen (offizielle App-Store-Beschreibung, Verbraucherzentrale-Test, allgemeine Suche) keine Teilen- oder Bewerten-Funktion für Rezepte in der App gefunden; die im Verbraucherzentrale-Test erwähnte „Bewertung" von KI-Rezepten ist erkennbar die Testmethodik der Redaktion, keine In-App-Funktion. Satz auf „und auch Restegourmets Fokus auf individuelle Rezeptverwaltung (Kochbuch, Wochenplan) schafft keinen Ort für Austausch" umgestellt — Kernaussage (keine echte Social-Community) bleibt unverändert und weiterhin richtig, nur die konkrete Begründung war unbelegt. `check_all.py`: 0 FEHLER.
| `sec:fazit` | `chapters/03_fazit/…` | FERTIG | Kürzungsrunde 2026-07-28: 597 → 426 Wörter (11,7 % Anteil, im Soll). Die vier Verhaltenshebel in `01_kernergebnisse_und_ausblick.tex` von vier Einzelsätzen auf eine belegte Aufzählung verdichtet (narrative `\textcite` dabei korrekt zu `\parencite` gewechselt, da die Namen nicht mehr im Satz stehen). Alle drei Limitationen bleiben dreischichtig (warum · Mitigation · Folgeschritt) — gegengeprüft. `check_formalia.py` 0 FEHLER. **Nachtrag 2026-07-28:** Kooperations-Satz in `01_kernergebnisse_und_ausblick.tex:7` auf einen Rückbezug umgestellt (`\autoref{sec:konzept}` statt erneuter Einführung), da er die Kooperationen aus `03_konzept.tex` wortnah wiederholte und sie zugleich als noch offene Option darstellte, obwohl sie dort fester Teil des Erlösmodells sind. −2 Wörter; „regionale Zero-Waste-Vereine" dabei entfallen (Variante ohne eigenen Begründungswert). Ebenfalls korrigiert in `02_limitationen.tex:5`: Limitation 2 sprach im **Singular** vom „UI/UX-Mockup" (es sind vier) und nannte als Mitigation „die Beschränkung auf die Kernfunktion Reste-Matching" — sachlich überholt, da die Mockups auch Profil, gespeicherte Rezepte und Rettungs-Feed zeigen. Jetzt „die vier UI/UX-Mockups" und „die Beschränkung auf die drei Hauptbereiche der Navigation" (deckungsgleich mit `03_konzept.tex:34`). Dreischichtung (warum · Mitigation · Folgeschritt) bleibt erhalten — die Schicht war beim Voll-Audit vom 24.07. schon einmal als −15-FAIL aufgefallen. **Zwei Formulierungen überarbeitet (2026-07-28):** In `01_kernergebnisse_und_ausblick.tex:5` verknüpfte „…als wissenschaftlich bewiesen darzustellen, **obwohl** er auf eigener Marktbeobachtung beruht" die beiden Hälften konzessiv, obwohl das Verhältnis kausal ist; zudem war „er" doppeldeutig (Marktvorteil oder Bericht) und der Bericht stand als handelnde Instanz da — derselbe Akteurs-Fehler wie in Voll-Audit-Befund 4. Jetzt „Diese Trennung macht kenntlich, dass die Differenzierung auf eigener Marktbeobachtung beruht und deshalb nicht als wissenschaftlich belegt gelten kann." („Differenzierung" statt „Marktvorteil", deckungsgleich mit Z. 3). In `02_limitationen.tex:5` benannte die Mitigation nur zwei Maßnahmen, ohne zu sagen, wogegen sie wirken; jetzt „Der Anspruch bleibt entsprechend begrenzt: … die Mockups zeigen nur die drei Hauptbereiche der Navigation, nicht die vollständige Bedienführung." — bricht zugleich den dreifach wiederholten Öffner „Abgemildert wird das…" (kosmetischer Hinweis aus dem Delta-Re-Audit vom 26.07.). Mitigationsschicht bleibt eindeutig erkennbar. `check_formalia.py` 0 FEHLER. **Delta-Re-Audit 2026-07-29:** ÜBERARBEITUNG NÖTIG — Kürzungskandidat 4 (Kontrastfigur in `02_limitationen.tex:3`, ~10 W.), siehe `pruefbericht.md` Befund 1. **Überarbeitung nach Prüfbericht abgeschlossen (2026-07-29, diese Session):** Kontrastfigur „Betroffen ist nicht das Vorgehen, sondern die Geltung der Ergebnisse." gestrichen — der Folgesatz („Abgemildert wird die Grenze dadurch, dass …") trägt die Mitigation weiterhin allein, Dreischichtung (warum · Mitigation · Folgeschritt) bleibt in allen drei Limitationen erhalten (nachgezählt). `check_all.py`: 0 FEHLER. **AENDERUNGEN.md Runde 8+9 abgearbeitet (2026-07-29, diese Session):** In `01_kernergebnisse_und_ausblick.tex` die dritte Wiederholung der Lückenaussage gestrichen (8.11), Drivers/Levers-Zuschreibung entschärft (9.9), Soma-Wirksamkeit auf „erste Hinweise" zurückgenommen (9.3), Nivedhitha-Mediator auf „gemeinsame Handlungsabsicht" korrigiert (9.5) — die tragende Trennung von Marktbeobachtung und wissenschaftlichem Beleg am Kapitelende blieb unangetastet. In `02_limitationen.tex` nur ein Locator ergänzt (9.6), keine inhaltliche Änderung, Dreischichtung erneut bestätigt unberührt.
| Anhang C (Style Tile) | `pages/appendix.tex` · `images/durchfuehrung/resteria_styletile.png` | FERTIG | **Mockup-Iteration abgeschlossen (2026-07-28):** Wochenplan-Banner auf beiden Startseiten (Anpassung 4) und „Foto"-Platzhalter in allen vier Screens vom Nutzer umgesetzt. Alle vier `\quelle{}`-Zeilen tragen jetzt die Folge-Prompts; der Foto-Prompt wurde sinngemäß nach dem Wortlaut aus `claude-design-nachbesserung.md` (Anpassung 3) ergänzt — vor Abgabe gegen die tatsächlich eingegebene Formulierung abgleichen. Daraufhin `07_iteratives_design.tex:3` von „Eine erste Rückkopplungsschleife" auf „Mehrere Rückkopplungsschleifen" umgestellt und die zwei Auslösertypen unterschieden (Abgleich gegen die Konzeptbeschreibung vs. Sichtung der Screens selbst) — stärkt Teilaufgabe 3 und das Kriterium Prozess; `06_evaluation_reflexion.tex:9` zur Konsistenz auf „Mehrere Nachbesserungsrunden" angeglichen (±0 Wörter). Neu 2026-07-28 auf Nutzerwunsch. `\section*{Anhang~C: Style Tile Resteria}` mit `fig:styletile` (Farbpalette inkl. oklch-Werten, Typografie Lora/Work Sans, Layout-Maße, Icons, Komponenten). Im Fließtext einmal erwähnt in `03_konzept.tex:34`. **Zweite einmalige `main.tex`-Freigabe** des Nutzers (nach dem 23.07.): Zeile „Anhang C: & Style Tile Resteria" in die Anhangsverzeichnis-Tabelle (`main.tex:339`) ergänzt — ohne sie wäre der Anhang unvollständig ausgewiesen. `\quelle{}` zitiert den Nutzer-Prompt wortgetreu inkl. grammatischer Unebenheit („für das Projekt erstellen"), da Prompt-Angaben Zitate der Eingabe sind. Zusatz „Abbildung nachträglich manuell bearbeitet." bewusst **nicht** gesetzt — anders als bei den vier Mockups ist keine manuelle Nachbearbeitung bekannt; vor Abgabe zu prüfen. |
| Prüf-Audit | `pruefbericht.md` | FERTIG | **Delta-Re-Audit 2026-07-30 (letzter Lauf des Tages): Score 100/100 — abgabereif.** Geprüft: die vier Befunde des Abgabe-Audits (72/100), der vollständige Textdiff `cf9a579..f5fa3a4` (zwei Kapiteldateien, `pages/meta.tex`, `references.bib`), alle sechs Skripte, ein Neubau (`latexmk -g -lualatex`, Exit 0) und das gebaute PDF. **Alle vier Befunde am PDF verifiziert behoben, keine Regression, kein neuer Befund, keine Handlungsempfehlung.** Belege je Befund: Titel trägt U+2013 (Zeichencode am extrahierten Text geprüft, nicht am Augenschein — Titelblatt und PDF-Metadaten) · `03_konzept.tex:15` macht keine Zählaussage mehr, die zwei eingelösten Zwecke sind benannt statt gezählt, der Widerspruch zum Folgesatz ist weg · Verzeichnis rendert „Artikel 907" und „Artikel e13038" formgleich mit Barker und Shen, kein Doppeldruck · Erlhofer „(2018). Website-Konzeption und Relaunch." und In-Text „Erlhofer und Brenner (2018, Kap. 17.2.2)" auf PDF-S. 12. **Build:** 0 `!`-Fehler, 0 Overfull \hbox, 0 Missing character, 0 undefinierte Referenzen, 23 PDF-Seiten, **Textteil 10 von 7–10** (gedruckt 1–10 = PDF-S. 6–15). `check_all.py --mit-quellen` ohne harte Funde, **Teil-Check G 20/20 FUNDSTELLE OK ohne Invalidierung durch den Delta**, `check_bib_hygiene.py` 0 Hinweise. Zwei Vorbericht-Hinweise haben sich mit erledigt: der `DOPPELBELEGUNG`-Fund (Startseite beschriftete zwei Objekte) und die Schwächung des Kriteriums Kreativität durch Befund 2. Einziger kosmetischer Rest ohne Handlungsbedarf: `03_konzept.tex:15` lässt sich streng gelesen als Aufzählung aller drei Aufgaben-Zwecke missverstehen — im Absatz auflösbar (Folgesatz plus `tab:funktionsuebersicht`, Zeile Kochtipp-Austausch), eine erneute Zahlangabe wäre der Rückschritt, den die Überarbeitung bewusst vermieden hat. — *Vorheriger Stand:* **Überarbeitung 2026-07-30 abgeschlossen (Nachtrag am Ende von `pruefbericht.md`):** Befund 1 und 2 umgesetzt und am neu gebauten PDF verifiziert (Textteil weiter 10 S., 0 Overfull, 0 Missing character), Befund 4 am DNB-Katalogisat sachlich entschieden (Titel ohne Untertitel, Jahr 2018) — offen bleiben nur noch Befund 3 und 4 als Zotero-Eingaben, danach Delta-Re-Audit. — *Audit-Stand:* **Abgabe-Audit 2026-07-30: Score 72/100 — Überarbeitung empfohlen.** Erster Lauf mit vollständigem Teil-Check F und mit `pages/meta.tex` außerhalb der Skript-Ausnahmeliste. Alle vier Befunde sind mechanisch, keiner betrifft These, Argumentation, Aufbau oder eine Pflichtregel: **(1)** Geviertstrich „—" im `\PaperTitle` (`pages/meta.tex:7`) — steht auf dem Titelblatt und in den PDF-Metadaten, `check_formalia.py` überspringt die Datei als Vorlagen-Gerüst (−8). **(2)** „drei Zwecke" angekündigt, zwei eingelöst (`03_konzept.tex:16`, −10). **(3)** Artikelnummer als Seitenzahl bei `somaFoodWasteReduction2020` (`pages={907}`) und `nivedhithaHowGreenSocial2024` (`pages={e13038}`) — beide gehören als `eid`; Barker und Shen derselben Klasse rendern korrekt „Artikel N", die zwei nicht. `check_bib_hygiene.py` erkennt Artikelnummern erst ab fünf Ziffern (−5). **(4)** `erlhoferWebsiteKonzeptionUndRelaunch2017` weicht vom Volltext ab: Der Untertitel „: Das Handbuch für die Praxis" steht weder auf dem Cover noch auf der Titelei noch im OPF-Titel, und das Impressum nennt „1. Auflage 2018" gegen `date = {2017}` (−5). **Sauber:** Build 0 Fehler / 0 Overfull / 0 Missing character, **Textteil exakt 10 Seiten von 7–10** am gebauten PDF gemessen, **Teil-Check G 20/20 FUNDSTELLE OK** (Soma S. 10–12, Erlhofer Kap. 17.2.2, Nielsen Kap. 6.8 und Sauro & Lewis Kap. 8 in diesem Lauf neu am Volltext verifiziert), Teil-Check H ohne Befund, Sprachstichprobe ohne Fehler, alle sechs Lieferobjekte am **Bild** geprüft. **Tooling-Korrektur (kein Textbefund):** Der gespeicherte Seitenversatz für `nivedhithaHowGreenSocial2024` war +9, das Skript prüfte deshalb das Literaturverzeichnis der Quelle statt S. 14; auf 0 gesetzt und am PDF verifiziert (gedruckt S. 14 = PDF-Seite 14). Erwarteter Score nach Umsetzung: 100/100, danach genügt ein Delta-Re-Audit. — *Vorheriger Stand:* **Überholt durch Runde 8+9 (2026-07-29):** Der Score 100/100 unten bezieht sich auf den Textstand vor der Gegenlesung/Zitatkontext-Prüfung (Runde 8+9) — seither wurden 12 Kapiteldateien geändert (siehe `sec:durchfuehrung`/`sec:fazit`). `pruefbericht.md` selbst bleibt unverändert als Momentaufnahme des alten Stands. Nächster Schritt: lokaler Build + Seitenzahl, dann neues Abgabe-Audit. — *Vorheriger Stand:* **Delta-Re-Audit 2026-07-29 (2. Lauf desselben Tages): Score 100/100 — abgabereif.** Geprüft: die drei Befunde des Vorberichts (77/100), der vollständige Textdiff `62e19ed..3ad42bb`, alle sechs Skripte, ein Neubau (`latexmk -g -lualatex`, Exit 0) und das PDF Seite für Seite. Alle drei Befunde am gebauten PDF verifiziert behoben, kein neuer Befund, keine Regression durch die fünf Streichungen (Limitationen weiter dreischichtig, „zwei Lehren" → „eine Lehre" korrekt mitgezogen, alle Anhänge trotz entfallener `\autoref`s weiter im Text verankert, keine Datei unter der Halbseiten-Schwelle). **Textteil 10 Seiten von 7–10** (gedruckt 1–10, Literaturverzeichnis ab 11), 0 Overfull \hbox, 0 Missing character, 0 `!`-Fehler, 23 PDF-Seiten. Teil-Check G 19/19 OK. **Eine Prüfnotiz korrigiert (kein Textbefund):** Der Urteilseintrag `d7fd6058a1` zu `01_ausgangslage_und_problem.tex:5` trug die Begründung der beiden anderen Vittuari-Stellen (Drivers/Levers) statt der des Haushalts-Claims; Urteil am Volltext bestätigt (S. 105 trägt beides), Notiz ersetzt — Fall P2-3, aber Dublette *innerhalb* eines Bib-Keys, die der dort vorgeschlagene Wächter nicht gefangen hätte. **Score-Einordnung:** 100/100 = alle im Delta-Umfang geprüften Kriterien; Teil-Check F lief nicht als Vollprüfung, die Sentence-Case-Klasse des Literaturverzeichnisses wartet mit −5 auf das Abgabe-Audit. **Nächster Schritt:** Zotero-Arbeitsliste (`check_bib_hygiene.py`) abarbeiten, neu exportieren, einmal kompilieren und die Seitenzahl gegenprüfen — der Textteil steht exakt auf der Obergrenze. Details und Nutzer-Checkliste in `pruefbericht.md`. — *Vorheriger Stand:* **Alle drei Befunde des Delta-Re-Audits vom 29.07. (Score 77/100) jetzt am gebauten PDF verifiziert behoben.** Befund 1 (Seitenumfang 11 statt 7–10): Textteil jetzt **exakt 10 Seiten** (Kapitel 1 auf gedruckter S. 1, Ende Limitation 3 auf S. 10, Literaturverzeichnis ab S. 11) — Kürzungsrunde plus Vittuari-Fix reichten aus, keine weitere Rückfrage zur Wettbewerbsanalyse nötig. Befund 2 (Randüberstand, Overfull \hbox 11,5 pt): **war falsch verortet** im Vorbericht (dort der `\quelle{}`-Zeile zugeschrieben) — tatsächliche Ursache die Tabellenkopfzelle „Nachhaltigkeits-/Restefokus" (`01_wettbewerbsanalyse.tex:19`): Schrägstrich ist kein TeX-Umbruchpunkt, und schon das erste Silbenfragment „Nachhaltigkeits-" war zu breit für die schmale Spalte. Fix: manuelle Trennstellen `Nach\-hal\-tig\-keits-/\allowbreak Restefokus`. `grep -c "Overfull" build/main.log` → 0. Befund 3 (U+2010 im Nivedhitha-Titel): über Zotero behoben (Titel neu getippt, BBT-Export), `grep -c "Missing character" build/main.log` → 0 (vorher 4). **Build sauber:** 0 Fehler, 0 Overfull, 0 Missing character, 23 PDF-Seiten gesamt. `check_all.py --mit-quellen`: 0 FEHLER. **Nächster Schritt:** Delta-Re-Audit („Prüf-Modus") zur formalen Score-Bestätigung — inhaltlich sind alle drei Befunde bereits verifiziert erledigt, erwarteter Score 100/100. `pruefbericht.md` selbst bleibt unverändert als Momentaufnahme vor dieser Session stehen. |

**Update-Regel:** Eine Komponente = eine Zeile, bestehende Zeile überschreiben, nie anhängen. Identifikator = `sec:<slug>` (in Plan-Modus Schritt 2.5 festgelegt) — hält Tabelle, Dateinamen und LaTeX-Labels synchron. Platzhalter-Zeilen spätestens bei Schritt 2.5 durch echte Einträge ersetzen; aus dem Plan entfernte Komponenten löschen.

**Aktuelle Richtung (2026-07-30)**: **Seitenüberlauf war ein Layout-, kein Textmengenproblem — behoben ohne jede Kürzung; anschließend Lesefluss-Runde.** Nutzer meldete 11 Seiten nach Runde 8+9. Vor der naheliegenden fünften Kürzungsrunde zuerst den Weißraum je Seite am gebauten PDF gemessen: gedruckt S. 8 trug **389 pt**, S. 5 **136 pt** Leerraum am Fuß — zusammen mehr, als die elfte Seite überhaupt an Text trug (462 pt). Ursache: `\begin{table}[H]` bei `tab:funktionsuebersicht` und `tab:phasenplanung`; passt eine `[H]`-Tabelle unten nicht mehr hin, bricht LaTeX um und lässt den Rest der Seite leer. Beide auf `[htbp]` umgestellt → **Textteil 10 Seiten**, beide Tabellen stehen weiterhin bei ihrem `\autoref`-Satz (Tab. 2 oben auf S. 6 direkt nach der Ankündigung am Fuß von S. 5, Tab. 3 unverändert im Fließtext auf S. 8). `\begin{table}[H]` bei `tab:wettbewerbsvergleich` bewusst gelassen — sie verursacht keinen Weißraum. Als **P15** in `SKILL-ANPASSUNGEN.md` dokumentiert: Weißraum-Diagnose gehört in Teil-Check D **vor** jede Kürzungsempfehlung. Vier Kürzungsrunden mit über 1.000 entfernten Wörtern liefen ohne diese Messung.

Danach auf Nutzerwunsch (Rückmeldung „manche Sätze lesen sich zu gekürzt") **Lesefluss-Durchgang über alle 12 Subsections**. Zwei Funde waren keine Stilfrage, sondern durch Kürzungen gebrochene Bezüge: `03_vorgehen_und_aufbau.tex:5` begann mit „**Es** beginnt mit der Wettbewerbsanalyse" — das Bezugswort „Kapitel 2" war in Runde 4 mitgekürzt worden (jetzt ausgeschrieben); `04_community_und_feedback.tex:7` sagte „**Beide** setzen wie dort auf moderierte Gruppendiskussion" unmittelbar nach einer Dreier-Aufzählung von Studiengruppen, gemeint waren Forum und Nutzergruppen aus zwei Sätzen davor (jetzt benannt, Präsenzgruppe als Vergleichsarm explizit). Dazu `06_evaluation_reflexion.tex:9`: „Daraus lässt sich eine Lehre ziehen" zeigte auf den falschen Absatz, seit die zweite Lehre gestrichen wurde → „Aus der zweiten dieser Kursänderungen". `02_zielgruppe.tex:3` (öffnete mit zwei kahlen Verweissätzen) um einen Ansagesatz ergänzt. Drei Blöcke waren nicht zu knapp, sondern zu dicht — je ein Absatzumbruch ohne Wortkosten: `01_wettbewerbsanalyse.tex:13` vor der Kriteriendefinition, `07_iteratives_design.tex:5` vor den Fragebögen, und der allein hängende Einzelsatz in `02_zielsetzung_und_verengung.tex` an den Vorabsatz angeschlossen. Netto **+25 Wörter** (3.646 → 3.671); Build danach: **Textteil weiter 10 Seiten**, 0 Overfull \hbox, 0 Missing character, Exit 0, 23 PDF-Seiten. Verbleibende Reserve auf der letzten Textseite nur noch **37 pt ≈ 2 Zeilen** — weitere Ergänzungen brauchen eine Gegenfinanzierung.

`check_all.py --mit-quellen`: 0 FEHLER, Teil-Check G 19/19 OK. Dabei ein Skript-Blindfleck gefunden und als **P16** notiert: Der Wechsel `[H]` → `[htbp]` setzte die drei Zitationen der Tabellenzelle `03_konzept.tex:22` von OK auf PRÜFEN zurück, weil die `\begin{table}`-Zeile in den Trägersatz-Hash eingeht — kein Zeichen zitierenden Textes hatte sich geändert. Durch Rückbau auf `[H]` verifiziert (sofort wieder 7/7 OK) und neu gebucht, mit je eigener Notiz pro Bib-Key (Lehre aus P2-3).

**Zitat-Rentabilität geprüft (2026-07-30, selbe Session, auf Nutzerfrage).** Alle in Runde 9 geänderten Trägersätze daraufhin durchgesehen, ob der Beleg den Satz *nach* der Änderung noch in seiner Stärke trägt. Ein Fund: `04_community_und_feedback.tex:7` begann mit „Die Entscheidung **stützt sich auf** einen Befund von Soma" und räumte zwei Sätze später selbst ein, dass die Übertragung von Präsenz- auf Online-Formate „eine Annahme dieser Arbeit" bleibt — Runde 9 hatte den Übertragungsvorbehalt korrekt ergänzt, die Rahmung aber nicht mitgezogen. Nach `AskUserQuestion` umgestellt auf „**Den Anstoß dazu gab** ein Befund von …" (−1 Wort). Das Zitat selbst bleibt nötig, der letzte Satz desselben Absatzes braucht es. Neu quittiert (Hash `b0f816e1af`); Vergleichsarm und Vorbehalt aus Runde 9 unangetastet. Alle übrigen 18 Zitationen tragen ihren Satz unverändert. Sekundärbefund ohne Änderung: Die Zelle `03_konzept.tex:22` trägt drei `\parencite` mit denselben Locatoren wie der Fließtext sechs bis acht Zeilen darüber, während alle anderen Zeilen derselben Spalte per `\autoref` oder auf die Aufgabenstellung verweisen — inkonsistent, aber nicht falsch. Build danach: **Textteil weiter 10 Seiten**, 0 Overfull, `check_quellentreue.py` 19/19 OK.

Zwei weitere Punkte in `SKILL-ANPASSUNGEN.md`: **P17** (Kürzen ist ein blinder Arbeitsschritt — kein Skill besitzt ihn, vier Runden liefen freihändig, und die drei gebrochenen Bezüge haben zwei Delta-Re-Audits mit 100/100 überlebt, weil geprüft wird, was der Diff zeigt, während der Schaden im Satz davor entsteht) und **P18** (nach einer Trägersatz-Änderung fehlt die Frage, ob sich das Zitat noch rentiert; Verdikt-Status `RENTIERT NICHT`, Vorfassung im Bericht, Dubletten-Report).

**Kürzungsfolgen systematisch gesucht (2026-07-30, selbe Session).** Statt eines weiteren Lesedurchgangs den **Diff gegen den Textstand vor der ersten Kürzungsrunde** (`296761d`, 24.07.) wortweise durchgesehen — rund 1.000 gestrichene Wörter, jede Streichstelle einzeln. Sechs Befunde behoben, zwei davon in derselben Klasse wie die drei vom Vormittag: **(1)** `01_kernergebnisse_und_ausblick.tex:3` — „Resteria verbindet **beide Seiten**" im Eröffnungsabsatz des Fazits war unauflösbar, seit die zwei Sätze gestrichen wurden, die die Seiten einführten → „verbindet Matching und Community". **(2)** `01_wettbewerbsanalyse.tex:13` — „Letztere beiden sind für die Marktlücke einschlägiger, weshalb dort **mehrere Vertreter** stehen" widersprach der Klammer zwei Wörter davor (die Weitergabe-Kategorie hat genau einen Vertreter) → „die Reste-Matching-Apps sind … am einschlägigsten, weshalb dort drei Vertreter stehen"; deckt sich jetzt mit „ein bis drei Vertretern" in `02_limitationen.tex:7`. **(3)** `01_ausgangslage_und_problem.tex:3` — „etabliert" zweimal in zwei Sätzen (die Kürzung ersetzte „stark gewachsen" durch „fest etabliert" und übersah die Dublette) → „gehören heute zum Alltag". **(4)** `03_konzept.tex:3` — die Namensherleitung stand ohne Pointe da, seit der Satz gestrichen wurde, der aus der Endung etwas machte; Halbsatz zurückgeholt („Resteria ist damit als Ort gedacht, nicht als bloßes Suchwerkzeug"). **(5)** `01_wettbewerbsanalyse.tex:34` — „**Der gewichtigste** Einwand" verwies auf drei leichtere, die es nicht mehr gibt → „Ein gewichtiger Einwand". **(6)** `05_phasenplanung.tex:21` — der Budget-Satz stand als Fremdkörper mit unmotiviertem „Nur" in der Abhängigkeiten-Passage → „Ressourcenseitig fällt allein die MVP-Entwicklung aus dem Rahmen: …". Bewusst gelassen: das fehlende Scharnier in `06_evaluation_reflexion.tex:5` und der Kollektivsingular in `02_zielgruppe.tex:5` (zulässiges Deutsch, nur hölzern). Netto **+17 Wörter** (3.671 → 3.688); Build: **Textteil weiter 10 Seiten**, 0 Overfull, 0 Missing character, freier Raum auf der letzten Textseite unverändert 37 pt. `check_all.py --mit-quellen`: alle sechs Skripte ohne harte Funde, Teil-Check G 19/19 OK.

Nebenbei den seit dem 29.07. bekannten Nebenbefund behoben: `kapitelplan.md:7` trug unter „Gesamtwortzahl (Richtwert)" eine **Seiten**angabe, weshalb `check_umfang.py` den Zielabgleich stillschweigend übersprang. Jetzt „2625–3750 Wörter · **Seitenvorgabe**: 7–10 Seiten" — der Abgleich läuft wieder (3.688 von 2.625–3.750).

**Platzierung von `tab:funktionsuebersicht` repariert (2026-07-30, selbe Session).** Nutzer-Rückmeldung, die Tabelle stehe „random im Abschnitt" — am gerenderten PDF bestätigt: Seite 5 endete mitten im Satz („… die zeitlich begrenzten Challenges ersetzen Forum"), Seite 6 brachte erst die Tabelle und dann die Fortsetzung („und Gruppen als Format …"). Der `[htbp]`-Float war an den Seitenkopf gerutscht und zerschnitt den Innovationsabsatz. Vier Varianten gebaut und vermessen: **(a)** Deklaration früher — wirkungslos, Seite 5 hat nur 47 pt frei, die Tabelle ist ~345 pt hoch; **(b)** `[b]` — Tabelle wandert bis auf Seite 10 und kollidiert dort mit `tab:phasenplanung`, verworfen; **(c)** `\clubpenalties 3 10000 10000 0` vor dem Absatz — greift nicht (erster Versuch scheiterte zusätzlich daran, dass die Rücksetzung ohne Leerzeile davor noch vor dem `\par` stand und damit schon für denselben Absatz galt); **(d) umgesetzt:** Ankündigungssatz `~\autoref{tab:funktionsuebersicht} fasst die Funktionen …` vom Ende des Absatzes „Drei weitere Kernfunktionen" an das Ende des Innovationsabsatzes verschoben und die Tabelle direkt dahinter gesetzt, zurück auf `[H]`. Ergebnis am PDF geprüft: Seite 5 endet mit einer normalen Absatzfortsetzung, Seite 6 beginnt mit den letzten zwei Zeilen des Innovationsabsatzes, danach der Ankündigungssatz, **unmittelbar darunter die Tabelle**, danach UI/UX. Nichts wird mehr zerschnitten, Ankündigung und Tabelle stehen zusammen. Textteil weiter **10 Seiten**, 0 Overfull, kein Wort geändert (nur ein Satz verschoben). Inhaltlich ist die neue Position sogar stimmiger: Der Verweis schließt jetzt die gesamte Funktionsdiskussion ab statt nur die Aufzählung der drei Aufgaben-Funktionen.

Dabei erneut die drei Zitationen der Tabellenzelle invalidiert (jetzt `03_konzept.tex:24`). Zellentext maschinell als identisch zur Vorfassung verifiziert, mit je eigener Notiz neu gebucht — **19/19 OK**. Gegenprobe ergänzt und in **P16** nachgetragen: Eine reine Zeilenverschiebung lässt die Hashes unverändert, Auslöser ist eindeutig das optionale Float-Argument.

**Anhang umgestellt: Style Tile jetzt B, Mockups C (2026-07-30, selbe Session, nach einmaliger Nutzer-Freigabe für `main.tex`).** Begründung: In der Designlogik definiert der Style Tile die visuelle Sprache, aus der die vier Screens abgeleitet sind — wer ihn zuerst sieht, liest die Mockups informierter. Vollständig umgesetzt, damit der Anhang weiterhin der Reihenfolge der Ersterwähnung folgt (sonst führt der Verweis eine Nummer daneben): **(1)** `03_konzept.tex:34` — die beiden Schlusssätze getauscht, der Style-Tile-Satz steht jetzt vor den Mockups, angeschlossen mit „Darauf aufbauend liegen für die drei Bereiche Mockups in Anhang C vor: …" (+2 Wörter); **(2)** `pages/appendix.tex` — Abschnitte getauscht, Buchstaben und `\label{sec:anhang_b/c}` mitgezogen, sodass Label und Buchstabe wieder übereinstimmen (die Labels werden nirgends referenziert, geprüft); **(3)** `main.tex:338--339` — Zeilen des Anhangsverzeichnisses getauscht (**dritte einmalige `main.tex`-Freigabe**, nach dem 23.07. und dem Style-Tile-Eintrag); **(4)** `02_limitationen.tex:5` — „vier UI/UX-Mockups in Anhang B" → „Anhang C". Am gebauten PDF verifiziert: Abbildungsverzeichnis listet jetzt 1 Style Tile, 2–5 Mockups; Anhangsverzeichnis A/B/C stimmt; die Verweise im Text lösen auf „Anhang B, Abbildung 1" bzw. „Anhang C … Abbildung 2–5" auf; 0 undefinierte Referenzen. Textteil unverändert **10 Seiten**, 0 Overfull, 0 Missing character. `check_all.py --mit-quellen`: alle sechs Skripte ohne harte Funde, 19/19 OK.

**Persona-Avatar eingebaut (2026-07-30, selbe Session, nach Vorbild der Vorarbeit).** Anlass: Nutzerfrage, ob das Avatar-Muster aus `sources/vorherige-projektarbeit/chapters/02_planung/02c_personas.tex` (drei Pexels-Portraits in der Persona-Tabelle) hier übernommen werden soll. Dafür sprach `aufgabe.md:55` — das Lieferobjekt heißt dort „Zielgruppen-/Persona-Beschreibung — **Text/Abbildung**", und die Persona war reiner Tabellentext; außerdem liegt sie in Anhang A und kostet damit keine der zehn Textseiten. Nutzer-Auswahl: [Foto 7994388](https://www.pexels.com/photo/a-woman-holding-a-basket-of-fresh-fruits-and-vegetables-7994388/) von **Dziana Hasanbekava**, heruntergeladen als `images/durchfuehrung/persona_mara.jpg` (229 KB, 1200×1797), unverändert eingebunden mit `width=3cm` als erste Zeile der Beschreibungsspalte von `tab:persona`. Pexels-Lizenz am Original geprüft: Attribution nicht erforderlich, Bearbeitung erlaubt, verboten sind herabsetzende Kontexte, Verkauf unveränderter Kopien, suggerierte Empfehlung durch die abgebildete Person, Weiterverbreitung über andere Stock-Plattformen — keiner dieser Fälle liegt vor. Zitiert wird trotzdem, weil der Zitierleitfaden das unabhängig von der Lizenz verlangt.

**Bewusst abweichend von der Vorarbeit**: Dort erzeugte jedes Foto ein eigenes `\captionof{figure}` (Abb. 6–8). Hier steht die Quelle stattdessen in der `\quelle{}`-Zeile der Tabelle — das vermeidet eine sechste Abbildung, eine Neunummerierung der gerade eingerichteten Reihenfolge (Style Tile 1, Mockups 2–5) und eine Bildunterschrift innerhalb einer bereits beschrifteten Tabelle. **Offen (Nutzer-Schritt):** Die Quellenzeile trägt derzeit die ausgeschriebene Form „Portraitfoto: Dziana Hasanbekava auf Pexels (Pexels-Lizenz)." statt eines Kurzbelegs — ein `\textcite` wäre bis zum Zotero-Eintrag eine undefinierte Zitation und würde den Build brechen. Nach Anlage und BBT-Export ist es eine Einzeiler-Umstellung. Nebenbefund: Die Pexels-Einträge der Vorarbeit stehen im abgegebenen PDF, fehlen aber in deren heutiger `references.bib` — derselbe Export-Verlust wie hier am 29.07., deshalb Citekey pinnen. Build danach: Textteil unverändert **10 Seiten**, 23 PDF-Seiten, 0 Overfull, 0 Missing character; `check_all.py --mit-quellen` ohne harte Funde.

**Zotero-Eintrag geprüft, Kurzbeleg gesetzt, Bild vergrößert, Mockup-Reihenfolge getauscht (2026-07-30, selbe Session).** `@artwork{pexelsHasanbekavaBasket}` ist importiert; die Quellenzeile von `tab:persona` läuft jetzt über `\citeauthor`/`\citeyear` und rendert „Quelle: Eigene Darstellung. Portraitfoto: Hasanbekava, n. d. Pexels-Lizenz." — genau das Muster des Zitierleitfadens für Datenbank-Bilder (Beispiel S. 24: „Quelle: kreatikar, 2018. Pixabay-Inhaltslizenz."). Foto von `width=3cm` auf `4.5cm` vergrößert. `fig:mockup_feed` und `fig:mockup_profil` in `pages/appendix.tex` getauscht (Rettungs-Feed jetzt Abb. 4, Profil Abb. 5), Aufzählung in `03_konzept.tex:34` mitgezogen, damit die Abbildungsnummern aufsteigend bleiben, und `main.tex:339` (Anhangsverzeichnis-Klammer) angeglichen — **vierte einmalige `main.tex`-Freigabe**.

**Leitfaden-Prüfung am Original (Kap. 2.2.5, S. 19–24, und 2.3.6/2.3.7, S. 34–35), vier Punkte davon offen als Zotero-Schritte:** Korrekt ist, dass beim Foto **kein** Erstellungs-Präfix steht („Bei Fotos, Cliparts und Bildern aus einer Online-Datenbank wird dieser Hinweis nicht angebracht"), dass die Lizenz genannt wird (verpflichtend, „auch in solchen Fällen", selbst wenn der Anbieter keinen Bildnachweis verlangt), dass der Volleintrag im Literaturverzeichnis steht („Quellen aus dem Anhang werden ebenfalls im Literaturverzeichnis aufgeführt") und dass „n. d." erscheint (S. 35: „Falls nicht vorhanden, wird „n.d." angegeben" — Pexels weist kein Datum aus, EXIF ebenfalls nicht). **Offen:** (1) Plattform „Pexels" fehlt im Literaturverzeichnis — der Leitfaden nennt sie im Muster für Plattform-Medien; per Testbuild mit vier Feldvarianten ermittelt, dass **nur `organization` von `style=apa` gedruckt wird** (`publisher` und `howpublished` nicht), in Zotero also `tex.organization: Pexels` ins Extra-Feld; (2) `urldate` leeren (S. 35: „Das Abrufdatum wird nicht hinzugefügt." — die Vorarbeit hatte es fälschlich drin); (3) Titel in **Sentence Case** („A woman holding a basket of fresh fruits and vegetables [Foto]"), da englischer Titel; (4) Sprachfeld `en`. Build danach: Textteil 10 Seiten, 0 Overfull, 0 Missing character, `check_all.py --mit-quellen` ohne harte Funde (die zwei offenen Punkte melden `check_bib_hygiene.py` als HINWEIS).

**Bib-Eintrag nach Zotero-Korrektur geprüft, UI-Satz nachgezogen, `[Foto]` am Leitfaden bestätigt (2026-07-30, selbe Session).** `references.bib` → `pexelsHasanbekavaBasket` trägt jetzt Sentence Case, `langid = english`, `organization = {Pexels}` und **kein** `urldate`; `check_bib_hygiene.py`: 0 Hinweise (vorher 2). Am gebauten PDF verifiziert, dass das Literaturverzeichnis exakt dem Leitfaden-Muster für Datenbank-Bilder folgt (S. 36: „Name, Vorname (abgekürzt) (Jahr). Titel [Art des Bildes, z. B. Foto, Illustration, Gemälde]. Name der Datenbank. URL"): „Hasanbekava, D. (n. d.). A woman holding a basket of fresh fruits and vegetables [Foto]. Pexels. https://…". **Der Zusatz `[Foto]` ist damit nicht optional, sondern das vom Muster verlangte Element „Art des Bildes".** Zusätzlich den UI-Satz in `03_konzept.tex:34` umgestellt („Startseite, **Rettungs-Feed**, Profilbereich"), damit die Reihenfolge der drei Hauptbereiche mit der neuen Anhang-Reihenfolge übereinstimmt. Textteil unverändert 10 Seiten, 0 Overfull, 0 Missing character.

**Skript-Blindfleck endgültig geklärt — P16 war falsch diagnostiziert, Korrektur als P19.** `SKILL-ANPASSUNGEN.md` war vom Nutzer geleert worden (P1–P18 übernommen); die Datei enthält jetzt nur noch diesen einen Punkt. Ursache der wiederholten Invalidierung der drei Zellen-Zitationen in `tab:funktionsuebersicht` ist **nicht** die Float-Option, sondern `satz_um()`: In einer Tabellenzelle gibt es keinen Satzschlusspunkt, weshalb der Trägersatz vom letzten Punkt **vor** der Tabelle bis zum ersten Punkt **danach** spannt — gemessen 1530 Zeichen, bis in den Folgeabsatz hinein. Deshalb fielen dieselben drei Urteile am 30.07. dreimal: bei `[H]`→`[htbp]`, beim Verschieben der Tabelle und beim Umstellen des Folgesatzes — jedes Mal ohne eine Änderung am zitierenden Text (Zellentext jedes Mal maschinell als identisch verifiziert). Neun Neubuchungen an einem Tag. Vorgeschlagener Fix: Trägersatz bei Zitationen innerhalb `tabular`/`tabularx` auf die **Zelle** begrenzen (Grenzen `&` und `\\`); die in P16 vorgeschlagene Filterung von Struktur-Zeilen wird damit überflüssig. **Wer P16 wie beschrieben umgesetzt hat, hat das Problem nicht behoben.**

**Aufräumarbeiten erledigt (2026-07-30, selbe Session).** Zwei Altlasten geschlossen, die das Abgabe-Audit sonst formal blockiert bzw. verfälscht hätten: **(1)** `AENDERUNGEN.md` trug die vollständig abgearbeiteten Runden 6–9 weiterhin unter „## Offen" — `aenderungen-format.md` verlangt aber „‚Offen' ist leer, bevor ein Voll-Audit gefahren wird". Datei nach dem verbindlichen Format umgebaut: „## Offen" steht jetzt leer am Anfang, darunter „## Erledigt" mit allen neun Runden auf einer Ebene (Runden 1–5 von `##` auf `###` gezogen). Die vollständigen Rundentexte bleiben erhalten — das Format verlangt „Abgearbeitete Punkte wandern ins Erledigt-Log, statt gelöscht zu werden", und die Runden enthalten die „Was sauber ist"-Listen, auf die spätere Sitzungen zurückgreifen. Jede der vier Runden 6–9 trägt jetzt einen Abarbeitungsvermerk mit Datum und den bewusst übersprungenen Punkten (6.8, 6.11, 7.4, 8.17). **(2)** `pages/meta.tex` ist entgegen der bisherigen Statusangabe **vollständig gefüllt** — `\ModuleCode{DLBMIMPFS01-01}`, `\MatrikelNr{3210267}`, Tutor, Studiengang und Schlagworte stehen; nur `\SubmissionDate` bleibt bewusst auf `\today` (Nutzerentscheidung, siehe `PERSISTENT.md`). Der Punkt war seit Tagen als offen geführt, ohne es zu sein.

**Cold-Read-Session abgeschlossen (2026-07-30, neue Sitzung ohne festen Skill-Aufruf, neuer Modus siehe `SKILL-ANPASSUNGEN.md` → P28).** Nutzer hat mehrere Kapitel satzweise mit Claude durchgesprochen. Ergebnis: alle vier Tabellen auf `\footnotesize` umgestellt (main.tex unangetastet, siehe P29), ein zweiter Testpunkt „Klick-Prototyp-Test mit externen Testpersonen" in Phase 2 ergänzt (`07_iteratives_design.tex`, `05_phasenplanung.tex`) und die dadurch veraltete Nutzertest-Erwähnung im Fazit nachgezogen (`01_kernergebnisse_und_ausblick.tex`, `02_limitationen.tex` — waren veraltet, siehe P30), außerdem mehrere Kontrastfiguren/Jargon-Stellen vereinfacht (`04_community_und_feedback.tex`, `06_evaluation_reflexion.tex`, `01_kernergebnisse_und_ausblick.tex`). `check_all.py`: 0 FEHLER, Umfang 3.701 Wörter (im Soll).

**Überarbeitung nach Prüfbericht: die beiden Textbefunde sind erledigt (2026-07-30, eigene Session).** Befund 2 (`03_konzept.tex:15`, Variante (a): „zwei der drei … Zwecke", +2 Wörter) und Befund 1 (`pages/meta.tex:7`, Geviertstrich U+2014 → Halbgeviertstrich U+2013 im `\PaperTitle`) umgesetzt. `check_all.py --kapitel chapters/02_durchfuehrung`: 0 FEHLER · `check_quellentreue.py --datei chapters/`: 20/20 OK, keine Zitation invalidiert · Umfang 3.702 Wörter im Korridor 2.625–3.750. Nebenbefund ohne Handlungsbedarf: In `meta.tex` stehen zwei weitere Geviertstriche, beide in `%`-Kommentarzeilen (Z. 2 und Z. 15) — sie werden nicht gesetzt und waren nicht Teil des Befunds. **Build auf ausdrückliche Nutzeranweisung selbst gefahren** (Ausnahme vom Grundprinzip „Kompilieren ist Sache des Nutzers", der Nutzer hat die Überprüfungen explizit angefordert): `latexmk -g -lualatex main.tex`, Exit 0, 23 PDF-Seiten, **0 Overfull \hbox, 0 Missing character, 0 `!`-Fehler**, **Textteil unverändert 10 Seiten** von 7–10 (gedruckt 1–10 = PDF-Seite 6–15). Titelblatt am PDF gegengelesen: „Resteria – Konzeption einer …", Halbgeviertstrich sitzt.

**Manueller Prüfpunkt 1 aufgelöst — Erlhofer erscheint 2018, nicht 2017.** Über die SRU-Schnittstelle der Deutschen Nationalbibliothek (`services.dnb.de/sru/dnb`, `num=9783836245579`, MARC21-xml): MARC 245 „Website-Konzeption und Relaunch / Sebastian Erlhofer, Dorothea Brenner" **ohne Untertitelfeld**, MARC 250 „1. Auflage", MARC 264 „Bonn : Rheinwerk Verlag : **2018**". Damit sind beide Hälften von Befund 4 belegt: Der Untertitel „: Das Handbuch für die Praxis" ist Buchhandels-Marketingtext (AbeBooks führt ihn, der Verlag nennt für die 2. Auflage wiederum „Planung, Optimierung, Usability" — drei verschiedene Zusätze, keiner im Buch), und das Impressum („1. Auflage 2018") schlägt die OPF-Metadaten (`2017-11-07`), die ein Produktions-, kein Erscheinungsdatum tragen. Nach dem Zotero-Fix rendert der Kurzbeleg in `07_iteratives_design.tex` als „Erlhofer & Brenner, 2018"; der gepinnte Citekey behält die 2017 im Namen (kosmetisch, folgenlos).

**Zotero-Schritte überprüft (2026-07-30):** **Befund 3 sauber durch** — `pages` bei Soma und Nivedhitha geleert, `eid` gesetzt; das gerenderte Verzeichnis zeigt „Sustainability, 12(3), Artikel 907." und „48(2), Artikel e13038.", je einmal und identisch zum Muster von Barker und Shen. Zwischenzeitlich standen beide Felder gleichzeitig belegt und druckten die Nummer **doppelt** („Artikel 907, 907") — als Nachtrag zu **P32** in `SKILL-ANPASSUNGEN.md` festgehalten, inklusive der Lehre, dass eine Zotero-Anweisung mit zwei Teilschritten („Feld setzen **und** Seitenfeld leeren") einzeln nummeriert gehört. **Befund 4 ebenfalls vollständig durch** (zweite Prüfung derselben Session): `title = {Website-Konzeption und Relaunch}` ohne Untertitel, `date = {2018}`. Am gebauten PDF verifiziert — Verzeichnis „Erlhofer, S. & Brenner, D. (2018). Website-Konzeption und Relaunch. Rheinwerk Computing.", In-Text „laut Erlhofer und Brenner (2018, Kap. 17.2.2)", die Jahresänderung ist also im Kurzbeleg angekommen. `check_bib_hygiene.py`: 10 Einträge, 0 Hinweise. **Damit sind alle vier Befunde des Abgabe-Audits erledigt.** Beobachtung ohne Handlungsbedarf: Das Autorenfeld trägt jetzt Initialen (`{Erlhofer, S and Brenner, D}`) statt ausgeschriebener Vornamen wie alle übrigen Einträge — die APA-Ausgabe ist identisch, weil der Stil ohnehin abkürzt.

**Gezielter Konsistenz-Durchgang statt zweiter Gegenlesung (2026-07-30, auf Nutzerfrage).** Volle Gegenlesung bewusst verworfen: Ihr Zweck — Abwesenheiten gegen `aufgabe.md` — war durch Teil-Check B/H desselben Tages abgedeckt, und bei 10 von 10 Seiten mit ~2 Zeilen Reserve erzwingt jeder additive Befund eine weitere Kürzungsrunde, den Arbeitsschritt mit der höchsten dokumentierten Fehlerquote in diesem Projekt. Stattdessen alle 12 Subsections gegen drei Fehlerklassen mit je einem belegten Vorfall gelesen (Frequenzangaben gegen Mechanismus · Absolutformulierungen · Modalität geplant/existierend). **Drei Funde, alle behoben:** (1) `03_konzept.tex:15` — der „täglich"-Fix hatte eine Wortdopplung eingeführt („wiederkehrenden Nutzung" gegen „monatlich wiederkehrende Zero-Waste-Woche" im selben Satz), jetzt „über die einmalige Nutzung hinaus verankert"; (2) `03_konzept.tex:34` — „für die **drei** Bereiche Mockups" gefolgt von **vier** Abbildungen, also dieselbe Fehlerklasse wie Befund 2 zwei Absätze weiter, jetzt „für die drei Bereiche **vier** Mockups … die Startseite …, **dieselbe mit ausgeklappten gespeicherten Rezepten** …" (löst zugleich den `DOPPELBELEGUNG`-Hinweis); (3) `06_evaluation_reflexion.tex:3` — Propagationslücke des Einzel-Stresstests vom selben Tag: Die in `02_zielsetzung_und_verengung.tex:5` gegen Soma abgeschwächte Aussage stand hier weiter im Indikativ („weil die Gamification-Mechanik vor allem bei bereits motivierten Personen **wirkt**"), jetzt „vor allem bereits motivierte **oder zumindest interessierte** Personen ansprechen **dürfte**". Geprüft und stimmig: alle Zeitangaben der Phasenplanung gegen `07_iteratives_design`, alle 14 Zählangaben, die Modalitätsgrenze geplant/erbracht, die Freemium-Trennlinie. `check_all.py` 0 FEHLER · `check_quellentreue.py` 20/20 OK · Build Exit 0, 0 Overfull, 0 Missing character, **Textteil weiter 10 Seiten**, 3.721 Wörter.

**Build-Bruch nach Commit `dbfe4f7` behoben (2026-07-30, auf Nutzermeldung „PDF damaged", Build in dieser Session auf ausdrückliche Anweisung selbst gefahren).** Der Commit „prompt anpassungen" hat in `pages/appendix.tex:60` (`\quelle{}` zu `fig:mockup_profil`) den Halbsatz „, die die Rettungspunkte in eine greifbare Einsparung übersetzt}" gestrichen — mit ihm die **schließende Klammer des inneren `\enquote{Ergänze unterhalb …}`**. Dadurch schloss das finale `}` nur noch das Folge-Prompt-Zitat, `\quelle{}` blieb offen: „File ended while scanning use of `\quelle`", danach Folgefehler bis „Fatal error occurred, no output PDF file produced". Die `main.pdf` auf der Platte stammte aus dem abgebrochenen Lauf — genau das meldet der Reader als beschädigt. Fix: ein `}` nach `\enquote{12,4 kg Lebensmittel gerettet}` ergänzt, damit steht die Zeile strukturgleich zu `fig:mockup_feed` (Z. 53). Der vom Nutzer geänderte **Prompt-Wortlaut selbst blieb unangetastet** — er zitiert die tatsächliche Eingabe. Neubau: `latexmk -g -lualatex`, **Exit 0**, 23 PDF-Seiten, 0 `!`-Fehler, 0 Overfull \hbox, 0 Missing character, **Textteil unverändert 10 Seiten** (Kapitel 1 auf PDF-S. 6, Literaturverzeichnis ab PDF-S. 16). Am gerenderten PDF geprüft, dass die reparierte Quellenzeile das Zitat korrekt schließt. `check_all.py`: 0 FEHLER. **Lehre:** Ein Commit an einer `.tex`-Datei ohne anschließenden Build kann die Arbeit unbaubar hinterlassen, ohne dass irgendein Skript anschlägt — `check_formalia.py` prüft keine Klammerbilanz, und der einzige Zeuge ist `build/main.log`.

**Nächster Schritt**: **Kein Arbeitsschritt am Text mehr offen — die Arbeit ist textseitig abgabereif (Delta-Re-Audit 2026-07-30, 100/100).** Es bleiben ausschließlich Nutzer-Schritte, die kein Audit ersetzen kann: Eidesstattliche Erklärung über myCampus (ohne sie nimmt Turnitin die Einreichung nicht an), LanguageTool-Durchgang über den Gesamttext (3.721 Wörter), einmal am PDF am Stück durchlesen, keine externe Plagiatssoftware vorab, und der eine verbliebene manuelle Prüfpunkt aus `pruefbericht.md` (Wortlaut der vier zitierten Claude-Design-Folge-Prompts gegen den Chatverlauf). Wird vor der Abgabe noch etwas geändert: neu bauen und die gedruckte Seitenzahl gegenprüfen — der Textteil steht exakt auf der Obergrenze 10, die Reserve auf der letzten Seite beträgt rund zwei Zeilen. *(Historisch, erledigt:)* **~~Delta-Re-Audit in frischer Session~~** — gefahren, Score 100/100. *(Historisch, erledigt:)* **~~zwei Zotero-Schritte~~, dann Delta-Re-Audit** — alles Übrige ist geprüft und erledigt. Befund 3: Extra-Feld `tex.eid: 907` bei `somaFoodWasteReduction2020` und `tex.eid: e13038` bei `nivedhithaHowGreenSocial2024`, Seitenfeld jeweils leeren. Befund 4: Erlhofer-Titel auf „Website-Konzeption und Relaunch" kürzen **und Jahr auf 2018 setzen** (Frage ist entschieden, siehe oben). Danach BBT neu exportieren, einmal bauen und die gedruckte Seitenzahl gegenprüfen — der Textteil steht exakt auf der Obergrenze 10, die Reserve auf der letzten Seite beträgt rund zwei Zeilen. Erwarteter Score nach vollständiger Umsetzung: 100/100. **Vor der Einreichung bleiben Nutzer-Schritte**, die kein Audit ersetzen kann: Eidesstattliche Erklärung über myCampus (ohne sie nimmt Turnitin die Einreichung nicht an), LanguageTool-Durchgang über den Gesamttext, einmal am PDF durchlesen, und die beiden manuellen Prüfpunkte aus `pruefbericht.md` (Erscheinungsjahr Erlhofer an der Rheinwerk-Produktseite; Wortlaut der vier zitierten Claude-Design-Folge-Prompts).

*(Vorheriger Stand:)* **keine Textänderung mehr offen, Audit-Voraussetzungen erfüllt.** Es bleiben drei reine Nutzer-Schritte — manuelles Durchlesen am PDF plus LanguageTool, die Eidesstattliche Erklärung über myCampus, und die Prüfung, ob die in `pages/appendix.tex` zitierten Claude-Design-Folge-Prompts wortgleich dem entsprechen, was im Chat eingegeben wurde. Danach das **Abgabe-Audit** (`pruef-modus`) als letzter Schritt in einer neuen Session; `pruefbericht.md` ist seit Runde 8+9 überholt und wird dort neu erhoben. Das Audit selbst muss dabei ohnehin (a) `check_quellentreue.py --mit-quellen` frisch laufen lassen — mehrere Trägersätze haben sich heute geändert (Soma, Erlhofer, Nielsen, Sauro & Lewis) —, und (b) einen lokalen Build fahren, um die reale Seitenzahl nach der Tabellen-Verkleinerung zu bestätigen (Schätzung weiterhin ~10 S., aber `check_umfang.py` kennt Tabellen-Layout nicht).

*(Vorheriger Stand:)* `AENDERUNGEN.md` Runde 8+9 (Gegenlesung + Zitatkontext-Gegenlesung, 2026-07-29) am 2026-07-29 vollständig abgearbeitet. Nächster Schritt: **lokaler Build** (`latexmk -g -lualatex main.tex`), gedruckte Seitenzahl gegenprüfen (Textteil stand vor der Runde exakt bei 10 Seiten, jetzt 3.646 statt 3.567 Wörter, ~9,7 S. geschätzt) — danach **Abgabe-Audit** (`pruef-modus`). Details der abgearbeiteten Runde: siehe Statuszeilen `sec:durchfuehrung`/`sec:fazit` oben und `AENDERUNGEN.md` → Runde 9 → „Nächster Schritt".

*(Ältere Einträge, zur Historie:)* GS-1…GS-6 aus Runde 2 vollständig abgearbeitet (2026-07-21) — GS-1 nach aktiver Rückfrage (`AskUserQuestion`) mit Nutzer-Entscheidung „volle Aufnahme": leftovercooking per Web-Recherche als 6. Plattform in Wettbewerbsanalyse aufgenommen. Runde-1-Befund 1 (UI/UX-Mockups) erledigt (2026-07-23): vier echte Mockup-Screenshots eingebunden, Quellenzeilen nach IU-Pflichtmuster mit KI-Kennzeichnung.

**Prüfbericht abgearbeitet (2026-07-23).** Nutzer hat für diese Session einmalig explizite Freigabe erteilt, `main.tex` zu ändern (Abweichung vom Grundprinzip „main.tex nie ändern"). Umgesetzt: (1) **Handlungsempfehlung 1** — `\appendix`/`\include{pages/appendix}` in `main.tex` einkommentiert; die vier UI/UX-Mockups + Persona-Tabelle werden jetzt kompiliert, die fünf Anhang-`\autoref`s sind aufgelöst. (2) **Handlungsempfehlung 2** — `\listoffigures`/`\listoftables` in `main.tex` einkommentiert. (3) **Handlungsempfehlung 3** — „vier" → „drei Plattform-Kategorien" in `chapters/03_fazit/02_limitationen.tex`. (4) **Handlungsempfehlung 4** — Zeitpuffer-Formulierung in `chapters/02_durchfuehrung/05_phasenplanung.tex:23` an die Tabelle angeglichen („innerhalb der MVP-Phase" statt „zwischen MVP-Entwicklung und Beta-Test"). Handlungsempfehlung 5 (UI/UX `\ac{}`-Vereinheitlichung) bewusst ausgelassen — als rein optional markiert, keine Regelverletzung. `check_formalia.py` auf beiden geänderten Kapiteldateien: 0 FEHLER. Teil-Check D (Build) bleibt vom Nutzer lokal zu verifizieren — `lualatex`/`latexmk` in dieser Umgebung nicht verfügbar.

**Delta-Re-Audit (2026-07-23, Score 100/100) ist durch Runde-3-Gegenlesung überholt** — der damalige Score bezog sich auf den Textstand vor den unten beschriebenen Änderungen an `01_wettbewerbsanalyse.tex`, `02_limitationen.tex` und `03_konzept.tex`; `pruefbericht.md` selbst wurde nicht überschrieben und ist als Momentaufnahme des alten Stands zu lesen, nicht als aktuell gültiger Score.

**AENDERUNGEN.md Runde 3 (Gegenlesung 2026-07-23) vollständig abgearbeitet.** Acht Befunde (3.1–3.8): 3.1 (Zähl-/Aufzählungsdrift „drei"→„vier" Einwände, „ein bis zwei"→„ein bis drei" Vertreter) und 3.6 (unbelegte Marktbehauptung abgeschwächt) mechanisch ohne Freigabe umgesetzt. Die vier `[FREIGABE]`-Punkte per `AskUserQuestion` einzeln geklärt: 3.2 Chefkoch Community-Funktionen nach Web-Faktencheck auf „stark" gehoben; 3.3 leftovercooking-Belohnungspunkte als individuelles Spar-Feedback vom Gamification-Kriterium abgegrenzt, Zelle „mittel" bleibt; 3.4 unbelegter Verbraucherzentrale-Verweis gestrichen, leftovercooking-Bewertung läuft rein als eigene Sichtung; 3.5 neue Matrixspalte „Rezept-Entdeckung" in `tab:wettbewerbsvergleich` ergänzt (jetzt 5 Kriterien statt 4). 3.7 (optionale Einstiegsliteratur) übersprungen wie in Runde 1 — Quellen fehlen weiter in `references.bib`. 3.8 ist ein reiner Build-Grenzvermerk ohne Textauftrag. `check_formalia.py` (0 FEHLER) und `check_bib_keys.py` (6/6 Keys valide) auf allen betroffenen Dateien bestätigt.

**Voll-Audit abgeschlossen (2026-07-23, Score 100/100 — abgabereif).** Alle Teil-Checks A–C fehlerfrei, `sources/Anmerkungen vom Prüfer.md` als zusätzlicher Maßstab abgeglichen (alle Prüfer-Punkte erfüllt, inkl. SWOT-Charakter der Wettbewerbsanalyse und KI-Mockup-Kennzeichnung). Keine Score-Abzüge. Teil-Check D (Build) blieb ÜBERSPRUNGEN (`lualatex`/`biber` nicht verfügbar), statisch aber sauber. Details in `pruefbericht.md`.

**Pruefbericht-Analyse (2026-07-23, diese Session):** Die vier Handlungsempfehlungen aus `pruefbericht.md` durchgesehen. Umsetzbar und rein kosmetisch/strukturell, ohne Bezug zu These/Kernargumenten, daher ohne Rückfrage umgesetzt: **Handlungsempfehlung 3** — `chapters/01_einleitung/03_vorgehen_und_aufbau.tex` um je einen Satz pro Absatz ergänzt (Verzahnung Wettbewerbsanalyse/Verhaltensliteratur; Trichter-Logik der Kapitelabfolge), keine neuen Fakten-Claims, `check_formalia.py` 0 FEHLER. Bewusst NICHT umgesetzt: **Handlungsempfehlung 1** (lokaler Build) und **2** (Seitenumfang 7–10 S.) — beides erfordert `lualatex`/`latexmk`, die in dieser Umgebung nicht verfügbar sind; bleibt Nutzer-Aufgabe laut „Vor der Einreichung". **Handlungsempfehlung 4** (optionale Einstiegsquelle Kapoor et al. / Hansch & Rentschler) übersprungen — beide Quellen fehlen in `references.bib`, das nur per Zotero/BBT-Auto-Export befüllt werden darf, nicht manuell; zudem laut `aufgabe.md` nicht Pflicht. Kein neuer Voll-Audit gefahren, da die Änderung rein strukturell ist und den bestehenden Score 100/100 nicht berührt.

**Terminologie-Korrektur (2026-07-24, diese Session):** Der Prüfer adressiert in `sources/Anmerkungen vom Prüfer.md` explizit eine Einzelperson („Du bist also Projektverantwortliche"), es gibt kein Team. „Projektgruppe" (16 Fundstellen über `01_einleitung`, `02_durchfuehrung`, `03_fazit`) durch pronomenfreie Selbstbezüge („diese Arbeit"/„dieser Bericht", Nutzerentscheidung per `AskUserQuestion`) ersetzt, in der Phasenplanungs-Tabelle „Projektgruppe"/„Gesamtes Team" (Ressourcen-Spalte) zu „Eigenleistung". Mehrere redundante „die Aufgabenstellung fordert/verlangt das auch nicht"-Nebensätze gestrichen (reine Wiederholung ohne Begründungswert); Stellen, die aktiv eine Scope-Entscheidung begründen (z. B. Event-Kalender-Verzicht, Funktionsübersicht-Tabelle), bleiben stehen. „Projektbericht" als Selbstbezeichnung des Dokuments bewusst NICHT verändert — das ist der tatsächliche Papiertyp laut `aufgabe.md`, kein austauschbares Label wie „Konzept" (das bezeichnet die Plattform-Idee, nicht das Dokument). Zusätzlich in `02_zielsetzung_und_verengung.tex:5` ein unbelegter Fakten-Claim „eher ein Hindernis als ein Mehrwert" auf die durch die Prämisse tatsächlich gedeckte Aussage „ohne echten Mehrwert" abgeschwächt. `check_formalia.py` über alle Kapitel: 0 FEHLER (nur Alt-Hinweise zu Satzlänge/Trias, unverändert). Kein neuer Voll-Audit gefahren — rein stilistisch/formal, kein neuer Fakten- oder Argumentations-Claim, Score 100/100 bleibt als letzter inhaltlicher Stand gültig, sollte aber vor Abgabe nochmal kurz gegengelesen werden, da mehrere Sätze umformuliert wurden.

**Mockup-Audit gegen Textansprüche (2026-07-24, diese Session):** `02_zielgruppe.tex` und `03_konzept.tex` gegen die vier bestehenden Mockups abgeglichen. Zwei Lücken gefunden: (1) Rezept-Upload-Funktion (`03_konzept.tex` + Funktionsübersicht-Tabelle) hat in keinem Mockup einen sichtbaren „Beitrag erstellen"-Button. (2) `02_zielgruppe.tex` verlangt eine sichtbare Einsparangabe („wie viel eine Person tatsächlich einspart"), das Profil-Mockup zeigt bisher nur abstrakte Rettungspunkte/Level. Neue Datei `sources/claude-design-nachbesserung.md` mit zwei konkreten Folge-Prompts für Claude Design erstellt (Plus-Button im Rettungs-Feed, Kennzahl „X kg gerettet" im Profil). Die `\quelle{}`-Zeilen in `pages/appendix.tex` für `fig:mockup_profil` und `fig:mockup_feed` bereits auf die kombinierten Prompts (Runde 1 + Nachbesserung) aktualisiert, `check_formalia.py`: 0 FEHLER. Nachtrag: Auf Nachfrage auch `04_community_und_feedback.tex` gegen die Mockups geprüft (war zunächst nicht Teil des Auftrags) — dritte Lücke gefunden: die dort beschriebenen Challenges/Rangliste („Zero-Waste-Woche") fehlen im Rettungs-Feed-Mockup komplett, zudem weicht das Tag-Format „gerettet: [Zutat]" leicht vom gezeigten „[Zutat]" ohne Präfix ab. Als „Anpassung 1" in `sources/claude-design-nachbesserung.md` mit dem bereits bestehenden Plus-Button-Fix zusammengelegt (derselbe Screen), Folge-Prompt bewusst unter 40 Wörtern gehalten (sonst hätte `hard-rules-formal.md` die `blockzitat`-Umgebung statt `\enquote{}` verlangt — in einer Bildquellenzeile unpassend). `\quelle{}` für `fig:mockup_feed` in `pages/appendix.tex` entsprechend erweitert, `check_formalia.py`: 0 FEHLER.

**Tag-Präfix „gerettet:" entfernt (2026-07-24, diese Session):** Auf Nutzerwunsch das Präfix „gerettet:" vor der Zutat im Rettungs-Feed-Tag gestrichen — wirkt auf dem kleinen Mobile-Screen überladen, der Kontext macht es ohnehin klar. Da die bestehenden Mockups sowieso schon nur die Zutat ohne Präfix zeigen, wurde stattdessen der Text in `04_community_und_feedback.tex:3` an das bestehende Bild angeglichen (`\enquote{gerettet: Brokkoli-Strunk}` → `\enquote{Brokkoli-Strunk}`), nicht umgekehrt. `sources/claude-design-nachbesserung.md` und die `\quelle{}`-Zeile für `fig:mockup_feed` in `pages/appendix.tex` entsprechend bereinigt — Anpassung 1 verlangt jetzt nur noch Plus-Button + Challenge-Banner, keine Tag-Änderung mehr. `check_formalia.py`: 0 FEHLER.

**Mockup-Nachbesserung abgeschlossen (2026-07-24, diese Session):** Nutzer hat `resteria_profil.png` und `resteria_rettungsfeed.png` mit den Folge-Prompts aus `sources/claude-design-nachbesserung.md` aktualisiert und die Dateien in `images/durchfuehrung/` ersetzt. Per Bild-Kontrolle (Read-Tool) verifiziert: Rettungs-Feed zeigt jetzt das Challenge-Banner „Zero-Waste-Woche · Rangliste · Platz 4" sowie den Terracotta-Plus-Button unten rechts; Profil zeigt „12,4 kg Lebensmittel gerettet" unter dem Rettungspunkte-Balken — beide exakt wie in `pages/appendix.tex` → `\quelle{}` zitiert. Damit sind alle drei in dieser Session gefundenen Text-Mockup-Lücken (Upload-Button, Einsparangabe, Challenges/Rangliste) auch visuell geschlossen. Kein weiterer Nutzer-Schritt zu den Mockups offen.

**Umfangsanalyse (2026-07-24, diese Session):** Fließtext über alle 14 `chapters/**/*.tex` gezählt — **4.508 Wörter**, bei 11pt/2 cm Rändern/`\onehalfspacing` geschätzt **10–11 Seiten Textteil** gegen die Vorgabe 7–10 (`aufgabe.md:18`). Kapitelanteile (14,3 / 70,8 / 14,9 %) liegen im Soll, Einleitung und Fazit aber am oberen Rand. Acht Kürzungsbefunde als **`AENDERUNGEN.md` Runde 4** angelegt (erwarteter Ertrag ~715 Wörter ≈ 16 %, ausschließlich Redundanz, kein Informationsverlust); 4.1, 4.2, 4.5, 4.7 sind `[FREIGABE]`. Befund 4.0 ist ein Nutzer-Schritt: lokaler Build als Messwert **vor** der Umsetzung — bei ≤ 9,5 Seiten genügen 4.1–4.4. Parallel drei Workflow-Befunde notiert [**KORREKTUR 2026-07-28:** Die hier genannte Datei `SKILL-VERBESSERUNGEN.md` existiert weder im Arbeitsverzeichnis noch in der Git-Historie — die drei Befunde sind verloren. Nachfolger ist `SKILL-ANPASSUNGEN.md` im Projekt-Root.] (4: kein Schritt im Fahrplan subtrahiert · 5: Umfangsprüfung hängt allein an Teil-Check D, der hier nie lief, weshalb 100/100 bei ungeprüftem Pflichtkriterium vergeben wurde · 6: Redundanz zwischen Dateien ist by design blind) samt Einschätzung gegen einen eigenen Kürzungs-Modus.

**AENDERUNGEN.md Runde 4 (Nutzer-Feedback/Umfangsanalyse, 2026-07-24) vollständig abgearbeitet.** Alle neun Punkte (4.0–4.8) umgesetzt: 4.0 ohne Messwert (lualatex/latexmk fehlen weiterhin in dieser Umgebung), Nutzer-Entscheidung nach Rückfrage: alle Kürzungen trotzdem umsetzen. Die vier `[FREIGABE]`-Punkte (4.1, 4.2, 4.5, 4.7) per `AskUserQuestion` einzeln geklärt und wie vorgeschlagen umgesetzt — bei 4.5 bewusst konservativer (3 statt 4 Stellen gekürzt, um eine Matrixbias-Mitigation aus Runde 1 und den laut 4.3 geschützten Prozess-Reflexions-Absatz nicht zu beschädigen) und bei 4.4 ebenfalls konservativer (vier statt zwei Sätze in `03_vorgehen_und_aufbau.tex`, sonst wäre die Datei unter die HALBSEITE-Schwelle von 150 Wörtern gerutscht). Bei 4.6 zusätzlich eine seit Runde 2 (GS-2) bestehende Propagationslücke behoben: Der Fazit-Satz zu Barkers Reminders verwies noch auf „die steigenden Reste-Level" statt auf die in `03_konzept.tex` bereits umbenannte Erinnerungsbenachrichtigung. `check_formalia.py`: 0 FEHLER (10 HINWEIS, überwiegend bekannte Tabellen-Satzlängen-Artefakte). `check_bib_keys.py`: alle 6 Keys valide. Tatsächlicher Wortzahl-Ertrag ca. 394 Wörter (~8,7 %) statt der geschätzten ~715 (~16 %) — geringer, weil mehrere Punkte bewusst konservativer umgesetzt wurden als ursprünglich skizziert. Details je Befund in `AENDERUNGEN.md`. „Offen" ist damit wieder vollständig leer.

**Delta-Re-Audit durchgeführt (2026-07-24, Score 90/100 — abgabereif).** Geprüft wurde der Delta der Runde-4-Kürzung (10 geänderte Kapiteldateien) plus beide Skripte vollständig und die kapitelübergreifenden Kürzungsfolgen. Ein Struktur-FAIL: In `chapters/02_durchfuehrung/01_wettbewerbsanalyse.tex:34` kündigt der Text „zwei Einwände" an, es folgen aber vier (drei kurz abgehandelte plus ein „gewichtigerer") — die Kürzung baute vier ausformulierte Einwände auf eine gestaffelte Darstellung um, ohne den Zählsatz mitzuführen. Derselbe Fehlertyp war in Runde 3 (Befund 3.1) an derselben Stelle schon einmal korrigiert worden. Substanz ist unbeschädigt (alle vier Einwände stehen und werden entkräftet), nur die Ankündigungszahl stimmt nicht. Keine weiteren Regressionen: Abhakliste-Schlüsselbegriffe per Grep vollständig abgedeckt, „Drivers/Levers" bleibt bei Erstnennung definiert, alle übrigen Zählangaben nachgezählt und stimmig, keine Datei unter die HALBSEITE-Schwelle gerutscht (Minimum 155 Wörter). Drei kosmetische Hinweise ohne Score-Wirkung im Bericht (unangekündigte Funktionsübersicht-Tabelle, entfallene UI/UX-Kurzerklärung, verwaistes „dabei").

**Überarbeitung nach Prüfbericht abgeschlossen (2026-07-24, diese Session).** Handlungsempfehlung 1 aus `pruefbericht.md` umgesetzt: Zählwiderspruch in `chapters/02_durchfuehrung/01_wettbewerbsanalyse.tex:34` behoben („zwei Einwände" → „mehrere Einwände", ohne inhaltlichen Eingriff — alle vier Einwände und ihre Entkräftung bleiben unverändert). `check_formalia.py` auf `chapters/02_durchfuehrung/`: 0 FEHLER, dieselben 10 bekannten HINWEISE wie im Delta-Re-Audit, keine neuen. Die drei optionalen kosmetischen Hinweise aus Handlungsempfehlung 3 (Anmoderation `tab:funktionsuebersicht`, „dabei" streichen, UI/UX-Kurzerklärung) bewusst NICHT umgesetzt — als score-neutral markiert, kein Auftrag dieser Session. Kein neuer Voll-Audit gefahren, da der Fix rein mechanisch ist und `pruefbericht.md` selbst als ÜBERHOLT markiert bleibt, bis ein neuer Audit-Lauf den Stand bestätigt. **Nächster Schritt: reine Nutzer-Schritte vor der Einreichung** (siehe `pruefbericht.md` → „Vor der Einreichung"): lokalen Build fahren (`latexmk -lualatex main.tex`), Seitenumfang 7–10 S. bestätigen (Schätzung ≈ 9–10 S., damit im Soll), LanguageTool-Durchgang, Eidesstattliche Erklärung über myCampus. Optionaler Feinschliff (Einstiegsliteratur, die drei kosmetischen Hinweise) ist nicht abgabekritisch.

**Gegenlesung Runde 5 angelegt (2026-07-24, diese Session).** Kalte Prüfer-Gegenlesung gegen `aufgabe.md` durchgeführt, elf Befunde (5.1–5.11) als neue Runde in `AENDERUNGEN.md` — „Offen" ist damit nicht mehr leer. Kein schwerer Befund (kein fehlendes Artefakt, keine unbelegte Kernbehauptung), aber drei Gruppen: Sichtbarkeit (5.1 Instagram fehlt trotz wörtlicher Nennung in `aufgabe.md:10`, 5.2 Teilaufgabe 3 ohne eigene Überschrift, 5.4 Achse „Kommunikation" umbenannt), Nachweisbarkeit (5.3 Wettbewerbsanalyse ohne durchgängigen Erhebungsnachweis, 5.6 Prozess-Reflexion ohne Schwierigkeit, 5.10 Ressourcenträger der MVP-Phase offen), Konsistenz (5.5 Doppelmaßstab Gruppen/Following, 5.7 Menüplanung ohne UI-Zugang, 5.8 Prompt-Zeile ≠ Bild, 5.9 Mockup-Zahlen ≠ Punkte-Regel, 5.11 Satzschlussmuster). `[FREIGABE]`: 5.1 und 5.5. **Nächster Schritt: „Schreib-Modus: AENDERUNGEN.md abarbeiten" in frischer Session** — vorher Nutzer-Schritt: lokaler Build zur Seitenmessung, da die Runde teils additiv ist (Arbeit liegt bei ~4.250 Wörtern ≈ 9–10 S. am oberen Rand). Ein Voll-Audit ist erst nach Abarbeitung sinnvoll.

**AENDERUNGEN.md Runde 5 (Gegenlesung, 2026-07-24) vollständig abgearbeitet (2026-07-24, diese Session).** Alle elf Befunde (5.1–5.11) umgesetzt. Vorab per `AskUserQuestion` geklärt: Seitenbudget (Nutzer-Entscheidung: konservativ planen, kein lokaler Build verfügbar) sowie die beiden `[FREIGABE]`-Punkte 5.1 (Option B: TikTok stellvertretend für Instagram) und 5.5 (a）Gruppen aus der Kriteriendefinition gestrichen, (b) Following für Resteria ergänzt — auf Nutzer-Nachfrage bewusst an die bereits sichtbare Nutzername-Struktur im Rettungs-Feed-Mockup angebunden statt einen dort nicht gezeigten Folgen-Button zu behaupten, daher kein neues Mockup nötig. Strukturänderung: Befund 5.2 (Teilaufgabe 3 ohne eigene Überschrift) wurde durch eine neue Subsection gelöst — `chapters/02_durchfuehrung/07_iteratives_design.tex` (`\subsection{Iteratives Design und Feedback}\label{sec:iteratives_design}`), inhaltlich zwischen `04_community_und_feedback` und `05_phasenplanung` eingebunden, `kapitelplan.md` entsprechend nachgezogen. Übrige Befunde rein mechanisch bzw. textlich gelöst: 5.3 (einheitlicher Methodensatz statt zwei isolierter Sichtungsvermerke), 5.4 (Spalte „Community-Funktionen" → „Kommunikation \& Community"), 5.6 (Prozess-Reflexion um zwei konkrete Ereignisse ersetzt statt Leerformel), 5.7 (Menüplanung als Startseiten-Zugang erklärt, Option A ohne neues Bild), 5.8 (Prompt-Zeile an Bild angeglichen), 5.9 (Rettungspunkte-Regel von starrem 1:1 auf „nach Menge/Art" gelockert, macht Mockup-Zahlen widerspruchsfrei), 5.10 (Ressourcenträger für Design- und MVP-Phase ergänzt), 5.11 (drei Pointen-Schlusssätze gestrichen). `check_formalia.py`: 0 FEHLER, 11 HINWEIS (ausschließlich bekannte Tabellen-/Satzlängen-Artefakte, keine neuen Verstöße). `check_bib_keys.py`: alle 5 in Kapitel 2 genutzten Keys valide. „Offen" in `AENDERUNGEN.md` ist damit wieder vollständig leer.

**Gegenlesung Runde 6 angelegt (2026-07-24, diese Session).** Kalte Prüfer-Gegenlesung gegen `aufgabe.md`, ausgeführt von einem Subagenten unter striktem Leseverbot (kein `kapitelplan.md`, kein `pruefbericht.md`, kein `AENDERUNGEN.md`-Inhalt, kein Projektstatus), weil die Hauptsession bereits Projektkontext trug. Elf Befunde (6.1–6.11) als neue Runde in `AENDERUNGEN.md` — „Offen" ist damit nicht mehr leer. Kein fehlendes Artefakt, keine nicht ausgeführte Teilaufgabe. Schwerster Punkt: 6.1 `[FREIGABE]` — die Wettbewerbsanalyse trägt außer dem Methodensatz (`01_wettbewerbsanalyse.tex:15`) keine einzige Quellenangabe, obwohl sie nachprüfbare Plattform-Fakten behauptet und der Prüfer genau deren Nachvollziehbarkeit verlangt. Weiter: 6.2 (Startseiten-Mockup deckt den in `03_konzept.tex:53` behaupteten Menüplanungs-/Reste-Journal-Zugang nicht), 6.3 (Plus-Button überlappt im Feed-Mockup die Aktionsleiste; „Foto:" ohne Folgetext — Prüfer erlaubt KI-Mockups nur ohne gestalterische Fehler), 6.4 („innovativ/Innovation" kommt in `chapters/` kein einziges Mal vor, obwohl Teilaufgabe 2 und das 15-%-Kriterium Kreativität es verlangen), 6.5 (Vokabel „Kochtipp-Austausch" nirgends wiederauffindbar), 6.6 `[FREIGABE]` (Freemium stellt exklusive Challenges hinter die Bezahlschranke, während Challenges das einzige verbliebene Interaktionsformat sind), 6.7 (Entstehungsgeschichte nur punktuell statt „lückenlos"), 6.8 (Einstiegsliteratur Kapoor/Hansch fehlt in `references.bib`, laut `aufgabe.md` nicht verpflichtend), 6.9 (Kontrastfigur „nicht X, sondern Y" ~1,6×/Seite gegen die Regel max. 1/Seite), 6.10 (Anhangsverzeichnis fehlt im Inhaltsverzeichnis, Fix vollständig in `pages/appendix.tex`), 6.11 `[FREIGABE]`, optional (Resteria fehlt als Zeile in `tab:wettbewerbsvergleich`). Umfang ~4.680 Wörter ≈ 9–10 S.; die Runde ist selbstfinanzierend gebaut (6.9 deckt die Zugaben aus 6.4/6.7). **Nächster Schritt: „Schreib-Modus: AENDERUNGEN.md abarbeiten" in frischer Session**, vorher Nutzer-Schritt lokaler Build zur Seitenmessung. Hinweis: `AENDERUNGEN.md` folgt historisch nicht dem Offen/Erledigt-Format aus `aenderungen-format.md`; die neue Sektion „## Offen" steht deshalb am Dateiende unterhalb der Alt-Runden.

**AENDERUNGEN.md Runde 6 (Gegenlesung, 2026-07-24) abgearbeitet (2026-07-24, diese Session).** Zehn der elf Befunde vollständig umgesetzt, einer (6.8, optionale Einstiegsliteratur) wie in Runde 1/3 bewusst übersprungen, einer (6.3) nur vorbereitet. Die drei `[FREIGABE]`-Punkte per `AskUserQuestion` geklärt: 6.1 Option A kombiniert mit Option C (Quelle-Zeile um sechs offizielle Plattform-Domains + Sichtungsdatum erweitert, „über zwanzig Food-Creator:innen"/„bis zu drei übrige Zutaten" abgeschwächt — Nutzer-Vorbehalt „keine Blogs zitieren" per Web-Check und ZITIERREGEL-Abgleich bestätigt: Wettbewerbsanalyse ist eigene Sichtung, kein `\parencite`-Fall, ergänzte URLs sind ausschließlich die offiziellen Plattform-Domains), 6.6 Option B (exklusive Challenges aus dem Freemium-Paket gestrichen, durch erweiterte Vorratsverwaltung ersetzt), 6.11 Option B (unverändert gelassen). 6.3 bleibt teilweise offen: Folge-Prompt und `\quelle{}`-Zeile sind vorbereitet, der tatsächliche Bildaustausch in Claude Design ist wie bei den vorherigen Nachbesserungsrunden ein externer Nutzer-Schritt. `check_formalia.py`: 0 FEHLER, 12 HINWEIS (ausschließlich bekannte Artefakte plus ein neuer Satzschnitt-Hinweis in einer nicht bearbeiteten Datei). `check_bib_keys.py`: alle 6 Keys valide. Netto-Umfangsänderung ca. +100 bis +140 Wörter, Ausgangswert ~4.680 Wörter — bleibt im geschätzten 9–10-Seiten-Korridor, lokaler Build zur Bestätigung weiterhin Nutzer-Aufgabe. „Offen" in `AENDERUNGEN.md` ist bis auf den ausstehenden Mockup-Austausch (6.3) wieder leer.

**Voll-Audit durchgeführt (2026-07-24, Score 49/100 — nicht in diesem Stand einreichen).** Vorbedingungen erfüllt: Gegenlesung (Runden 5, 6) und Gesamt-Stresstest (Runde 2) abgearbeitet, `AENDERUNGEN.md` → „Offen" bis auf 6.3 leer. Befund 6.3 (Plus-Button-Überlapp im Rettungs-Feed-Mockup) wurde auf ausdrückliche Nutzer-Anweisung aus dem Audit ausgenommen und nicht gewertet. Fünf Befunde, Details je Befund in `pruefbericht.md`: (1) Formalia, −8 — elf narrative Zitate („Vittuari et al. unterscheiden … \parencite{}") doppeln bei `style=apa` den Autornamen; `\textcite` kommt in der Arbeit kein einziges Mal vor. Kein Regressions-, sondern ein Altbefund seit dem Erstschreiben, den die Audits vom 23.07. übersehen haben, weil `check_formalia.py` narrativ/parenthetisch nicht prüft. (2) Formalia, −8 — „BMEL", „SDG" und „USP" ohne Auflösung bei Erstnennung und ohne Eintrag in `pages/acronyms.tex`, während die Arbeit MVP, Nudge, Drivers/Levers, UI/UX und Fairteiler vorbildlich erklärt. (3) Struktur, −10 — `01_wettbewerbsanalyse.tex:36` folgert „Die USP … liegt damit nicht im bloßen Verbinden beider Elemente", der Folgesatz und Z. 34 sagen das Gegenteil; gemeint ist erkennbar eine Präzisierung gegen leftovercooking, formuliert ist eine Verneinung der eigenen Kernthese. (4) Struktur, −10 — `07_iteratives_design.tex:3` macht „diesen Bericht" zum Akteur der künftigen Design-Phase und prüft „am Klick-Prototyp", den es nicht gibt; der Folgeabsatz grenzt mit „im weiteren Entwicklungsprozess" genau diese eine Schleife von der Planungs-Kennzeichnung aus. Die Subsection stammt aus Runde 5 und war nie in einem Voll-Audit. (5) Pflichtregel, −15 — Limitation 2 fehlt die Mitigations-Schicht; sie stand bis Commit `3249b0f` (Kürzungsrunde 4) im Text und fiel als Kollateralschaden der Umfangskürzung weg. Teil-Check D blieb ÜBERSPRUNGEN (`lualatex`/`latexmk`/`biber` in dieser Umgebung nicht verfügbar); Umfang geschätzt 9–10 Seiten bei 4.870 Wörtern, Kapitelanteile 11,9 / 75,5 / 12,6 % im Soll. `check_formalia.py` 0 FEHLER, `check_bib_keys.py` 6/6 Keys valide, `check_status.py` 0 FEHLER.

**Nächster Schritt**: neue Session, „Schreib-Modus: Überarbeitung nach Prüfbericht" — die fünf Befunde in der Reihenfolge der Handlungsempfehlungen abarbeiten (größter Hebel zuerst: Mitigationssatz im Fazit). Danach genügt ein Delta-Re-Audit, kein neuer Voll-Audit. Erwarteter Score nach vollständiger Umsetzung: 100/100. Nicht in der Audit-Session weiterschreiben — `pruefbericht.md` trägt alles Nötige hinüber.

**Überarbeitung nach Prüfbericht abgeschlossen (2026-07-26, diese Session).** Alle fünf Befunde aus `pruefbericht.md` in der Reihenfolge der Handlungsempfehlungen umgesetzt, keiner betraf These oder Kernargumente, daher ohne `AskUserQuestion`-Rückfrage: **Befund 5** — Mitigationssatz in `02_limitationen.tex:5` ergänzt, gegenfinanziert durch Streichung des Pointen-Schlusssatzes am Kapitelende. **Befund 3** — Widerspruchssatz `01_wettbewerbsanalyse.tex:36` umformuliert, verneint die Kernthese nicht mehr, löst zugleich „USP" auf. **Befund 4** — `07_iteratives_design.tex:3` in den Planungsmodus gesetzt (dieser Bericht ist nicht mehr Akteur der künftigen Design-Phase), „im weiteren" gestrichen. **Befund 1** — alle elf narrativen Zitate (`03_konzept.tex`, `04_community_und_feedback.tex`, `01_kernergebnisse_und_ausblick.tex`, `02_limitationen.tex`) von `\parencite` auf `\textcite` umgestellt, doppelte Namensnennung jeweils gestrichen. **Befund 2** — `\acro{BMEL}` und `\acro{SDG}` in `pages/acronyms.tex` ergänzt (Klammer-Argument auf `[BMEL]` als längste Abkürzung aktualisiert), alle neun BMEL- und beide SDG-Fundstellen über `chapters/` auf `\ac{}` umgestellt. `check_formalia.py`: 0 FEHLER, 11 HINWEIS (bekannte Tabellen-/Satzlängen-Artefakte, ein Hinweis weniger als vor der Überarbeitung). `check_bib_keys.py --dir chapters/`: alle 6 Keys valide. `check_status.py`: 0 FEHLER, 9 HINWEIS (bekannte `DATEI-OHNE-ZEILE`-Hinweise, projekttypische Konvention). Kein neuer Voll-Audit gefahren — laut Handlungsempfehlung genügt ein Delta-Re-Audit.

**Delta-Re-Audit durchgeführt (2026-07-26, Score 100/100 — abgabereif).** Geprüft wurde der Delta der Überarbeitung (Commit `1843fa8`, 9 Dateien), beide Skripte vollständig, `check_status.py`, Teil-Check D und die Pflichtregeln der berührten Kapitel. Alle fünf Befunde verifiziert behoben: elf `\textcite`-Stellen vollständig und mit erhaltenen Seitenangaben umgestellt (die fünf legitimen `\parencite` unverändert), BMEL/SDG durchgängig über `\ac{}` mit korrekter Erstnennungs-Reihenfolge und „USP" restlos aufgelöst, der Widerspruchssatz zeigt jetzt in dieselbe Richtung wie Z. 34 und der Folgesatz, `07_iteratives_design.tex:3` steht im Planungsmodus ohne Akteurs-Fehler, Limitation 2 ist dreischichtig. Keine Regression: 0 FEHLER und 11 HINWEIS im Formalia-Skript (einer weniger als vorher, kein neuer), 6/6 BBT-Keys valide, alle 14 `\autoref`-Ziele existieren, keine Datei unter der HALBSEITE-Schwelle (Minimum 159 Wörter), alle Zählangaben der geänderten Stellen nachgezählt. Erstmals angewandt: die Vorbericht-Lehre, dass Streichungen Pflicht-Schichten mitreißen können — die einzige Streichung (Pointen-Schlusssatz) ist gegen alle sieben Pflichtregeln gegengeprüft, kein Verlust. Umfang netto **+20 Wörter** (4.890), Schätzung unverändert 9–10 Seiten. Teil-Check D bleibt ÜBERSPRUNGEN (`lualatex`/`latexmk`/`biber`/`pdflatex` geprüft, alle nicht installiert), daher ist der Score als „alle prüfbaren Kriterien erfüllt" zu lesen, nicht als „alle Kriterien geprüft" — der Seitenumfang ist ein Pflichtkriterium, das allein im Build gemessen wird. Vier kosmetische Hinweise ohne Score-Wirkung im Bericht (`\ac{}`-Erstnennung im Bindestrich-Kompositum bei BMEL und MVP, wiederholter Mitigations-Öffner in `02_limitationen.tex`, doppeltes „damit", gemischte Modalität in `07_iteratives_design.tex:3`).

**Skill-Update abgeglichen, drei Funde behoben (2026-07-28, diese Session).** Nach Anpassung von Skills und CLAUDE.md die neuen Skripte gegen den Textstand laufen lassen. (1) **Regression durch die Template-Updates** (`e06cd8e`/`f9c06c7`): `\listoffigures` und `\listoftables` in `main.tex` waren wieder auskommentiert, obwohl 5 Abbildungen und 4 Tabellen im Text stehen (ab 3 Pflicht) — derselbe Fix wie am 23.07., erneut nötig. Zulässige Änderung, da reines Schalten der markierten Aktivierungsblöcke. (2) Geviertstrich in `pages/appendix.tex:41` (innerhalb der zitierten Prompt-Zeile für `fig:mockup_profil`) durch Halbgeviertstrich ersetzt. (3) Feld **„Autorenschaft: Einzelarbeit"** in `aufgabe.md` ergänzt — das neue `typen/projektbericht.md` bindet den Selbstbezug daran, und `check_formalia.py` prüft „Projektgruppe" erst, wenn das Feld gesetzt ist. `check_formalia.py`: **0 FEHLER**, 36 HINWEIS.

**Abbildungs-Annotation gegen den aktuellen Zitierleitfaden geprüft und korrigiert (2026-07-28, diese Session).** Nutzer-Hinweis, dass die Leitfaden-PDFs ausgetauscht wurden — `git status` bestätigt: `Zitierleitfaden.pdf` (jetzt **Stand 01.10.2025**) und `Richtlinien für die Gestaltung wissenschaftlicher Arbeiten.pdf` modifiziert, neu hinzugekommen `sources/Richtlinie für die Nutzung KI-gestützter Systeme im Studium.pdf`. Alle drei neu extrahiert. Maßgeblich ist Zitierleitfaden Kap. 2.2.5 (S. 19–24): Beschriftung über der Abb./Tab. mit „Abb. N"/„Tab. N", Quellenzeile darunter nach dem Muster `Quelle: [Art der Erstellung] [Kurzbeleg] [ggf. Lizenz]`, KI-Bilder als `Erstellt mit dem Prompt „…" durch <Unternehmen>, <Jahr>` (Beispiel S. 24: „durch Microsoft, 2024. Microsoft 365-Lizenz."). Die neue KI-Richtlinie verweist für Bildquellen ausdrücklich auf den Zitierleitfaden zurück und legt für Texte „keine explizite Kennzeichnungspflicht" fest. **Korrigiert:** In allen vier Mockup-Quellenzeilen (`pages/appendix.tex`) stand „durch Claude Design (Claude Sonnet 5)" — also das Werkzeug statt des anbietenden Unternehmens und ohne Jahr; jetzt „durch Anthropic, 2026 (Claude Design, Modell Claude Sonnet 5)." Der projekteigene Zusatz „Abbildung nachträglich manuell bearbeitet." bleibt. Layout, `\quelle{}`-Makro, „Eigene Darstellung." bei den drei Eigenwerken und die Caption-Reihenfolge waren bereits regelkonform. `check_formalia.py`: 0 FEHLER. **Offen (Nutzer-Entscheidung):** Lizenzangabe (im Muster „ggf.", im KI-Beispiel gesetzt) bewusst weggelassen, da das Abo-Modell nicht bekannt ist; sowie die Frage, ob KI-Mockups zusätzlich ins Literaturverzeichnis gehören — der Leitfaden verweist dafür auf „Kapitel 2.3.7", dort steht aber Multimedia (Software-Beispiel: 2.3.9), der Querverweis ist im Original fehlerhaft. Lesart hier: kein Bib-Eintrag, weil KI als Hilfsmittel und nicht als Untersuchungsgegenstand genutzt wurde (Zitierleitfaden S. 4).

**Umfang erstmals gemessen statt geschätzt (2026-07-28):** `check_umfang.py` (neu) meldet **4.683 Wörter ≈ 12,5 Seiten** gegen die Vorgabe 7–10 (`aufgabe.md`) — die bisherige Projektschätzung „9–10 Seiten" war zu optimistisch, Abbildungen und Tabellen sind darin noch nicht enthalten. Kapitelanteile: Einleitung 579 / Durchführung 3.507 / Fazit 597 Wörter. Damit ist der Delta-Re-Audit vom 26.07. (100/100) überholt: Der neue `pruef-modus` deckelt den Score bei 89, solange der Umfang ungeprüft bzw. außerhalb der Vorgabe liegt.

**Kürzungsrunde durchgeführt (2026-07-28, diese Session) — Textteil jetzt gemessen 10 Seiten, Vorgabe 7–10 erfüllt.** Erstmals stand `lualatex`/`latexmk`/`biber` in der Umgebung zur Verfügung, der Umfang wurde deshalb **am gebauten PDF gemessen statt geschätzt**. Ausgangsmessung: **14 Seiten** Textteil (arabisch S. 1–14) bei 4.683 Wörtern — deutlich schlechter als die Heuristik von `check_umfang.py` (12,5 S.). Endstand: **10 Seiten, 3.637 Wörter** (Einleitung 521 / Durchführung 2.690 / Fazit 426 = 14,3 / 74,0 / 11,7 % — alle drei Anteile im Soll).

Drei Hebel, in dieser Reihenfolge wirksam:
1. **Template-Regression behoben** (größter Einzelposten, ohne jeden Textverlust): `pages/chapters.tex` trug wieder `\pagebreak` zwischen den Hauptkapiteln — genau der Fall, den `PERSISTENT.md` → `[LERNEN:sonstiges]` 2026-07-22 ausdrücklich verbietet und zur Entfernung anweist. Die Seitenmessung belegte den Schaden: eine Seite mit 169, eine weitere mit **13 Wörtern**. Entfernt; `\blankpage` und `\pagenumbering{arabic}` vor Kapitel 1 bleiben. Gleiche Regressionsklasse wie das ausgeknipste Abbildungsverzeichnis am selben Tag.
2. **Redundanzkürzung über alle 12 Subsections** (~1.000 Wörter): mehrfach ausformulierte Kernbefunde (die leftovercooking-Einordnung stand dreimal), Kontrastfiguren als Absatzschluss, Wegweiser-Absätze in den drei Kapitel-Mastern (dupliziert `03_vorgehen_und_aufbau.tex`), die als Aufzählung wiederholten drei Gegenargumente in `01_wettbewerbsanalyse.tex` (alle drei Fakten stehen unverändert in den Plattform-Absätzen; der gewichtigste Einwand samt Entkräftung bleibt ausformuliert), die dritte „Lehre" in `06_evaluation_reflexion.tex` (die Namenskollision selbst bleibt im Reflexionsabsatz erzählt, „drei Lehren" → „zwei Lehren" mitgeführt).
3. **`fig:konzeptfluss` gestrichen** (nach Rückfrage per `AskUserQuestion`): Das Tikz-Flussdiagramm gab den umgebenden Fließtext wieder, war **an keiner Stelle per `\autoref` referenziert** (also auch formal ein Float ohne Textverweis) und ist kein Lieferobjekt laut `aufgabe.md`. Abbildungs- und Tabellenverzeichnis bleiben mit je 4 Einträgen über der Pflichtschwelle von 3.

Ausdrücklich **nicht** gekürzt, nach Nutzer-Rückfrage: Wettbewerbsvergleich bleibt bei **sechs** Plattformen. Begründung: nur ~90 Wörter Ertrag, aber `02_limitationen.tex` führt „sechs Vertreter aus drei Plattform-Kategorien" als Stärke der Auswahl, und das gesamte Alleinstellungsmerkmal ist gegen leftovercooking formuliert. Ebenfalls unangetastet: die drei Subsections nahe der Halbseiten-Schwelle (`02_zielsetzung` 152, `03_vorgehen` 152, `07_iteratives_design` 148 Wörter). Alle Pflichtschichten geprüft erhalten: Limitationen dreischichtig, Phasenplanung als Tabelle, Syntheseleistung je Kapitel, Abhakliste-Schlüsselbegriffe (Event-Kalender-Begründung, Journaling/Menüplanung, Gruppen/Foren, Rezept-Upload, Bewertungs-/Kommentarsystem, personalisierte Empfehlungen), alle Lieferobjekte. `check_formalia.py` über `chapters/` + `pages/`: **0 FEHLER**, 52 HINWEIS. `check_bib_keys.py`: 6/6 valide. `check_status.py`: 0 FEHLER.

**Nächster Schritt: Delta-Re-Audit in frischer Session** — der Score ist neu zu erheben (der letzte, 100/100 vom 26.07., bezog sich auf den ungekürzten Textstand und ist damit überholt). Teil-Check D (Build) ist erstmals durchführbar und nicht mehr zu überspringen.

**Danach reine Nutzer-Schritte vor der Einreichung** (siehe `pruefbericht.md` → „Vor der Einreichung"): Eidesstattliche Erklärung über myCampus, LanguageTool-Durchgang, Mockup-Nachbesserung zu Befund 6.3. **Erledigt am 2026-07-28:** lokaler Build und Seitenumfang-Bestätigung — `latexmk -lualatex main.tex` läuft fehlerfrei durch, Textteil 10 Seiten, damit innerhalb 7–10. Zusätzlich offen: `\ModuleCode` und `\MatrikelNr` in `pages/meta.tex` sind noch Platzhalter und erscheinen so auf dem Titelblatt.

**AENDERUNGEN.md Runde 7 angelegt (2026-07-28, Methodenfundierung Beta-Test) — „Offen" ist damit nicht mehr leer.** Anlass: Abgleich der eigenen Vorprojektarbeit (`sources/vorherige-projektarbeit/`, Volltext `/home/normi/Documents/projekt-ui-design/latex/`) gegen den Resteria-Stand. Dort sind Usability-Tests real durchgeführt und methodisch belegt (ISO 9241-210, Think-Aloud nach Nielsen, SEQ nach Sauro & Lewis, Stichprobengröße nach Erlhofer); hier ist der Beta-Test in `07_iteratives_design.tex:5` und `05_phasenplanung.tex:14` rein organisatorisch beschrieben — ohne Verfahren, Erfolgskriterium oder Begründung der Gruppengröße. Fünf Befunde: 7.1 SEQ als messbares Kriterium, 7.2 Think-Aloud als benanntes Verfahren, 7.3 Gruppengröße begründen, 7.4 `[FREIGABE]` ISO-9241-210-Prozessrahmen (drei Optionen, inkl. Verwerfen), 7.5 Gegenfinanzierung über eine Dublette zwischen Iterations- und Phasenkapitel. Netto rund +60 Wörter bei 10 Seiten Textstand — **vor dem Commit neu bauen**, bei 11 Seiten zuerst 7.4 verwerfen. Norman und Tidwell bewusst ausgeschlossen (deuten in der Vorarbeit beobachtetes Nutzerverhalten, das hier fehlt). Die Runde enthält einen verbindlichen Abschnitt „Eigenplagiat-Grenze": keine Satzübernahme aus der Vorarbeit (die drei kritischen Stellen sind benannt), keine Ergebnisübernahme (SEQ-Werte, Zeitmessungen, Testpersonen betreffen DocSecBox), keine Zitationspflicht der Vorarbeit, da ausschließlich fremde Primärquellen übernommen werden. **Einmalige Nutzer-Freigabe für `references.bib`** genutzt: vier Einträge (`sauroLewisQuantifying2016`, `nielsenUsabilityEngineering1993`, `erlhoferWebsiteKonzeptionUndRelaunch2017`, `iso_9241_210`) aus der Vorarbeits-Bib von Hand übernommen — **keine BBT-Exporte**, ein künftiger Zotero-Export löscht sie wieder. `check_bib_hygiene.py` meldet für sie elf Hinweise (abgekürzter Institutionenautor „DIN", `location` bei drei Büchern, fehlendes Sprachfeld bei allen vier), die vor Abgabe über Zotero zu bereinigen sind. `check_bib_keys.py`: 6/6 genutzte Keys valide, die vier neuen erwartungsgemäß als ungenutzt gemeldet. `check_status.py`: 0 FEHLER. **Nächster Schritt: „Schreib-Modus: AENDERUNGEN.md abarbeiten" in frischer Session** — 7.4 dort per `AskUserQuestion` klären. Danach Delta-Re-Audit.

**AENDERUNGEN.md Runde 7 (Methodenfundierung Beta-Test) abgearbeitet (2026-07-28, diese Session).** Vier der fünf Befunde umgesetzt, einer auf direkte Nutzeranweisung übersprungen: 7.1 (SEQ als messbares Erfolgskriterium, Schwelle Mittelwert ≥ 5), 7.2 (Think-Aloud-Verfahren für das moderierte Gespräch) und 7.3 (Gruppengröße über Erlhofer begründet, Richtungsfehler „erforderlich" statt „ausreichend" vermieden) als drei neue Sätze in `chapters/02_durchfuehrung/07_iteratives_design.tex:5` ergänzt, jeweils mit den drei von Hand eingetragenen Bib-Quellen belegt. `\acro{SEQ}{Single Ease Question}` in `pages/acronyms.tex` ergänzt, da der Begriff zusätzlich in der Phasenplanungs-Tabelle vorkommt. 7.5 (Gegenfinanzierung): Dublette in `07_iteratives_design.tex:3` gestrichen („genau das begründet die Abhängigkeit…"), `tab:phasenplanung`-Zelle „Beta-Test & Feedback" von „Test mit kleiner Nutzergruppe, Fragebögen, Feedback-Gespräche" auf „Think-Aloud-Sitzungen, \ac{SEQ}-Fragebogen" umgestellt. **7.4 auf ausdrückliche Nutzeranweisung beim Sessionstart übersprungen** (Option C, kein neuer Prozessrahmen-Absatz) — keine `AskUserQuestion`-Rückfrage nötig, da die Entscheidung bereits direkt vorlag, nicht aus einer Auslassung stillschweigend abgeleitet wurde. `iso_9241_210` bleibt entsprechend ungenutzt in `references.bib`. `check_formalia.py` auf beiden Dateien: 0 FEHLER (nur bekannte HINWEIS-Artefakte plus ein neu identifizierter Skript-Blindfleck: `check_formalia.py`s `LOCATOR_RE` erkennt `[Kap.~8]`-Stellenangaben mit dem IU-korrekten geschützten Leerzeichen `~` vor der Ziffer nicht, meldet fälschlich SEITENANGABE-Hinweise — die Stellenangabe ist inhaltlich vorhanden, kein echter Regelverstoß). `check_bib_keys.py`: alle drei neu zitierten Keys valide. **Nicht maschinell geprüft:** Die drei neuen Zitationen haben keinen Volltext im Bib-Eintrag (`check_quellentreue.py` → NICHT PRÜFBAR); Details und Prüfauftrag in `AENDERUNGEN.md` Runde 7 → „Bitte manuell prüfen". Umfang: `check_umfang.py` 3.783 Wörter ≈ 10,1 Seiten gesamt — vor dem nächsten Build-Check die Seitenzahl neu messen, die Runde könnte die zuvor exakt bei 10 stehende Grenze in Richtung 11 kippen.

**`check_quellentreue.py` um E-Book-Unterstützung erweitert (2026-07-28, diese Session, auf Nutzerwunsch).** Bib-Einträge dürfen im `file`-Feld jetzt auch ein `.epub` tragen; statt eines PDF-Seitenversatzes wird das eigene NCX-Inhaltsverzeichnis des E-Books ausgewertet und `[Kap. X]`-Locators (z. B. „17.2.2") direkt gegen die dort hinterlegte Kapitelnummer aufgelöst – reine Stdlib (`zipfile`/`re`), kein neues Pip-Paket. Löst das Inhaltsverzeichnis die zitierte Unterebene nicht auf, greift dieselbe Ganzbuch-Fallback-Logik wie bei PDFs ohne bestimmbaren Seitenversatz (Prüfung bleibt möglich, nur nicht kapitelgenau). `references.bib` → `erlhoferWebsiteKonzeptionUndRelaunch2017` um `file = sources/literatur/Website-Konzeption…epub` ergänzt (fällt unter die bereits erteilte einmalige Freigabe für die vier Runde-7-Einträge). Getestet: `epub_kapitel()`/`resolve_kapitel()` direkt gegen die reale Datei (259 erkannte Kapitel, „17.2.2" exakt aufgelöst, „17.2.2.9" korrekt auf „17.2.2" zurückgefallen, unbekannte Nummer „99.9" korrekt in den Ganzbuch-Fallback gegangen, defektes Test-EPUB sauber als Exception abgefangen). Voller Lauf über `chapters/` bestätigt: Die Runde-7-Zitation `\textcite[Kap.~17.2.2]{erlhoferWebsiteKonzeptionUndRelaunch2017}` ist jetzt **NICHT PRÜFBAR → PRÜFEN** (`quellencheck.md`, Hash `c27b5ba809`) – kein FEHLER in `check_formalia.py`/`check_bib_hygiene.py`. **Nebenbefund beim Testen:** Die zitierte Aussage ist am Original verifiziert – Erlhofer, Kap. 17.2.2, Praxistipp „Fünf Test-User reichen meist": „Schon bei fünf Personen werden Sie feststellen, dass sich die meisten Usability-Probleme wiederholen und Sie damit die gravierenden Stolperstellen aufgedeckt haben." Deckt die Textaussage in `07_iteratives_design.tex:5` inhaltlich; damit ist Punkt 1 aus der „Bitte manuell prüfen"-Liste der Runde 7 für Erlhofer erledigt, Sauro & Lewis sowie Nielsen bleiben offen (kein epub/PDF für diese beiden im `file`-Feld hinterlegt).

**Nachtrag selbe Session (2026-07-28): Nutzer hat auch die Volltexte für Sauro & Lewis und Nielsen nachgelegt** (beides PDF, nicht epub, unter `sources/literatur/`) — `references.bib` um die passenden `file`-Felder ergänzt. Dabei zwei echte Bugs gefunden und behoben, keine kosmetischen: (1) `[Kap. X]`-Locators funktionierten bisher nur für epub-Quellen; eine PDF-Quelle mit `[Kap. X]` blieb fälschlich NICHT PRÜFBAR, obwohl ein PDF vorlag. Fix: PDF+Kapitel-Locator fällt jetzt sauber auf eine Ganzdokument-Suche zurück (neuer `elif z.kapitel:`-Zweig in `pruefe()`, eigene Catch-all-Meldung ohne das sonst PDF-Seiten-spezifische `--offset`). (2) `pdf_pfad`/`epub_pfad` zerschnitten das `file`-Feld an jedem `;` (Zotero-Mehrdatei-Konvention) — die Anna's-Archive-Dateinamen enthalten aber selbst Semikolons („Jeff Sauro; Professor of Religious Studies…", „Morgan Kaufmann; Morgan Kaufmann"), wodurch der Pfad in unbrauchbare Fragmente zerfiel und beide Dateien trotz vorhandenem `file`-Feld als „weder PDF noch URL" galten. Fix: `_datei_kandidaten()` versucht das ganze Feld zuerst unverändert, bevor es nach Zotero-Konvention aufteilt. Nach dem Fix lösen beide Pfade korrekt auf. **Volltextprüfung:** Nielsen Kap. 6.8 („Thinking Aloud", S. 195) deckt die Textaussage vollständig ab. Sauro & Lewis Kap. 8: die SEQ selbst (siebenstufige Skala) ist exakt belegt, die konkrete Schwelle „Mittelwert ≥ 5" fand sich in der Stichwortsuche **nicht** als explizit formulierte Regel — bleibt als offener Punkt in `AENDERUNGEN.md` Runde 7 → „Bitte manuell prüfen" stehen. Voller Lauf über `chapters/`: 0 NICHT PRÜFBAR mehr (vorher 2), `check_formalia.py`/`check_bib_hygiene.py`/`check_bib_keys.py` weiterhin 0 FEHLER.

**Nachtrag auf Nutzerwunsch „nochmal nachlesen" (2026-07-28, diese Session):** Alle 41 SEQ-Fundstellen im Sauro-&-Lewis-Volltext einzeln durchgesehen (nicht nur der Definitionsabschnitt) — die Schwelle „Mittelwert von mindestens fünf" steht an keiner Stelle als Regel; die einzige „5" im SEQ-Kontext betrifft die 5- vs. 7-Punkte-Skalenvariante, eine spätere Übungsaufgabe („average of 5.6 … greater than 5?") ist ein reines Statistikbeispiel ohne Bezug zu einem SEQ-Erfolgskriterium. **Entschärft:** `chapters/02_durchfuehrung/07_iteratives_design.tex:5` — „Als Schwelle für eine gelungene Aufgabe gilt dabei ein Mittelwert von mindestens fünf." → „Als Schwelle für eine gelungene Aufgabe legt diese Arbeit einen Mittelwert von mindestens fünf fest." Der `\parencite[Kap.~8]{sauroLewisQuantifying2016}` bleibt am SEQ-Definitionssatz stehen (dort korrekt belegt); die Schwelle selbst ist jetzt explizit als eigene methodische Festlegung ausgewiesen, nicht mehr implizit Sauro & Lewis zugeschrieben. `check_formalia.py`: weiterhin 0 FEHLER, `check_umfang.py`: +1 Wort (vernachlässigbar). Damit ist der letzte offene Punkt aus der „Bitte manuell prüfen"-Liste der Runde 7 geschlossen.

**Kleine Kürzungsrunde (2026-07-28, diese Session):** Nutzer meldete 10,3 Seiten aus einem lokalen Build (Runde 7 stand bei geschätzten 10,1 S. laut `check_umfang.py`, also war real schon mehr Overflow da als die Heuristik zeigte). Acht rein redundanzbasierte Kürzungen ohne jeden Bezug zu These, Kernargumenten oder bereits per Freigabe/Audit fixierten Formulierungen (Widerspruchssatz `01_wettbewerbsanalyse.tex:34`, SWOT-Satz, Freemium-Begründung, Instagram/TikTok-Stellvertreter-Regelung — alles unangetastet): (1) `06_evaluation_reflexion.tex` — Dublette „Challenge-Banner und Einsparangabe" gestrichen, die wortgleich schon in `07_iteratives_design.tex:3` (unmittelbar davor gelesen) steht; (2) ebd. — generischer Reflexionssatz „mehr Zeit für Vergleichsplattformen" gestrichen (Absatz behält zwei konkrete, belegte Kursänderungen); (3) ebd. — Teilaufgaben-/Berichtsinhalte-Checkliste sprachlich verdichtet (alle `\autoref`s erhalten); (4) ebd. — „Zwei Lehren"-Absatz gestrafft; (5) `05_phasenplanung.tex` — Tabellenankündigungssatz verdichtet; (6) ebd. — selbsterklärende Abhängigkeits-Begründung „da sich Nutzer:innen sonst nicht sinnvoll äußern können" gestrichen (Kernaussage „Beta-Test setzt MVP voraus" bleibt); (7) `01_wettbewerbsanalyse.tex` — abstrakte Lücken-Vorwegnahme gestrichen, die der unmittelbar folgende Satz ohnehin konkret ausführt; (8) `03_konzept.tex` — vorwegnehmender Navigations-Satz gestrichen, der in Z. 34 mit den drei Bereichen ausführlich wiederholt wird. Bei zwei Edits (Rückblickend-Satz, Navigation-Satz) riss die Ersetzung versehentlich die Absatzleerzeile mit heraus — beide direkt nachkorrigiert und per `git diff` verifiziert. Ergebnis: **3.784 → 3.703 Wörter** (`check_umfang.py`: 9,9 statt 10,1 S., Kapitel 2 allein 2.749 W.). `check_formalia.py`: 0 FEHLER (43 HINWEIS, ausschließlich bekannte Artefakte). `check_bib_keys.py`/`check_status.py`: unverändert ok. Kompiliert wurde bewusst **nicht** selbst (Grundprinzip „Kompilieren ist Sache des Nutzers") — die 81 Wörter Ersparnis liegen nach dem alten Ankerwert 3.637 W. ≈ 10,0 S. (echter Build, große Kürzungsrunde) in der Größenordnung, die für „unter 10 Seiten" nötig war, aber ob es angesichts der Lumpigkeit von Seitenumbrüchen reicht, zeigt erst der nächste lokale Build.

**Kürzungsrunde 2, selbe Session (2026-07-28):** Nutzer-Rückmeldung nach Runde 1: lokaler Build bricht weiterhin auf 11 Seiten um. Vor weiteren Kürzungen zuerst auf eine Strukturregression geprüft (Grund für den großen Sprung 14→10 S. in der ursprünglichen Kürzungsrunde war ein wiedereingeschlichener `\pagebreak`) — `pages/chapters.tex` und `main.tex` diesmal sauber, kein `\pagebreak`, keine Regression; der Rückstand ist real inhaltlich (Runde 7 vom selben Tag hatte ~150 Wörter Methodenfundierung ergänzt, die offenbar genau die Seite kippen ließ). Sieben weitere Kürzungen, wieder ausschließlich Prosa-Verdichtung ohne Faktenverlust, keine Zitation/kein `\autoref` entfernt: `07_iteratives_design.tex` Übergangssatz zur Design-Phase verschmolzen, Beta-Test-Methodik-Absatz gestrafft (SEQ/Think-Aloud/Erlhofer-Belege, Stichprobenbegründung, Disclaimer-Satz alle erhalten) · `03_konzept.tex` Cafeteria-Beispiel bei der Namensherleitung gestrichen · `05_phasenplanung.tex` „Das MVP ist dabei..." verdichtet · `01_wettbewerbsanalyse.tex` Einwand-Einleitung verdichtet (der audit-fixierte Schlusssatz zum Alleinstellungsmerkmal blieb unangetastet) sowie die leftovercooking-Beschreibung verdichtet (jede Einzelaussage — KI-Vorschläge, Food-Creator:innen, Punktesystem-Einordnung — erhalten) · `01_kernergebnisse_und_ausblick.tex` dritte Wiederholung von „Verfolgen von Creator-Inhalten" gestrichen (stand bereits zweimal in der Wettbewerbsanalyse). **Ergebnis: 3.784 → 3.654 Wörter** (`check_umfang.py`: 9,7 S.), praktisch auf dem alten Ankerwert 3.637 W., der beim letzten echten Build exakt 10 Seiten ergab. `check_all.py --kapitel chapters/02_durchfuehrung`: 0 FEHLER (die einzelne `check_formalia.py`-FEHLER-Meldung bei isoliertem Lauf auf nur `chapters/` war ein Scope-Artefakt — Abbildungen liegen in `pages/appendix.tex`, das der Einzelaufruf nicht sah; mit `check_all.py` sauber). Kompiliert wurde weiterhin bewusst nicht selbst.

**Nachtrag zum Delta-Re-Audit (2026-07-28): Der erste Durchgang war falsch, Teil-Check G wurde nach Nutzer-Einwand vollständig nachgeholt.** Ursprünglich meldete der Audit „sechs Quellen ohne Volltext-Snapshot (−15)" und stützte sich dabei ungeprüft auf `check_quellentreue.py`. Der Nutzer fragte nach, ob `sources/literatur/` vielleicht in `.gitignore` stehe — das war der entscheidende Hinweis. Tatsächlich lag der Ordner offen da, mit den PDFs zu Barker, Soma und Vittuari. Deren `file`-Feld war gesetzt, zeigte aber auf die **lokale Zotero-Ablage des Nutzers** (`/home/normi/Zotero/storage/…`), die es in der Session-Umgebung nicht gibt; das Skript meldet einen nicht auflösbaren Pfad wortgleich wie ein fehlendes Feld. Lehre: Vor einem „Quelle nicht prüfbar"-Befund gehört ein `ls sources/literatur/` — das Grundprinzip „Erst prüfen, dann behaupten" gilt auch für Skriptausgaben. Als **P1-1 und P1-2** in `SKILL-ANPASSUNGEN.md` dokumentiert (die Datei wurde auf Nutzerwunsch komplett ersetzt, die alten Punkte sind abgearbeitet). Zwischenlösung: Die drei `file`-Felder tragen jetzt zusätzlich den repo-relativen Pfad nach Zoteros Mehrdatei-Konvention (`<Zotero-Pfad>;sources/literatur/<datei>.pdf`) — der nächste BBT-Export überschreibt das wieder.

**Ergebnis der nachgeholten Volltextprüfung: 10 von 19 Zitationen mit OK quittiert, drei echte Fundstellen-Befunde gefunden**, die der Fehlalarm verdeckt hatte. Bestätigt wurden die beiden tragendsten Zahlenaussagen der Arbeit: Soma S. 9, Tabelle 3 belegt 1,37 → 0,67 kg/Woche/Person bei Vielspieler:innen (51 %) gegen 1,64 → 1,00 bei Wenigspieler:innen (39 %) mit Mann-Whitney U p = 0,030 bei Audit 2 — Prozentwerte und Signifikanzaussage in `03_konzept.tex:11` stimmen exakt; Barker S. 10 nennt die vier als reliabel eingestuften Studien, darunter einen visuellen Reminder mit signifikantem Effekt. Neu gefunden: **(1)** `03_konzept.tex:5` und `01_kernergebnisse_und_ausblick.tex:5` zitieren Vittuari mit `[S. 1]`, während der Bib-Eintrag die Publikation als S. 104–114 ausweist — die „1" stammt aus der White-Rose-Preprint-Zählung, die Arbeit mischt also zwei Seitensysteme (in der Einleitung steht `[S. 106]`, Verlagszählung). Inhaltlich einwandfrei, aber nicht nachschlagbar. **(2)** `04_community_und_feedback.tex:7` belegt den Teilnahme-Claim (passive und Gamification-Gruppe gegen Community-Gruppe) mit `[S. 9]`; die Aussage steht fast wörtlich im Abstract, die Zahlen aber in Abschnitt 3.2 auf S. 10–12 — auf S. 9 liegt Tabelle 3. Diese Stelle trägt die Begründung für den Verzicht auf Forum und Gruppen. **(3)** Die schon vorher gefundene unbelegte Autorennennung in `06_evaluation_reflexion.tex:7`.

**Delta-Re-Audit durchgeführt (2026-07-28, Score 43/100 — nicht in diesem Stand einreichen).** Geprüft wurde der gesamte Textstand nach den beiden Kürzungsrunden (Commit `1f188dc`), da sich seit dem letzten Audit praktisch jede Kapiteldatei geändert hat; dazu alle sechs Skripte, die Mockups per Bildsichtung und das eingecheckte `main.pdf`. Der Score 100/100 vom 26.07. ist damit abgelöst — nicht weil der Text schlechter geworden wäre, sondern weil Seitenumfang und Quellentreue erstmals wirklich geprüft statt geschätzt wurden. **Alle fünf Abzüge sind mechanisch**: drei falsche Seitenangaben, rund 100 Wörter zu viel, eine fehlende Zitation, drei fehlende Volltexte. Kein Befund betrifft These, Argumentation oder Aufbau. Nach Befund 1–4 liegt der Score bei **85/100**; die verbleibenden −15 hängen allein an den drei Quellen ohne Volltext (Nivedhitha/Wiley, Shen/Elsevier, UN-Resolution — alle drei ohne `file`-Feld und live nicht abrufbar).

**Seitenumfang erstmals belastbar:** „1 Einleitung" steht auf PDF-Seite 6 (gedruckt 1), das Literaturverzeichnis auf PDF-Seite 17 (gedruckt 12) — Textteil **11 Seiten** gegen die Vorgabe 7–10. Das PDF wurde vorher als aktuell verifiziert (vier Textproben aus `e6e024f` stehen darin, die Vorgängerformulierungen nicht), Kürzungsrunde 2 hat die Seitenzahl also **nicht** unter 11 gedrückt. Die elfte Seite trägt nur 90 Wörter; fünf konkrete Kürzungsstellen stehen priorisiert in `pruefbericht.md`. `check_umfang.py` schätzte 9,8 Seiten — die Heuristik liegt systematisch zu niedrig, weil sie Abbildungen, Tabellen und Float-Umbrüche nicht kennt.

**Positiv verifiziert:** Befund 6.3 ist behoben (Plus-Button im Rettungs-Feed steht frei, überlappt die Aktionsleiste nicht mehr), das Wochenplan-Banner ist auf der Startseite sichtbar und deckt die Textaussage in `03_konzept.tex:13`, die „Foto"-Platzhalter tragen keinen Doppelpunkt mehr. Alle drei Limitationen weiter dreischichtig, alle Zählangaben nachgezählt, keine Kürzungsregression. `check_formalia.py`: 0 FEHLER, 58 HINWEIS. Teil-Check D lief über das vorhandene PDF, weil `lualatex`/`latexmk`/`biber` in dieser Session-Umgebung nicht installiert sind (anders als am Vormittag desselben Tages).

**Nächster Schritt: neue Session, „Schreib-Modus: Überarbeitung nach Prüfbericht"** — Befund 1 (zwei Seitenangaben, am Verlags-PDF gegenprüfen), Befund 3 (eine Seitenangabe), Befund 4 (ein Satz) und Befund 2 (rund 100 Wörter aus Kapitel 2), danach lokal bauen und die gedruckte Seitenzahl gegenprüfen. Befund 5 ist ein reiner Zotero-Schritt. Nicht in der Audit-Session weiterschreiben — `pruefbericht.md` trägt alles Nötige hinüber.

**Überarbeitung nach Prüfbericht abgeschlossen (2026-07-28, diese Session).** Vier der fünf Handlungsempfehlungen umgesetzt, Befund 5 bleibt reiner Zotero-Schritt. **Befund 1** — Vittuari `[S. 1]` in `03_konzept.tex:5` und `01_kernergebnisse_und_ausblick.tex:5` korrigiert: Die Verlagsfassung war nicht erreichbar (ScienceDirect/WUR-Repository liefern über den Proxy 403), stattdessen am vorliegenden Preprint-PDF (`sources/literatur/Vittuari2023_DriversLeversFoodWaste.pdf`) verifiziert, dass die Drivers-/Levers-Definition in Kapitel 1, Absatz 3 steht; beide Stellen auf `[Kap.~1, Abs.~3]` umgestellt (Locator-Konvention für nicht verlässlich verifizierbare Seitenzahlen, `hard-rules-formal.md`). Dieselbe Textstelle deckt auch den bislang nur als „bitte manuell prüfen" geführten Haushalts-Claim in `01_ausgangslage_und_problem.tex:5` — `[S. 106]` war ebenfalls unverifizierbar (`check_quellentreue.py` meldete SEITE VERDÄCHTIG, da S. 106 außerhalb des 23-seitigen Preprints liegt) und wurde als dritte, im Prüfbericht nur unter „Bitte manuell prüfen" geführte Fundstelle mitkorrigiert. **Befund 3** — Soma-Zitation in `04_community_und_feedback.tex:7` von `[S. 9]` auf `[S. 10--12]` korrigiert; am Volltext verifiziert (Teilnahmequoten Information/Community/Gamification stehen exakt auf S. 10–12, S. 9 trägt nur Tabelle 3 mit den Abfallmengen). **Befund 4** — unbelegte Autorennennung „nach dem Befund von Soma et al." in `06_evaluation_reflexion.tex:7` gestrichen, durch Rückverweis `~\autoref{sec:community_und_feedback}` ersetzt (die kürzere der beiden im Prüfbericht vorgeschlagenen Varianten). **Befund 2** — fünf der sechs vorgeschlagenen Kürzungsstellen umgesetzt (TikTok/Instagram-Satz verschmolzen, Nudge-Doppeldefinition gestrichen, MVP-Abhängigkeitsbegründung gestrafft, „User Interface und User Experience" → „UI/UX", Kontrastsatz in `02_zielgruppe.tex` gestrichen) plus eine zusätzliche Redundanz: das Cafeteria-Beispiel in der Namensherleitung (`03_konzept.tex:3`), das laut Statuszeile vom 28.07. bereits einmal als gestrichen protokolliert war, im Dateibestand aber noch vorhanden war — Dateien sind Wahrheit, nachgeholt und hier richtiggestellt. Ergebnis: **3.658 → 3.598 Wörter** (`check_umfang.py`). Kein lokaler Build in dieser Session-Umgebung möglich (`lualatex`/`latexmk`/`biber` weiterhin nicht installiert) — die reale Seitenzahl bleibt bis zum nächsten Build unbestätigt; die Ersparnis liegt unter dem im Prüfbericht angepeilten Korridor von 90–120 Wörtern (Wortzahl auf dem zuvor überlaufenden elften Blatt), **vor Abgabe daher unbedingt neu bauen**. **Befund 5** (drei Quellen ohne Volltext) bewusst nicht angefasst — reiner Zotero-Schritt, in dieser Umgebung kein Volltext beschaffbar (Wiley-/Elsevier-Paywalls, kein UN-Snapshot; auch der direkte PDF-Download der Vittuari-Verlagsfassung scheiterte am Proxy). Nebenbefund: `pdfplumber`/`pypdf` waren zu Sessionbeginn nicht installiert und wurden nachinstalliert (`pip install pypdf`), um die Vittuari- und Soma-Volltexte gegenzuprüfen — nur für diese Session wirksam, nicht Teil des Repositorys. `check_formalia.py` über `chapters/` + `pages/`: 0 FEHLER, 57 HINWEIS (keine neuen). `check_bib_keys.py`: alle Keys valide. `check_quellentreue.py --datei chapters/`: alle vier neu/geänderten Zitationen mit OK quittiert (Notizen in `quellencheck.md`/`quellencheck-state.json`), die fünf LIVE-PRÜFEN-Einträge aus Befund 5 unverändert offen. Freigabepflicht geprüft: Keiner der fünf Befunde berührt These, Kernargumente oder Leitfrage (ausschließlich Zitat-Locatoren, Redundanzkürzung, Zitierformat) — keine `AskUserQuestion`-Rückfrage nötig, deckt sich mit der eigenen Einschätzung des Prüfberichts. **Nächster Schritt**: Nutzer-Schritt lokaler Build (`latexmk -lualatex main.tex`) zur Seitenzahl-Bestätigung, danach Delta-Re-Audit („Prüf-Modus"). Bei weiterhin über 10 Seiten: Rückfrage, ob die Wettbewerbsanalyse (sechs Plattformen) diesmal doch gekürzt werden soll, oder gezielt einen der bislang unangetasteten, inhaltlich dichten Absätze kürzen (`07_iteratives_design.tex`, `04_community_und_feedback.tex` Absatz 3, `01_wettbewerbsanalyse.tex` Absatz 9 – alle drei bewusst verschont, weil audit-relevant bzw. jüngst gestärkt).

**Quellenzugang geklärt, Skills nachgezogen (2026-07-29, diese Session).** Anlass war ein wiederkehrendes Muster: Über die Zitierfähigkeit entschied bisher Teil-Check G ganz am Ende, ausgewählt wird die Quelle aber in Plan-Modus Schritt 1 ganz am Anfang – eine Bezahlschranke fiel deshalb erst auf, wenn das Argument schon auf der Quelle stand. `SKILL-ANPASSUNGEN.md` komplett ersetzt (fünf Punkte, alle umgesetzt): **Beschaffbarkeits-Gate** in Plan-Modus Schritt 1 mit drei Urteilen (VOLLTEXT LIEGT VOR / BESCHAFFBAR (Route) / KEIN ZUGANG) und einer Routenleiter (Projektablage → freie Fassung per OpenAlex-DOI-Abfrage → IU-Bibliothek → Nutzer-Beschaffung); bei Büchern ist ein fehlendes PDF ausdrücklich **kein** Blocker, bei Journal-Artikeln ohne Zugangsweg schon. `check_quellentreue.py` unterscheidet jetzt `VOLLTEXT BESCHAFFBAR` (Buch/Bibliothek/OA – außerhalb des Abgabe-Audits ohne Abzug) von `ZUGANG PRÜFEN` (Bezahlschranken-Host, −5 wie bisher), Heuristik über Eintragstyp plus Hostliste, Override über `tex.zugang` im Zotero-Extra-Feld. Volltext-Status wird durch Plan (`[zitiert: <key>, Volltext: …]`, Literaturliste mit Spalten *Volltext* und *gelesene Fassung*) und Schreib-Modus (Punkt 3b: bei fehlendem Volltext **nicht** aus dem Abstract formulieren) mitgeführt; Snapshot-Pflicht beim ersten Zugriff statt vor der Abgabe. Ordnername auf `sources/literatur/` vereinheitlicht (13 Fundstellen in vier Skills, `projektstruktur.md`, `CLAUDE.md`) – die Skills nannten durchgängig `literature/`, der reale Ordner heißt deutsch. 98 Skript-Tests grün, `check_all.py` ohne harte Funde.

**Befund 5 des Delta-Re-Audits (drei Quellen ohne Volltext, −15) ist damit weitgehend erledigt.** Nutzer hat Nivedhitha und Shen als PDF beschafft (in `sources/literatur/` nach Konvention umbenannt), die UN-Resolution wurde nach Freigabe als offizieller Snapshot von digitallibrary.un.org gezogen (`UN2015_Agenda2030.pdf`, 35 S.). `references.bib` trug für alle drei per **einmaliger Nutzer-Freigabe** ein repo-relatives `file`-Feld nach. **Erledigt am 2026-07-29:** Der Nutzer hat die drei PDFs zusätzlich in Zotero an die Einträge gehängt (`storage/DQBFC7TU`, `JY5BSEPP`, `ZT3RIJDQ`). Der nächste BBT-Export ersetzt die manuellen Pfade damit durch Zoteros absolute – **und das ist hier unschädlich**: `/home/normi/Zotero/storage/` existiert in der Session-Umgebung und löst auf, wie die bereits so eingetragenen Barker-, Soma- und Vittuari-Einträge zeigen. Die frühere Warnung, ein Re-Export werfe die Zitationen zurück auf „nicht prüfbar", galt für eine Umgebung ohne diese Ablage (Container, zweiter Rechner) und trifft auf diesen Arbeitsplatz nicht zu. **Einziger verbleibender Vorbehalt:** Die Kopien in `sources/literatur/` tragen die Projektkonvention (`Nivedhitha2024_…pdf`), Zotero benennt anders (`Nivedhitha et al. - 2024 - How green social…pdf`). Auf einem anderen Rechner griffe der Basename-Rückfall des Skripts deshalb nicht; wer dort prüfen will, benennt die Kopien vorher wie Zotero. Alle fünf zuvor unprüfbaren Zitationen sind seither maschinell prüfbar; zwei am Volltext bestätigt: **UN** (SDG 12.3 wörtlich auf S. 22, deckungsgleich) und **Shen** (Browsing wirkt „as if by osmosis" indirekt auf PEB, Pfad über *environmental concern* auch ohne eigene Teilnahme signifikant, β = 0,10, p < 0,001, H3a bestätigt – der Trägersatz bleibt mit „kann erhöhen" hinter der Quelle).

**Neuer inhaltlicher Befund aus der Volltextprüfung – Nivedhitha, drei Fundstellen, nach Freigabe behoben.** Der Volltext widerlegte, was die Arbeit ihm zuschreibt: `03_konzept.tex:9` sagt „\textcite{nivedhithaHowGreenSocial2024} zeigen zudem, dass Menschen umweltbewusstes Verhalten eher fortsetzen, wenn sie sich zunehmend selbst als umweltbewusst handelnde Person wahrnehmen". Die Studie **verwirft** genau diesen direkten Zusammenhang: „the direct effect of green self-identification on PEB is insignificant, as the 95% CI includes 0, thus rejecting H1" (S. 14). Der Effekt läuft **vollständig** über *collective intention*, also über ein Gemeinschaftsgefühl. Betroffen sind `03_konzept.tex:9`, die Zelle „Rettungspunkte & Reste-Level" in `tab:funktionsuebersicht` (`03_konzept.tex:22`) und `01_kernergebnisse_und_ausblick.tex:5`. Nach Freigabe per `AskUserQuestion` an allen drei Stellen umgestellt: Die Selbstwahrnehmung wirkt jetzt ausdrücklich über das Gemeinschaftsgefühl und der Titel im Zusammenspiel mit dem Rettungs-Feed, jeweils mit `[S. 14]` belegt (gedruckte Seitenzahl am Fußtext „14 of 29" verifiziert; der Eintrag trägt `pages = {e13038}`, also greift die `SEITE AUSSERHALB`-Prüfung hier nicht). Fachlich ist das ein Gewinn – der Titel steht nicht mehr allein. **Nebenwirkung, für künftige Sessions relevant:** Die Urteile in `quellencheck-state.json` hängen am ganzen Trägersatz, nicht an der einzelnen Zitation. Die drei Edits haben deshalb fünf benachbarte, inhaltlich unveränderte Zitationen (Barker, Soma, Vittuari in derselben Tabellenzeile bzw. demselben Fazitsatz) mit-invalidiert; sie wurden mit Begründung wieder auf OK gesetzt. **Stand jetzt: 19 von 19 Zitationen OK, `check_all.py --mit-quellen` ohne harte Funde.** Umfang 3.610 Wörter (+12).

**Zwei verrutschte Prüfnotizen korrigiert (2026-07-29).** Die Vittuari-Urteile `bdf041cb6b` und `b7402f55c9` trugen in `quellencheck-state.json` wortwörtlich den Soma-Prüftext aus der Sitzung vom 28.07. – beim blockweisen Eintragen mehrerer `--verdikt`-Aufrufe war dieselbe `--notiz` mitgegeben worden. Die **Urteile selbst waren richtig**: Am Preprint nachgeprüft trägt Kap. 1, Abs. 3 beide zitierten Aussagen („a large part of the literature allocates the responsibility to consumers, particularly at the household level" sowie die Definitionen von *drivers* und *levers*); Abs. 1 nennt zusätzlich die Eurostat-Zahl von 65 % für Haushalte **und Food Service zusammen**. Beide Notizen sind jetzt ersetzt. **Warum das kein Randfall ist:** `pruefe()` überspringt jede mit OK quittierte Zitation vollständig, ohne den Volltext erneut zu öffnen, und kein Audit-Umfang ruft `--alle` auf – ein einmal vergebenes OK wird also nie wieder angesehen, und seine Notiz ist die einzige überlebende Begründung. Als **P2-3 in `SKILL-ANPASSUNGEN.md`** aufgenommen (zwei deterministische Wächter: Notiz-Dublette über verschiedene Bib-Keys melden, Autornennung in der Notiz gegen `author`-Feld und Trägersatz prüfen) – noch nicht umgesetzt.

**Delta-Re-Audit durchgeführt (2026-07-29, Score 77/100 — Überarbeitung empfohlen).** Erstmals seit dem 28.07. standen `lualatex`/`latexmk`/`biber` wieder zur Verfügung; der Umfang ist deshalb **am neu gebauten PDF gemessen, nicht geschätzt** (`latexmk -g -lualatex`, Exit 0, 24 Seiten). **Vier der fünf Befunde des Vorberichts sind verifiziert erledigt**: alle drei Vittuari-Stellen laufen jetzt einheitlich auf `[Kap.~1, Abs.~3]`, Soma in `04_community_und_feedback.tex:7` auf `[S. 10--12]`, die unbelegte Autorennennung in `06_evaluation_reflexion.tex:7` ist durch einen `\autoref` ersetzt, und die drei zuvor fehlenden Volltexte liegen vor — **Teil-Check G steht bei 19/19 OK**, keine WORTLAUT-, Zitat- oder Seitenbefunde mehr. Das war der größte Posten des Vorberichts (−47 von −57).

Drei Befunde bleiben, alle mechanisch, keiner betrifft These, Argumentation oder Aufbau: **(1) Seitenumfang unverändert 11 statt 7–10** (−10) — die Kürzung um 60 Wörter hat die Seite nicht gekippt; die elfte Seite trägt 66 Wörter, nötig sind rund 90–110. Fünf konkrete Kürzungsstellen priorisiert in `pruefbericht.md`, Schwerpunkt `06_evaluation_reflexion.tex` (Redundanz Reflexion ↔ Fazit: die zweite „Lehre" sagt dasselbe wie der Fazit-Schlusssatz, das Reichweitenrisiko ist eine Limitation im Reflexionskapitel). **(2) Overfull \hbox 11,54 pt** in der `\quelle{}`-Zeile unter `tab:wettbewerbsvergleich` (`01_wettbewerbsanalyse.tex:28`, −8) — die nicht trennbare Domainkette ragt gut 4 mm in den rechten Rand; der einzige Overfull \hbox im Dokument, Fix spart nebenbei ~6 Wörter. **(3) U+2010 im `title`-Feld von `nivedhithaHowGreenSocial2024`** (−5) — Arial hat keine Glyphe dafür, LuaTeX meldet 4× „Missing character", im gedruckten Literaturverzeichnis steht sichtbar „pro?environmental". Reiner Zotero-Fix. Befunde 2 und 3 sind Altlasten, keine Regressionen — sie waren nur nie messbar, weil in den letzten Audits kein Build lief. Nebenbefund: `kapitelplan.md:7` trägt unter „Gesamtwortzahl (Richtwert)" eine **Seiten**angabe (7–10), weshalb `check_umfang.py` den Zielabgleich stillschweigend überspringt.

**Überarbeitung nach Prüfbericht abgeschlossen (2026-07-29, diese Session).** Befund 2 und vier der fünf Kürzungsstellen aus Befund 1 umgesetzt (Details siehe Statuszeilen `sec:durchfuehrung`/`sec:fazit`/Prüf-Audit oben). `check_all.py` über beide betroffenen Kapitelordner: 0 FEHLER. `check_quellentreue.py --datei chapters/`: weiterhin 19/19 OK. Gemessen 3.533 Wörter (−77 gegenüber dem Audit-Stand). Befund 3 (U+2010 im Nivedhitha-Titel) bewusst nicht angefasst — reiner Zotero-Fix, `references.bib` wird nicht von Hand editiert.

**Zotero-Export-Ausfall behoben und Portabilität nachgerüstet (2026-07-29, neue Session).** Nach dem BBT-Export zur Vittuari-Aktualisierung fehlten in `references.bib` plötzlich drei ganze Einträge: `sauroLewisQuantifying2016`, `nielsenUsabilityEngineering1993`, `erlhoferWebsiteKonzeptionUndRelaunch2017` (Runde-7-Methodenfundierung). Ursache per Direktzugriff auf `zotero.sqlite` (nur lesend) geklärt: Diese drei standen nie als echte Zotero-Items in der Collection „Medienplattformen" – Erlhofer existierte nur in der Collection der Vorarbeit („UI Projekt"), Sauro & Lewis sowie Nielsen gar nicht in Zotero. Der erste **echte** BBT-Export (vorher liefen nur handgepflegte Bib-Einträge) hat sie deshalb gelöscht – exakt die Gefahr, vor der `PERSISTENT.md`/Statuszeilen bereits gewarnt hatten. Nutzer hat alle drei in Zotero ergänzt (Erlhofer zusätzlich der Collection zugeordnet, zwei neu angelegt), Citekeys per „Pin BibTeX key" auf die exakten Strings gepinnt, neu exportiert. Nach Re-Export: alle 9 Einträge vorhanden, `check_bib_keys.py` 0 FEHLER. Vittuari-Seitenversatz gesetzt (`--offset vittuariHowReduceConsumer2023=-103`, PDF-Index 0 = gedruckt S. 104) und die drei S.-105-Zitationen (Haushalts-Claim, Drivers/Levers-Definition) final gegen den Verlagstext verifiziert und mit OK quittiert — **Teil-Check G steht wieder bei 19/19 OK**.

**Grundsätzliches Portabilitäts-Problem erkannt und behoben (P4-1 in `SKILL-ANPASSUNGEN.md`).** Nutzer-Anlass: möchte gelegentlich von einem zweiten Rechner weiterarbeiten. Der BBT-Export hatte bei **allen neun** Einträgen den `file`-Pfad auf Zoteros absolute lokale Ablage umgeschrieben und jeden vorherigen `sources/literatur/…`-Verweis gelöscht — auf einem zweiten Rechner wäre damit **keine einzige** Quelle mehr auflösbar gewesen, da der bestehende Basename-Rückfall in `check_quellentreue.py` einen Dateinamen sucht, den Zotero/Anna's Archive vergeben hat, nicht den, den das Projekt für dieselbe Datei gewählt hat. Fix: `check_quellentreue.py` sucht jetzt **zuerst** `sources/literatur/<citekey>.pdf`/`.epub` — unabhängig vom `file`-Feld-Inhalt (neue Funktion `_projekt_pfad`, in `pdf_pfad()`/`epub_pfad()` vorrangig vor dem alten Pfad-Rückfall; `unreferenzierte_volltexte()` zählt Citekey-Dateien korrekt als referenziert). Alle neun zitierten Volltexte in `sources/literatur/` per `git mv` auf `<citekey>.<ext>` umbenannt (z. B. `vittuariHowReduceConsumer2023.pdf`). **Portabilität verifiziert, nicht nur behauptet:** Testlauf mit vollständig geleertem `file`-Feld in einer Bib-Kopie (`--bib <kopie> --alle`) — alle 19 Zitationen lösten weiterhin auf. Fünf neue Tests (`TestCitekeyFallback`), 265 Skript-Tests insgesamt grün. Regeltexte nachgezogen: `hard-rules-formal.md`, `plan-modus` Schritt 1, `setup-check` Schritt 6c, `projektstruktur.md` — Namenskonvention ist jetzt überall `<citekey>.<ext>` statt `<Autor><Jahr>_<Kurztitel>`. Bewusst nicht verfolgt: Zoteros „Base Directory for linked attachments" (GUI-seitige Alternative) — nicht git-versionierbar, nicht von hier aus verifizierbar, erfordert Umbau aller neun Attachments auf „Linked File". `check_all.py --mit-quellen`: 0 FEHLER.

**„Bitte manuell prüfen" aus dem Prüfbericht abgearbeitet (2026-07-29, diese Session):**

1. **Vittuari-Locator erledigt.** Nutzer hat die Verlagsfassung als PDF beschafft; per Metadaten- und Volltextabgleich verifiziert (`/Subject`-Feld: „Sustainable Production and Consumption, 38 (2023) 104-114. doi:10.1016/j.spc.2023.03.023" — exakte Übereinstimmung mit `references.bib`). Umbenannt zu `sources/literatur/Vittuari2023_DriversLeversFoodWaste_Verlagsfassung.pdf` (Konvention, unterscheidet sich vom bestehenden Preprint `Vittuari2023_DriversLeversFoodWaste.pdf`, der unangetastet bleibt). PDF ist 11 Seiten, durchgehend 1:1 auf die gedruckte Zählung 104–114 gemappt (Index 0 = S. 104). Die Drivers-/Levers-Definition **und** der Haushalts-Verantwortungs-Claim liegen beide auf **S. 105** (direkt am Volltext zitiert: „Under drivers, we define the factors that impact behaviour…"; „a large part of the literature highlights the prominent role of the responsibility to consumers, particularly at the household level"). Alle drei Fundstellen (`01_ausgangslage_und_problem.tex:5`, `03_konzept.tex:5`, `01_kernergebnisse_und_ausblick.tex:5`) von `[Kap.~1, Abs.~3]` auf `[S. 105]` umgestellt. `check_formalia.py`: 0 FEHLER. **Offen (Nutzer-Schritt, wie angekündigt):** `references.bib` bleibt unangetastet — der Nutzer verknüpft die neue Datei selbst in Zotero und exportiert per BBT neu. Bis dahin zeigt das `file`-Feld weiter auf den Preprint (23 S., keine Verlagspaginierung); `check_quellentreue.py --datei chapters/` meldet die drei Stellen deshalb übergangsweise als `SEITE VERDÄCHTIG` — kein neuer Fehler, sondern erwartetes Verhalten bis zum Zotero-Update. Nach dem Re-Export testen mit `check_quellentreue.py --seite vittuariHowReduceConsumer2023 105`; falls die Seitenzählung im Export nicht automatisch passt, `--offset vittuariHowReduceConsumer2023=<N>` setzen.
2. **Zitierleitfaden zu Folge-Prompts geprüft.** Kap. 2.2.5 (PDF-S. 20/21) verlangt für KI-Bilder wörtlich nur „**den** verwendeten Prompt" (Singular) — Muster: „Erstellt mit dem Prompt „…" durch [Unternehmen]." Der Leitfaden kennt keine Regelung für chat-basierte iterative Werkzeuge (Claude Design) und verlangt an keiner Stelle, dass alle Folge-Prompts eines Verfeinerungsdialogs zitiert werden müssen. Die Pflicht ist damit geringer als bisher angenommen. **Für die manuelle Prüfung heißt das:** Es muss nicht geklärt werden, ob alle Folge-Prompts zitiert werden müssen (nein) — sondern nur, ob die tatsächlich zitierten Prompt-Texte wörtlich dem entsprechen, was im Claude-Design-Chat eingegeben wurde (unverändert offen, siehe Prüfbericht Punkt 2). Fehlende Folge-Prompts wären kein Regelverstoß, nur weniger transparent als die im Projekt bisher gewählte Praxis.
3. **Style Tile:** Nutzer bestätigt keine manuelle Nachbearbeitung — `\quelle{}`-Zeile zu `fig:styletile` bleibt unverändert ohne den Zusatz „Abbildung nachträglich manuell bearbeitet.". Punkt erledigt, keine Änderung nötig.

**Lokaler Build durchgeführt und alle drei Delta-Re-Audit-Befunde verifiziert behoben (2026-07-29, diese Session — Ausnahme vom Grundsatz „Kompilieren ist Sache des Nutzers": Nutzer hatte explizit um Prüfung gebeten und selbst gerade gebaut, deshalb hier einmalig direkt nachgebaut statt nur Anweisungen zu geben).** Textteil exakt 10 Seiten (im Soll), 0 Overfull \hbox (Ursache war entgegen dem Vorbericht nicht die `\quelle{}`-Zeile, sondern die Tabellenkopfzelle „Nachhaltigkeits-/Restefokus" — Schrägstrich kein TeX-Umbruchpunkt, korrigiert mit manuellen Trennstellen), 0 Missing character (U+2010-Fix über Zotero griff, nach einem zunächst zu früh gebauten Log verifiziert). Build: 0 Fehler, 23 PDF-Seiten gesamt. Details in der Prüf-Audit-Zeile oben.

**Delta-Re-Audit gefahren (2026-07-29, 2. Lauf, Score 100/100 — abgabereif).** Keine score-relevanten Befunde mehr; Details in der Prüf-Audit-Statuszeile und in `pruefbericht.md`.

**Zotero-Arbeitsliste erledigt (2026-07-29, diese Session).** Alle 28 Hygiene-Hinweise in zwei Export-Runden abgearbeitet, Ergebnis am gerenderten Verzeichnis geprüft: Sentence Case bei sechs Einträgen (Nivedhitha hatte der Vorbericht fälschlich als korrekt geführt), `urldate` bei sechs geleert, URL neben DOI bei fünf entfernt, Sprachfeld bei vier gesetzt (Erlhofer `ngerman`), Verlagsort bei drei geleert, zwei Artikelnummern als `eid` (rendert als „Artikel 11099"/„Artikel 107706"). `check_bib_hygiene.py`: **0 Hinweise** (vorher 28). **Zusätzlicher Fund, den kein Skript sah:** Nielsen und Sauro & Lewis trugen den Autorennamen in Körperschafts-Klammern (`author = {{Jakob Nielsen}}`), wodurch der Vorname ausgeschrieben wurde und beide Einträge alphabetisch unter „J" standen; in Zotero auf zwei Namensfelder umgestellt, jetzt korrekt einsortiert. Gefunden nur durch Lesen des gerenderten Verzeichnisses — der Schritt, den Teil-Check F Punkt 2 vorschreibt. Nach dem Re-Export: Build sauber (0 Fehler, 0 Overfull, 0 Missing character), **Textteil unverändert 10 Seiten**, `check_all.py --mit-quellen` ohne harte Funde, Teil-Check G 19/19 OK. Damit entfällt der −5-Posten, den das Abgabe-Audit für die Sentence-Case-Klasse angesetzt hätte.

**Gegenlesung Runde 8 angelegt (2026-07-29, diese Session) — „Offen" ist damit nicht mehr leer.** Kalte Prüfer-Gegenlesung gegen `aufgabe.md`, ausgeführt von einem Subagenten unter striktem Leseverbot (kein Kapitelplan, kein Prüfbericht, kein `AENDERUNGEN.md`-Inhalt, keine Git-Historie, kein Projektstatus), weil die Hauptsession über den auto-geladenen Status bereits die vollständige Prüfhistorie trug. Zusatzprämisse des Nutzers: **Abgabe innerhalb von Woche 3** — alle Zeit-, Phasen- und Prozessaussagen wurden dagegen geprüft und in einer eigenen Tabelle dokumentiert. 18 Befunde plus eine Gegenfinanzierungsreserve (8.1–8.19). **Kein fehlendes Artefakt, keine nicht ausgeführte Teilaufgabe, keine Text-Bild-Lücke in den Mockups** — alle sechs Lieferobjekte existieren, alle Schlüsselbegriffe sind umgesetzt oder begründet ausgelassen, und jede im Text behauptete Oberflächen-Eigenschaft ist im Bild belegt (Element für Element geprüft). Schwerste Punkte: 8.1 `[FREIGABE]` (die Auswahl der sechs verglichenen Plattformen ist nirgends begründet, obwohl sie die Beweisgrundlage der Marktlücke ist — und fünf von sechs sind keine Social-Media-Plattformen, während die Aufgabe genau die verlangt), 8.3 (`07_iteratives_design.tex:3` „bereits durchlaufen" gegen `:5` „nicht real durchgeführt" — betrifft ausgerechnet die Ehrlichkeitsklausel), 8.4 (Woche-3-Prämisse: „Die nächste Schleife folgt in der Design-Phase (Woche 3–4)" kündigt als bevorstehend an, worin die Arbeit bei Abgabe steht; der Rest der Phasenplanung ist robust und hängt an dem Satz „Die Wochenangaben zählen ab Projektstart"), 8.5 (die einzige Innovationsaussage trifft keinen der drei von der Aufgabe genannten Zwecke und erklärt deren drei Funktionen ausdrücklich als „bewusst übernommen" — verschenkte Substanz bei Kreativität 15 %), 8.2 (`tab:funktionsuebersicht` ist Lieferobjekt und einziger von neun Floats ohne `\autoref`), 8.10 (die zitierten Folge-Prompts der beiden Startseiten-Screens beschreiben eine Karte mit Wochentagen, im Bild steht ein Banner ohne Wochentage). `[FREIGABE]`: 8.1 und 8.9. Umfang: 3.567 Wörter bei zuletzt gemessenen 10 Seiten — die Runde ist selbstfinanzierend gebaut (+105 gegen −159 Wörter, netto ca. −55). **Wichtiger Fallstrick beim Abarbeiten:** Der in 8.14 zu streichende Abhaklisten-Satz in `06_evaluation_reflexion.tex:3` enthält den **einzigen** `\autoref{sec:iteratives_design}` des ganzen Dokuments — Verweis mitnehmen, nicht ersatzlos streichen. Ein eigener Abschnitt der Runde listet zehn Stellen, die nicht angefasst werden dürfen (u. a. die dreischichtigen Limitationen, die Claim-Abgrenzung in `03_konzept.tex:11`, der die Phasenplanung tragende Wochen-Satz, und ausdrücklich die Kontrastfiguren, die an der Grenze, aber nicht darüber liegen).

Die Runde trägt eine eigene Sektion **„Bitte manuell prüfen"** mit vier Punkten, die die Gegenlesung nicht selbst verifizieren konnte: der tatsächlich eingegebene Wochenplan-Folge-Prompt (Voraussetzung für 8.10 Option A, sonst gilt Option B), ob Barker Feedback als Nudge-Typ führt (Voraussetzung für 8.8 Option A, sonst Option B), die Seitenzahl von SDG 12.3 im UN-PDF (8.18) und die gedruckte Seitenzahl nach der Umsetzung. Jeder Punkt nennt Datei/Stelle, Erkennungsmerkmal und Ausweichoption — **vor** der jeweils abhängigen Option abarbeiten, nicht danach.

**Gegenlesung Runde 9 angelegt (2026-07-29, dieselbe Session) — zugeschnitten auf Zitatkontext.** Auf Nutzerwunsch ein zweiter Subagent unter demselben Leseverbot, **erweitert um eine Lesefreigabe für `references.bib` (inhaltlich) und die zehn Volltexte in `sources/literatur/`**; zusätzlich gesperrt waren `quellencheck.md`/`quellencheck-state.json` und die Ausführung von `check_quellentreue.py`, weil sie die Urteile früherer Läufe enthalten. Geprüft wurden **alle 19 Zitationen** gegen den Volltext, gelesen im Kontext des Absatzes, entlang von sieben Achsen (Deckung, Richtung, Reichweite, Locator, Ankündigung↔Einlösung, Attribution, Doppelnutzung). **Kein Fall von „nicht auffindbar", kein Fall von falscher Richtung, kein unbelegter Autorenname** — die Zitationen sind handwerklich in Ordnung. Die zehn Befunde betreffen durchweg die **Reichweite**. Schwerster Punkt: **9.3** — das Fazit schreibt „die **belegte** Wirksamkeit von Gamification `\parencite[S. 9]{soma…}`", während Soma auf derselben Seite keine signifikante Gruppendifferenz berichtet (Kruskal-Wallis p = 0,30, Spielgruppe nur marginal p = 0,07) und selbst nur von „promising" spricht; `03_konzept.tex:11` schränkt dieselbe Quelle mustergültig ein, im Fazit fällt die Einschränkung weg — die Aussage wird auf dem Weg durch die Arbeit stärker statt schwächer. Weiter: **9.1** (Barker trägt Feedback **nicht**, nur Reminders — die Ankündigung „zwei Befunde" und die Tabellenzelle `03_konzept.tex:22` sind überdehnt), **9.2** (Somas Vergleichsarm waren **Präsenz-Workshops**, kein Diskussionsformat; die Ausfallgründe sind reine Präsenzhürden und übertragen sich auf ein Online-Forum gerade nicht — betrifft die einzige mit einer Studie begründete Weglassungsentscheidung), **9.4** (der signifikante Mann-Whitney-Test prüft das Nachher-Niveau, nicht die Differenz 51 % vs. 39 %), **9.5** (Nivedhithas Mediator ist „collective intention" = geteilte Handlungsabsicht, nicht Zugehörigkeitsgefühl — die Quelle grenzt beides ausdrücklich ab; korrekt gelesen wird die Ableitung sogar stärker), **9.6** (drei fehlende Locatoren, alle drei Fundstellen jetzt am Volltext verifiziert: UN S. 22, Shen S. 4 und 6, Barker S. 10), **9.7** (Erlhofer nennt **fünf** Testpersonen, nicht 10–15; dazu Numerusfehler „gibt" statt „geben" bei zwei Autor:innen). Keine `[FREIGABE]`, kein Befund berührt These oder Kernargumente. Netto rund ±0 Wörter bei der empfohlenen Kombination (9.2 voll, 9.1 in der Sparvariante). **Zwei manuelle Prüfpunkte der Runde 8 sind dadurch erledigt** (Barker/Feedback → Option B gilt; SDG-Seite → S. 22 bestätigt) und dort als solche markiert. Runde 9 nennt zusätzlich vier Reichweiten-Begrenzungssätze, die einer Kürzungsrunde nicht zum Opfer fallen dürfen.

**Nächster Schritt**: **„Schreib-Modus: AENDERUNGEN.md abarbeiten" in frischer Session — Runde 8 und 9 zusammen**, da sie teils dieselben Dateien berühren (`03_konzept.tex`, `01_kernergebnisse_und_ausblick.tex`, `07_iteratives_design.tex`); getrennte Durchgänge würden dieselben Absätze zweimal umbauen. Reihenfolge: erst die kleinteiligen Zitat-Befunde der Runde 9, dann Runde 8. Dabei 8.1 und 8.9 per `AskUserQuestion` klären, die verbliebenen „Bitte manuell prüfen"-Punkte vorab abarbeiten, netto ≤ 0 Wörter halten, am Ende bauen und die gedruckte Seitenzahl gegenprüfen (der Textteil steht exakt auf der Obergrenze 10, `check_umfang.py` zählt zu niedrig). Danach manuelles Durchlesen plus LanguageTool, dann `pages/meta.tex` (`\ModuleCode`, `\MatrikelNr` noch Platzhalter) und die Eidesstattliche Erklärung, **zuletzt das Abgabe-Audit**. Ein Gesamt-Stresstest bleibt beim Papiertyp Projektbericht optional (Kompakt-Prüfkette).

**Offene Fragen**: Keine blockierenden Punkte, keine offene Nutzer-Entscheidung. `AENDERUNGEN.md` Befund 8.17 (Kapoor et al. 2018 als Einstiegsliteratur) ist auf Nutzerwunsch **endgültig als Verzicht entschieden** (2026-07-29) — soll in künftigen Gegenlesungen nicht erneut aufgemacht werden. Erledigt am 2026-07-29 nach dem Delta-Re-Audit, in derselben Session: ~~Haushalts-Claim in `01_ausgangslage_und_problem.tex:5`~~ **nach Rückfrage umformuliert** — „… entsteht dabei nicht im Handel oder in der Industrie, sondern auf der Verbraucherebene und dort vor allem in privaten Haushalten …". Bildet den Quellensatz (Vittuari S. 105: „the responsibility to consumers, particularly at the household level") Ebene für Ebene ab und ist damit enger als die Quelle statt weiter; am Volltext gegengeprüft und neu quittiert (Hash `3ead511986`, alter Hash `d7fd6058a1` entfällt mit dem alten Satz). Ebenfalls erledigt: App-Name in der Quellenzeile `01_wettbewerbsanalyse.tex:29` auf `\enquote{Zu gut für die Tonne!}` angeglichen. Bewusst **nicht** umgesetzt (Nutzer-Entscheidung): der Halbsatz zur leftovercooking-Wertung in `:13` — bleibt ein Hinweis ohne FAIL, eine spätere Ergänzung müsste gegenfinanziert werden.

Nutzer-Restpflichten (nicht-blockierend): Namensverfügbarkeit „Resteria" (DPMA / .de-Domain) · ~~Nivedhitha/Shen/UN-Resolution ohne Volltext (Befund 5)~~ **erledigt 2026-07-29: Volltexte liegen vor, in Zotero verknüpft, alle 19 Zitationen am Volltext mit OK quittiert** · ~~Rettungs-Feed-Mockup Plus-Button-Überlapp (Befund 6.3)~~ **erledigt, am 28.07. per Bildsichtung verifiziert behoben** · ~~lokaler Build steht aus~~ **erledigt 2026-07-29: Textteil gemessen 10 Seiten, im Soll** — die Rückfrage zur Sechs-Plattform-Wettbewerbsanalyse ist damit gegenstandslos · ~~Vittuari-Zitationen laufen auf `[Kap.~1, Abs.~3]` statt Seitenzahl~~ **erledigt 2026-07-29: Verlagsfassung beschafft, alle drei Stellen auf `[S. 105]` umgestellt und am Volltext verifiziert**.

**Fristen**: Abgabe: kein festes Datum (`\today` auf Nutzerwunsch, siehe `PERSISTENT.md`) · Eidesstattliche Erklärung (myCampus): [offen]

> Aktualisiert nach jedem Checkpoint bzw. jeder gespeicherten Datei, sichtbar quittiert. Für Planungsinhalte gilt bei Widerspruch der Plan/Draft, für Statuswerte diese Tabelle.
